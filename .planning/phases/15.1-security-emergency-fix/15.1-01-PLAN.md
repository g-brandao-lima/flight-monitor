# Phase 15.1 Plan 01: Security Emergency Fix

**Goal**: Eliminar vazamento de dados entre usuarios na API REST e adicionar rate limit emergencial no login.

## TDD Cycle

### RED

Criar `tests/test_data_isolation_api.py` com:

1. `test_list_route_groups_isolates_by_user` - GET `/api/v1/route-groups/` retorna apenas grupos do usuario logado
2. `test_get_other_user_group_returns_404` - GET `/api/v1/route-groups/{id}` de grupo de outro user retorna 404
3. `test_patch_other_user_group_returns_404` - PATCH idem 404
4. `test_delete_other_user_group_returns_404` - DELETE idem 404
5. `test_create_assigns_current_user_id` - POST atribui user_id do usuario logado automaticamente
6. `test_unauthenticated_request_gets_redirect` - chamada sem sessao segue middleware (303 redirect)

Rodar: devem falhar com os endpoints atuais.

### GREEN

1. Adicionar `get_required_user` em `app/auth/dependencies.py` (retorna User ou levanta 401)
2. Refatorar `app/routes/route_groups.py`:
   - Injetar `UserDep = Annotated[User, Depends(get_required_user)]` em todos endpoints
   - `list`: filtrar `RouteGroup.user_id == current_user.id`
   - `get/patch/delete`: filtrar no query, 404 se nao for do user
   - `create`: setar `user_id=current_user.id` no modelo
3. Rodar testes: todos verdes
4. Rodar suite completa: garantir 0 regressoes

### Rate Limit

1. Adicionar `slowapi==0.1.9` no `requirements.txt`
2. `main.py`: criar limiter, registrar handler de 429, adicionar middleware
3. `app/auth/routes.py`: decorar `/auth/google` com `@limiter.limit("10/minute")`
4. Teste: `test_auth_rate_limit.py` - 11 requests rapidas a `/auth/google` retornam 429 na 11a

### Refactor

- Limpar imports
- Verificar que `get_current_user` continua exportado (ainda usado em outros lugares)

## Verification

- [ ] 6 novos testes de isolamento API passando
- [ ] Todos os testes existentes continuam passando (0 regressoes)
- [ ] Rate limit funciona (teste de 11 requests)
- [ ] `slowapi` adicionado em requirements.txt

## Commit

`security(15.1): isolate route groups by user and add login rate limit`
