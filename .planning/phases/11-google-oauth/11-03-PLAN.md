---
phase: 11-google-oauth
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - app/templates/base.html
  - app/routes/dashboard.py
  - render.yaml
  - .env.example
  - tests/test_auth.py
autonomous: false
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Header exibe avatar (foto ou iniciais) + primeiro nome + botao Sair quando logado"
    - "Header exibe botao 'Entrar com Google' quando nao logado"
    - "Avatar sem foto mostra circulo com iniciais do nome em cor accent (per D-08)"
    - "Botao Sair aparece em todas as paginas autenticadas (per D-03)"
    - "render.yaml tem variaveis de ambiente para Google OAuth"
    - "Todos os testes passando"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Header condicional com avatar/nome/logout ou botao login"
      contains: "user-menu"
    - path: "render.yaml"
      provides: "Variaveis de ambiente para deploy"
      contains: "GOOGLE_CLIENT_ID"
  key_links:
    - from: "app/templates/base.html"
      to: "user variable"
      via: "Jinja2 context"
      pattern: "\\{%.*if user.*%\\}"
    - from: "app/routes/dashboard.py"
      to: "app/auth/dependencies.py"
      via: "get_current_user dependency"
      pattern: "get_current_user"
---

<objective>
Adicionar UI de usuario no header (avatar, nome, logout) e preparar deploy com variaveis de ambiente. Checkpoint visual para validar aparencia.

Purpose: Sem o header atualizado, o usuario nao ve quem esta logado nem tem como sair. Sem render.yaml atualizado, deploy quebra.
Output: Header com avatar/nome/logout, render.yaml com env vars, checkpoint visual aprovado.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-google-oauth/11-CONTEXT.md
@.planning/phases/11-google-oauth/11-RESEARCH.md
@.planning/phases/11-google-oauth/11-01-SUMMARY.md
@.planning/phases/11-google-oauth/11-02-SUMMARY.md

<interfaces>
<!-- Interfaces dos planos anteriores -->

From app/auth/dependencies.py:
```python
def get_current_user(request: Request, db: Session = Depends(get_db)) -> User | None
```

From app/models.py:
```python
class User(Base):
    __tablename__ = "users"
    id, google_id, email, name, picture_url, created_at
```

From app/templates/base.html (header-actions block):
```html
<div class="header-actions">
    {% block header_actions %}
    <a href="/groups/create" class="btn btn-primary">+ Novo Grupo</a>
    {% endblock %}
</div>
```

Design system existente:
- Background: #0b0e14
- Text: #e2e8f0
- Accent blue: #3b82f6
- Font: Inter
- Button ghost: border #1e293b, color #94a3b8
- Button primary: bg #3b82f6
- Glassmorphism header: rgba(11, 14, 20, 0.88) + backdrop-filter blur(12px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header com avatar/nome/logout e injecao de user no contexto Jinja2</name>
  <files>app/templates/base.html, app/routes/dashboard.py, tests/test_auth.py</files>
  <read_first>app/templates/base.html, app/routes/dashboard.py, app/auth/dependencies.py, tests/test_auth.py, tests/conftest.py</read_first>
  <behavior>
    - test_header_shows_user_info: GET / com authenticated_client retorna HTML contendo nome do usuario e img com src da foto
    - test_header_shows_initials_without_photo: GET / com authenticated_client (user sem picture_url) retorna HTML contendo div.avatar-initials com iniciais do nome
    - test_header_shows_login_button_when_not_logged: GET / sem sessao retorna HTML contendo "Entrar com Google" e link para /auth/login
    - test_header_shows_logout_on_all_pages: GET /groups/create com authenticated_client retorna HTML contendo "Sair" e link para /auth/logout
  </behavior>
  <action>
**TESTES PRIMEIRO (RED):**
Adicionar 4 testes ao tests/test_auth.py existente (ou ao final do arquivo) cobrindo o comportamento do header.

**IMPLEMENTACAO (GREEN):**

1. Atualizar TODAS as rotas em app/routes/dashboard.py que renderizam templates para injetar `user` no contexto Jinja2:
- Adicionar import: `from app.auth.dependencies import get_current_user`
- Em cada rota que faz `templates.TemplateResponse(...)`, adicionar `Depends(get_current_user)` como parametro e passar `"user": user` no context dict.
- Rotas afetadas: `dashboard_index`, `create_group_page`, `create_group_form` (erro), `dashboard_detail`, `edit_group_page`, `edit_group_form` (erro).
- A rota `/` (dashboard_index) e publica per D-05, entao user pode ser None.

2. Atualizar app/templates/base.html no bloco header-actions:
Substituir o conteudo default do bloco `header_actions` por:
```html
{% block header_actions %}
{% if user %}
    <div class="user-menu">
        {% if user.picture_url %}
            <img src="{{ user.picture_url }}" alt="" class="avatar" width="32" height="32" referrerpolicy="no-referrer">
        {% else %}
            <div class="avatar-initials">{{ user.name[:1].upper() }}{{ user.name.split(' ')[-1][:1].upper() if ' ' in user.name else '' }}</div>
        {% endif %}
        <span class="user-name">{{ user.name.split(' ')[0] }}</span>
        <a href="/auth/logout" class="btn btn-ghost btn-sm">Sair</a>
    </div>
{% else %}
    <a href="/auth/login" class="btn btn-primary">Entrar com Google</a>
{% endif %}
{% endblock %}
```

3. Adicionar CSS no base.html (dentro do style existente):
```css
/* User menu */
.user-menu {
    display: flex;
    align-items: center;
    gap: 10px;
}
.avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid rgba(255,255,255,0.1);
}
.avatar-initials {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #3b82f6;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: -0.02em;
}
.user-name {
    font-size: 13px;
    font-weight: 500;
    color: #94a3b8;
}
.btn-sm {
    padding: 4px 12px;
    min-height: 28px;
    font-size: 12px;
}
@media (max-width: 768px) {
    .user-name { display: none; }
    .user-menu { gap: 6px; }
}
```

Per D-04: layout do avatar respeita design existente (glassmorphism, Inter font, paleta dark mode #0b0e14). Per D-08: iniciais em circulo com cor accent #3b82f6.

4. IMPORTANTE: Para paginas que herdam base.html e fazem override de header_actions (verificar se alguma faz), garantir que o bloco header_actions no template filho tambem considere o user. Se nenhum template filho faz override de header_actions, o default do base.html sera usado em todas as paginas.
  </action>
  <verify>
    <automated>python -m pytest tests/test_auth.py -x -v && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "user-menu" app/templates/base.html retorna 0
    - grep -q "avatar-initials" app/templates/base.html retorna 0
    - grep -q "Entrar com Google" app/templates/base.html retorna 0
    - grep -q "auth/logout" app/templates/base.html retorna 0
    - grep -q "get_current_user" app/routes/dashboard.py retorna 0
    - python -m pytest tests/test_auth.py -x -v passa todos os testes de auth (incluindo os 4 novos de header)
    - python -m pytest tests/ -x -q passa todos os testes
  </acceptance_criteria>
  <done>Header exibe avatar (foto ou iniciais) + nome + Sair quando logado, e "Entrar com Google" quando nao logado. Todas as rotas injetam user no contexto Jinja2. Todos os testes passando.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar render.yaml e .env.example com variaveis OAuth</name>
  <files>render.yaml, .env.example</files>
  <read_first>render.yaml, .env.example</read_first>
  <action>
1. Atualizar render.yaml adicionando as novas variaveis de ambiente (como sync:false, configuradas no dashboard do Render):
```yaml
- key: GOOGLE_CLIENT_ID
  sync: false
- key: GOOGLE_CLIENT_SECRET
  sync: false
- key: SESSION_SECRET_KEY
  sync: false
```

2. Atualizar .env.example (se existir) ou criar, adicionando:
```
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET_KEY=gere-uma-string-aleatoria-de-32-chars
```

3. Verificar se o Gunicorn/Uvicorn no render.yaml tem `--forwarded-allow-ips="*"` para confiar nos headers X-Forwarded-Proto do Render (necessario para redirect_uri HTTPS). Se nao tiver, adicionar ao startCommand. Exemplo:
```yaml
startCommand: gunicorn main:app -w 2 -k uvicorn.workers.UvicornWorker --forwarded-allow-ips="*"
```
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "GOOGLE_CLIENT_ID" render.yaml retorna 0
    - grep -q "GOOGLE_CLIENT_SECRET" render.yaml retorna 0
    - grep -q "SESSION_SECRET_KEY" render.yaml retorna 0
    - grep -q "forwarded-allow-ips" render.yaml retorna 0
    - python -m pytest tests/ -x -q passa todos os testes
  </acceptance_criteria>
  <done>render.yaml e .env.example atualizados com variaveis de OAuth. Gunicorn configurado para confiar em proxy headers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual do fluxo de autenticacao</name>
  <files></files>
  <action>
Verificacao visual do fluxo completo de autenticacao Google OAuth implementado nos planos 01, 02 e 03:
- Login com Google (rota /auth/login)
- Sessao persistente (cookie assinado, max_age 1 ano)
- Logout (rota /auth/logout)
- Header com avatar/nome/Sair quando logado, "Entrar com Google" quando nao logado
- Middleware protegendo rotas (redireciona nao-logados para /)
- Iniciais no avatar quando usuario nao tem foto de perfil
- Flash messages para erros de auth

Passos de verificacao:
1. Iniciar servidor local: `python main.py`
2. Abrir http://localhost:8000 no navegador
3. Verificar que o header mostra botao "Entrar com Google" (nao logado)
4. Tentar acessar http://localhost:8000/groups/create diretamente
5. Verificar que redireciona para / com mensagem "Faca login para acessar"
6. Verificar aparencia geral do header: avatar/nome/logout coerente com design dark mode
7. (Opcional) Se Google Cloud Console estiver configurado: clicar "Entrar com Google", autorizar, verificar redirect para /dashboard com avatar no header
  </action>
  <verify>Verificacao visual pelo usuario</verify>
  <done>Usuario aprovou a aparencia e o comportamento do fluxo de autenticacao.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passa todos os testes
- `python -m pytest tests/test_auth.py -v` mostra todos os testes de auth passando
- Header exibe avatar/nome/logout quando logado
- Header exibe "Entrar com Google" quando nao logado
- render.yaml tem variaveis de OAuth
</verification>

<success_criteria>
1. Header exibe nome e foto do usuario logado (AUTH-04)
2. Avatar sem foto mostra iniciais em circulo accent (per D-08)
3. Botao Sair visivel em todas as paginas autenticadas (AUTH-03)
4. "Entrar com Google" visivel quando nao logado
5. Flash de erro exibida na landing quando auth falha (AUTH-05)
6. render.yaml preparado para deploy
7. Checkpoint visual aprovado pelo usuario
</success_criteria>

<output>
After completion, create `.planning/phases/11-google-oauth/11-03-SUMMARY.md`
</output>
