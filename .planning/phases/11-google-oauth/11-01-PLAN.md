---
phase: 11-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/config.py
  - app/models.py
  - app/auth/__init__.py
  - app/auth/dependencies.py
  - tests/conftest.py
  - alembic/versions/xxxx_add_users_table.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Modelo User existe no banco com campos google_id, email, name, picture_url, created_at"
    - "Alembic migration cria tabela users com indice unico em google_id"
    - "Authlib esta instalado e presente em requirements.txt"
    - "Config tem campos google_client_id, google_client_secret, session_secret_key"
    - "Todos os 188+ testes existentes continuam passando"
    - "Fixture test_user cria um usuario no banco de teste"
    - "Fixture authenticated_client permite requests autenticadas nos testes"
  artifacts:
    - path: "app/models.py"
      provides: "User model com google_id, email, name, picture_url, created_at"
      contains: "class User"
    - path: "app/auth/__init__.py"
      provides: "Pacote auth"
    - path: "app/auth/dependencies.py"
      provides: "get_current_user dependency"
      exports: ["get_current_user"]
    - path: "app/config.py"
      provides: "Settings com google_client_id, google_client_secret, session_secret_key"
      contains: "google_client_id"
    - path: "tests/conftest.py"
      provides: "Fixtures test_user e authenticated_client"
      contains: "test_user"
  key_links:
    - from: "tests/conftest.py"
      to: "app/models.py"
      via: "User model import"
      pattern: "from app.models import.*User"
    - from: "app/auth/dependencies.py"
      to: "app/models.py"
      via: "User model import"
      pattern: "from app.models import User"
---

<objective>
Criar a fundacao de autenticacao: modelo User, migration Alembic, instalar Authlib, configurar settings, e preparar fixtures de teste ANTES de qualquer middleware (para nao quebrar os 188+ testes existentes).

Purpose: Sem esta fundacao, adicionar middleware ou rotas OAuth quebraria todos os testes existentes. Fixtures primeiro, middleware depois.
Output: User model no banco, authlib instalado, config com credenciais Google, fixtures de teste prontas.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-google-oauth/11-CONTEXT.md
@.planning/phases/11-google-oauth/11-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que o executor precisa conhecer -->

From app/database.py:
```python
from sqlalchemy.orm import DeclarativeBase
class Base(DeclarativeBase): pass
def get_db():
    db = SessionLocal()
    try: yield db
    finally: db.close()
```

From app/models.py (modelos existentes):
```python
class RouteGroup(Base):
    __tablename__ = "route_groups"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    # ... 12 campos

class FlightSnapshot(Base):
    __tablename__ = "flight_snapshots"
    route_group_id: Mapped[int] = mapped_column(ForeignKey("route_groups.id"))

class BookingClassSnapshot(Base):
    __tablename__ = "booking_class_snapshots"

class DetectedSignal(Base):
    __tablename__ = "detected_signals"
```

From app/config.py:
```python
class Settings(BaseSettings):
    database_url: str = "sqlite:///./flight_monitor.db"
    serpapi_api_key: str = ""
    gmail_sender: str = ""
    gmail_app_password: str = ""
    gmail_recipient: str = ""
    app_base_url: str = "http://localhost:8000"
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}
```

From tests/conftest.py:
```python
@pytest.fixture(name="db")
def db_fixture(): ...

@pytest.fixture(name="client")
def client_fixture(db): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: User model + config + Authlib + Alembic migration</name>
  <files>requirements.txt, app/config.py, app/models.py, app/auth/__init__.py, app/auth/dependencies.py</files>
  <read_first>app/models.py, app/config.py, app/database.py, requirements.txt, alembic/env.py, alembic.ini</read_first>
  <action>
1. Instalar authlib: `pip install authlib` e adicionar `authlib==1.6.9` ao requirements.txt (manter ordem alfabetica).

2. Em app/config.py, adicionar 3 campos ao Settings (per D-03):
   - `google_client_id: str = ""`
   - `google_client_secret: str = ""`
   - `session_secret_key: str = "dev-secret-change-in-production"`

3. Em app/models.py, adicionar classe User (ANTES das outras classes, logo apos os imports):
```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    google_id: Mapped[str] = mapped_column(String(255), unique=True, index=True)
    email: Mapped[str] = mapped_column(String(255), unique=True)
    name: Mapped[str] = mapped_column(String(255))
    picture_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
    created_at: Mapped[datetime.datetime] = mapped_column(
        DateTime, server_default=func.now()
    )
```
NAO adicionar relationship com RouteGroup ainda (isso e Phase 12 - Data Isolation).

4. Criar diretorio app/auth/ com __init__.py vazio.

5. Criar app/auth/dependencies.py com get_current_user:
```python
from fastapi import Depends, Request
from sqlalchemy.orm import Session
from app.database import get_db
from app.models import User

def get_current_user(request: Request, db: Session = Depends(get_db)) -> User | None:
    user_id = request.session.get("user_id")
    if not user_id:
        return None
    return db.get(User, user_id)
```

6. Gerar migration Alembic: `alembic revision --autogenerate -m "add users table"`. Verificar que a migration gerada contem create_table("users") com colunas id, google_id, email, name, picture_url, created_at, e indices unique em google_id e email.

7. Atualizar .env.example (se existir) com as novas variaveis:
```
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET_KEY=
```
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "authlib" requirements.txt retorna 0
    - grep -q "google_client_id" app/config.py retorna 0
    - grep -q "google_client_secret" app/config.py retorna 0
    - grep -q "session_secret_key" app/config.py retorna 0
    - grep -q "class User" app/models.py retorna 0
    - grep -q "google_id" app/models.py retorna 0
    - grep -q "def get_current_user" app/auth/dependencies.py retorna 0
    - python -c "from app.models import User; print(User.__tablename__)" imprime "users"
    - ls alembic/versions/*add_users_table*.py encontra o arquivo de migration
    - python -m pytest tests/ -x -q passa todos os 188+ testes
  </acceptance_criteria>
  <done>User model definido, authlib instalado, config com credenciais Google, migration gerada, dependency get_current_user criada, todos os testes existentes passando.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures de teste para autenticacao (test_user + authenticated_client)</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, app/models.py, app/auth/dependencies.py</read_first>
  <action>
Atualizar tests/conftest.py para adicionar fixtures de autenticacao. CRITICO: estas fixtures devem existir ANTES de qualquer middleware ser adicionado (Plan 02).

1. Adicionar import de User no topo: `from app.models import User`

2. Adicionar fixture test_user (apos a fixture db):
```python
@pytest.fixture(name="test_user")
def test_user_fixture(db):
    user = User(
        google_id="google-test-123",
        email="test@gmail.com",
        name="Test User",
        picture_url=None,
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user
```

3. Adicionar fixture authenticated_client (apos test_user):
```python
@pytest.fixture(name="authenticated_client")
def authenticated_client_fixture(client, test_user):
    from app.auth.dependencies import get_current_user
    from main import app as test_app
    test_app.dependency_overrides[get_current_user] = lambda: test_user
    yield client
    # cleanup: client_fixture ja faz dependency_overrides.clear()
```

4. IMPORTANTE: Modificar a fixture `client` existente para tambem injetar SessionMiddleware no app de teste, para que `request.session` funcione quando o middleware for adicionado no Plan 02. Adicionar ANTES de criar o TestClient:
```python
from starlette.middleware.sessions import SessionMiddleware
# Verificar se SessionMiddleware ja esta adicionado (evitar duplicar)
has_session = any(
    getattr(m, "cls", None) is SessionMiddleware
    for m in getattr(app, "user_middleware", [])
)
if not has_session:
    app.add_middleware(SessionMiddleware, secret_key="test-secret-key")
```

NAO: Nao mexer na fixture `client` se isso quebrar testes. Se quebrar, adicionar o SessionMiddleware somente na fixture `authenticated_client` criando um TestClient separado.

5. Executar todos os testes e garantir que passam: `python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_user" tests/conftest.py retorna 0
    - grep -q "authenticated_client" tests/conftest.py retorna 0
    - grep -q "google-test-123" tests/conftest.py retorna 0
    - python -m pytest tests/ -x -q passa todos os 188+ testes (regressao zero)
    - python -c "import tests.conftest" nao gera erro de import
  </acceptance_criteria>
  <done>Fixtures test_user e authenticated_client existem em conftest.py. Todos os 188+ testes existentes continuam passando sem alteracao.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passa todos os testes (188+)
- `python -c "from app.models import User; print(User.__tablename__)"` imprime "users"
- `python -c "from app.auth.dependencies import get_current_user"` nao gera erro
- `pip show authlib` mostra authlib instalado
- Arquivo de migration Alembic existe em alembic/versions/
</verification>

<success_criteria>
1. User model com google_id, email, name, picture_url, created_at definido em app/models.py
2. Alembic migration para tabela users gerada
3. Authlib instalado e em requirements.txt
4. Config tem google_client_id, google_client_secret, session_secret_key
5. get_current_user dependency criada em app/auth/dependencies.py
6. Fixtures test_user e authenticated_client em conftest.py
7. Zero testes quebrados (regressao)
</success_criteria>

<output>
After completion, create `.planning/phases/11-google-oauth/11-01-SUMMARY.md`
</output>
