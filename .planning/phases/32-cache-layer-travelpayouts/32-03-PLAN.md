---
phase: 32-cache-layer-travelpayouts
plan: 03
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - app/services/route_cache_service.py
  - app/services/flight_search.py
  - app/scheduler.py
  - tests/test_route_cache_service.py
  - tests/test_flight_search_route_cache.py
autonomous: true
requirements: [CACHE-04, CACHE-05, CACHE-07]
must_haves:
  truths:
    - "Cron APScheduler 'travelpayouts_refresh' roda a cada 6h e popula route_cache"
    - "search_flights_ex() consulta route_cache ANTES de chamar SerpAPI"
    - "Cache hit em route_cache retorna flights sinteticos com source='travelpayouts_cached' e was_cache_hit=True"
    - "Cache miss chama SerpAPI normalmente e grava resultado em route_cache"
    - "Quota SerpAPI cai porque polling em massa vira responsabilidade do Travelpayouts"
  artifacts:
    - path: "app/services/route_cache_service.py"
      provides: "refresh_top_routes() e get_cached_price()"
      contains: "def refresh_top_routes"
    - path: "app/scheduler.py"
      provides: "Job travelpayouts_refresh a cada 6h"
      contains: "travelpayouts_refresh"
    - path: "app/services/flight_search.py"
      provides: "search_flights_ex consulta route_cache primeiro"
      contains: "route_cache_service"
  key_links:
    - from: "app/scheduler.py"
      to: "app/services/route_cache_service.refresh_top_routes"
      via: "APScheduler CronTrigger hour='*/6'"
      pattern: "travelpayouts_refresh"
    - from: "app/services/flight_search.py"
      to: "app/services/route_cache_service.get_cached_price"
      via: "lookup antes de SerpAPI"
      pattern: "get_cached_price"
    - from: "app/services/route_cache_service.py"
      to: "app/services/travelpayouts_client.TravelpayoutsClient"
      via: "fetch_calendar"
      pattern: "TravelpayoutsClient"
---

<objective>
Criar `route_cache_service` com `refresh_top_routes()` (cron 6h via APScheduler) e `get_cached_price()` (consumido por `search_flights_ex`). Refatorar `flight_search.search_flights_ex` para consultar o cache persistente ANTES do SerpAPI. Registrar job no scheduler.

Purpose: Eliminar gargalo SerpAPI — 95% das leituras atendidas pelo `route_cache` alimentado em massa pelo Travelpayouts.
Output: Polling automatico populando tabela, lookup persistente integrado ao fluxo, testes end-to-end mockados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/services/flight_search.py
@app/services/flight_cache.py
@app/services/polling_service.py
@app/scheduler.py
@app/database.py
@app/models.py

# Plans anteriores:
@.planning/phases/32-cache-layer-travelpayouts/32-01-PLAN.md
@.planning/phases/32-cache-layer-travelpayouts/32-02-PLAN.md

<interfaces>
Modelo RouteCache (criado em Plan 01):
```python
class RouteCache(Base):
    __tablename__ = "route_cache"
    id: Mapped[int]
    origin: Mapped[str]
    destination: Mapped[str]
    departure_date: Mapped[datetime.date]
    return_date: Mapped[datetime.date | None]
    min_price: Mapped[float]
    currency: Mapped[str]
    cached_at: Mapped[datetime.datetime]
    source: Mapped[str]
```

TravelpayoutsClient (criado em Plan 02):
```python
class TravelpayoutsClient:
    @property
    def is_configured(self) -> bool: ...
    def fetch_calendar(self, origin: str, destination: str, depart_month: str, currency: str = "BRL") -> list[dict]: ...
    # item: {"origin", "destination", "departure_date", "return_date", "min_price", "currency", "airline"}
```

search_flights_ex atual (app/services/flight_search.py):
```python
def search_flights_ex(origin, destination, departure_date, return_date, max_results=5, max_stops=None, adults=1, use_cache=True) -> tuple[list[dict], dict | None, str, bool]:
    # Hoje: flight_cache in-memory → SerpAPI
```
</interfaces>

<seed_routes>
Top 50 rotas BR seed (lista canonica a usar em refresh_top_routes):

**Domesticas (15):**
("GRU","GIG"), ("GIG","GRU"), ("GRU","SSA"), ("GRU","REC"), ("GRU","FOR"),
("CGH","SDU"), ("GRU","BSB"), ("GRU","POA"), ("GRU","CWB"), ("GRU","CNF"),
("GRU","MAO"), ("GIG","SSA"), ("BSB","GRU"), ("REC","GRU"), ("FOR","GRU")

**Internacionais (13):**
("GRU","LIS"), ("GRU","MAD"), ("GRU","MIA"), ("GRU","JFK"), ("GRU","SCL"),
("GRU","EZE"), ("GRU","LHR"), ("GRU","CDG"), ("GRU","FCO"), ("GRU","BCN"),
("GRU","FRA"), ("GIG","LIS"), ("GIG","MIA")

Total: 28 rotas iniciais (lista pode ser expandida depois; 28 × 30 datas ~= 840 calls/ciclo × 4 ciclos/dia = 3360/dia; conservador).

**Datas a puxar:** 30 datas espacadas a partir de hoje, 1 data por mes (6 meses para frente) × 5 variacoes de return_date (stay 7d, 10d, 14d, 21d, 30d). Total: 30 combinacoes por rota.

Simplificacao para Phase 32: puxar via `fetch_calendar` por mes — cada call cobre um mes inteiro dia-a-dia. 6 meses futuros × 28 rotas = 168 calls/ciclo, que e muito menos. return_date vem automatico do response (Travelpayouts ja sugere o melhor).
</seed_routes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN — route_cache_service com get_cached_price e refresh_top_routes</name>
  <files>app/services/route_cache_service.py, tests/test_route_cache_service.py</files>
  <read_first>
    - app/models.py (RouteCache ja criado)
    - app/services/travelpayouts_client.py (contrato fetch_calendar)
    - app/services/snapshot_service.py (estilo de queries SQLAlchemy com Session)
  </read_first>
  <behavior>
    RED — criar tests/test_route_cache_service.py com:
    - test_get_cached_price_hit_within_ttl: insere RouteCache(GRU, LIS, 2026-09-01, 2026-09-15, 2800.0, BRL, cached_at=now-2h, source="travelpayouts"). `get_cached_price(db, "GRU", "LIS", "2026-09-01", "2026-09-15", ttl_hours=6)` retorna dict com min_price=2800.0 e source="travelpayouts_cached".
    - test_get_cached_price_miss_when_stale: cached_at=now-10h, ttl_hours=6 → retorna None.
    - test_get_cached_price_miss_when_absent: query sem registro → retorna None.
    - test_get_cached_price_picks_cheapest_when_multiple: insere 2 registros para mesma rota/datas (um com 2800, outro com 2500, ambos dentro do TTL). Retorna o de 2500.
    - test_refresh_top_routes_populates_cache: mockar TravelpayoutsClient.fetch_calendar retornando 3 items. Chamar `refresh_top_routes(db, client, routes=[("GRU","LIS")], months=["2026-09"])`. Verificar que 3 rows foram inseridos em RouteCache com origin="GRU", source="travelpayouts".
    - test_refresh_top_routes_updates_existing: chamar 2x para mesma rota/data; segunda chamada deve fazer upsert (atualizar min_price e cached_at), nao duplicar.
    - test_refresh_top_routes_skips_when_client_not_configured: mockar `client.is_configured = False`. `refresh_top_routes` retorna dict com skipped=True e nao chama fetch_calendar.

    GREEN — criar app/services/route_cache_service.py:
    ```python
    """Servico de cache persistente de precos (Travelpayouts → route_cache).

    Responsavel por: (a) polling periodico alimentando route_cache via
    TravelpayoutsClient, (b) lookup usado por search_flights_ex antes de SerpAPI.
    """
    import datetime
    import logging
    from typing import Iterable

    from sqlalchemy.orm import Session

    from app.models import RouteCache
    from app.services.travelpayouts_client import TravelpayoutsClient

    logger = logging.getLogger(__name__)

    DEFAULT_TTL_HOURS = 6


    def get_cached_price(
        db: Session,
        origin: str,
        destination: str,
        departure_date: str,
        return_date: str | None,
        ttl_hours: int = DEFAULT_TTL_HOURS,
    ) -> dict | None:
        """Retorna o menor preco cacheado para a rota/datas se dentro do TTL.

        Resposta: {"min_price", "currency", "cached_at", "source"} ou None.
        source sempre vem como 'travelpayouts_cached' para downstream distinguir.
        """
        cutoff = datetime.datetime.utcnow() - datetime.timedelta(hours=ttl_hours)
        dep = datetime.date.fromisoformat(departure_date)
        ret = datetime.date.fromisoformat(return_date) if return_date else None

        q = (
            db.query(RouteCache)
            .filter(
                RouteCache.origin == origin,
                RouteCache.destination == destination,
                RouteCache.departure_date == dep,
                RouteCache.cached_at >= cutoff,
            )
            .order_by(RouteCache.min_price.asc())
        )
        if ret is not None:
            q = q.filter(RouteCache.return_date == ret)

        row = q.first()
        if row is None:
            return None
        return {
            "min_price": row.min_price,
            "currency": row.currency,
            "cached_at": row.cached_at,
            "source": "travelpayouts_cached",
        }


    def _upsert(db: Session, item: dict) -> None:
        dep = datetime.date.fromisoformat(item["departure_date"])
        ret = datetime.date.fromisoformat(item["return_date"]) if item.get("return_date") else None
        existing = (
            db.query(RouteCache)
            .filter(
                RouteCache.origin == item["origin"],
                RouteCache.destination == item["destination"],
                RouteCache.departure_date == dep,
                RouteCache.return_date == ret,
            )
            .first()
        )
        if existing:
            existing.min_price = item["min_price"]
            existing.currency = item["currency"]
            existing.cached_at = datetime.datetime.utcnow()
            existing.source = "travelpayouts"
        else:
            db.add(RouteCache(
                origin=item["origin"],
                destination=item["destination"],
                departure_date=dep,
                return_date=ret,
                min_price=item["min_price"],
                currency=item["currency"],
                cached_at=datetime.datetime.utcnow(),
                source="travelpayouts",
            ))


    def refresh_top_routes(
        db: Session,
        client: TravelpayoutsClient,
        routes: Iterable[tuple[str, str]],
        months: Iterable[str],
        currency: str = "BRL",
    ) -> dict:
        """Para cada (origin, destination) × mes, chama fetch_calendar e faz upsert."""
        if not client.is_configured:
            logger.warning("Travelpayouts client not configured, skipping refresh")
            return {"skipped": True, "reason": "client_not_configured"}

        total_calls = 0
        total_upserts = 0
        for origin, destination in routes:
            for month in months:
                items = client.fetch_calendar(origin, destination, month, currency=currency)
                total_calls += 1
                for item in items:
                    _upsert(db, item)
                    total_upserts += 1
        db.commit()
        logger.info("travelpayouts_refresh done: %d calls, %d upserts", total_calls, total_upserts)
        return {"skipped": False, "calls": total_calls, "upserts": total_upserts}


    # Lista canonica de rotas BR seed (ver PLAN 32-03 seed_routes)
    TOP_BR_ROUTES: list[tuple[str, str]] = [
        ("GRU","GIG"), ("GIG","GRU"), ("GRU","SSA"), ("GRU","REC"), ("GRU","FOR"),
        ("CGH","SDU"), ("GRU","BSB"), ("GRU","POA"), ("GRU","CWB"), ("GRU","CNF"),
        ("GRU","MAO"), ("GIG","SSA"), ("BSB","GRU"), ("REC","GRU"), ("FOR","GRU"),
        ("GRU","LIS"), ("GRU","MAD"), ("GRU","MIA"), ("GRU","JFK"), ("GRU","SCL"),
        ("GRU","EZE"), ("GRU","LHR"), ("GRU","CDG"), ("GRU","FCO"), ("GRU","BCN"),
        ("GRU","FRA"), ("GIG","LIS"), ("GIG","MIA"),
    ]


    def _next_n_months(n: int) -> list[str]:
        """Retorna ['YYYY-MM', ...] comecando no mes atual e avancando n meses."""
        today = datetime.date.today()
        out = []
        y, m = today.year, today.month
        for _ in range(n):
            out.append(f"{y:04d}-{m:02d}")
            m += 1
            if m > 12:
                m = 1
                y += 1
        return out
    ```

    Rodar `pytest tests/test_route_cache_service.py -v` — os 7 testes devem passar.
  </behavior>
  <action>
    Seguir RED→GREEN: escrever testes, confirmar falha, implementar servico, confirmar verde.
  </action>
  <verify>
    <automated>pytest tests/test_route_cache_service.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_route_cache_service.py com 7 testes passando
    - `grep -n "def get_cached_price" app/services/route_cache_service.py` retorna 1 match
    - `grep -n "def refresh_top_routes" app/services/route_cache_service.py` retorna 1 match
    - `grep -n "TOP_BR_ROUTES" app/services/route_cache_service.py` retorna >=1 match
  </acceptance_criteria>
  <done>route_cache_service completo com lookup + refresh + lista seed de 28 rotas BR. 7 testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar route_cache em search_flights_ex + job scheduler</name>
  <files>app/services/flight_search.py, app/scheduler.py, tests/test_flight_search_route_cache.py</files>
  <read_first>
    - app/services/flight_search.py (fluxo atual: in-memory cache → SerpAPI)
    - app/scheduler.py (padrao de add_job com CronTrigger)
    - app/services/route_cache_service.py (recem criado)
  </read_first>
  <behavior>
    RED — criar tests/test_flight_search_route_cache.py com:
    - test_search_flights_ex_route_cache_hit_returns_synthetic_flight: popular RouteCache(GRU, LIS, 2026-09-01, 2026-09-15, 2500.0). Chamar `search_flights_ex("GRU", "LIS", "2026-09-01", "2026-09-15")` (mockar SerpApiClient para falhar se for chamado). Retorno esperado: (flights=[{"price": 2500.0, "airline": "??", ...}], insights=None, source="travelpayouts_cached", was_cache_hit=True). SerpApiClient NAO deve ser instanciado.
    - test_search_flights_ex_route_cache_miss_falls_back_to_serpapi: RouteCache vazio. Mockar SerpApiClient retornando 1 voo. Esperado: was_cache_hit=False, source="serpapi".
    - test_search_flights_ex_route_cache_populated_after_serpapi_miss: (opcional, so se for rapido) apos serpapi hit, nova entrada pode ser gravada em route_cache para reuso; manter simples — somente assert que o fallback funciona.

    GREEN — em app/services/flight_search.py:

    ```python
    # No topo, adicionar import:
    from app.database import SessionLocal
    from app.services import route_cache_service

    def search_flights_ex(
        origin, destination, departure_date, return_date,
        max_results=5, max_stops=None, adults=1, use_cache=True,
    ) -> tuple[list[dict], dict | None, str, bool]:
        pax = max(1, int(adults))

        # 1. In-memory cache (mesmo ciclo) — mantido como esta hoje
        cache_key = flight_cache.make_key(origin, destination, departure_date, return_date, max_stops, pax)
        if use_cache:
            hit = flight_cache.get(cache_key)
            if hit is not None:
                flights_cached, insights_cached, orig_source = hit
                return flights_cached[:max_results], insights_cached, orig_source, True

        # 2. Cache persistente route_cache (Travelpayouts)
        if use_cache:
            db = SessionLocal()
            try:
                cached = route_cache_service.get_cached_price(
                    db, origin, destination, departure_date, return_date,
                )
            finally:
                db.close()
            if cached is not None:
                synthetic = [{
                    "price": cached["min_price"],
                    "airline": "??",
                    "flights": [],
                    "type": "Round trip",
                }]
                # Popular cache in-memory para economizar no proximo lookup
                flight_cache.put(cache_key, (synthetic, None, "travelpayouts_cached"))
                logger.info(
                    "route_cache HIT %s->%s %s (travelpayouts_cached)",
                    origin, destination, departure_date,
                )
                return synthetic[:max_results], None, "travelpayouts_cached", True

        # 3. Fallback SerpAPI (mesmo comportamento atual)
        client = SerpApiClient()
        flights, insights = client.search_flights_with_insights(
            origin=origin, destination=destination,
            departure_date=departure_date, return_date=return_date,
            max_results=max_results, max_stops=max_stops, adults=pax,
        )
        if use_cache:
            flight_cache.put(cache_key, (flights, insights, "serpapi"))
        return flights, insights, "serpapi", False
    ```

    Em app/scheduler.py — adicionar job:
    ```python
    from app.services.travelpayouts_client import TravelpayoutsClient
    from app.services import route_cache_service
    from app.database import SessionLocal


    def run_travelpayouts_refresh():
        """Chamado a cada 6h: puxa precos dos proximos 6 meses para TOP_BR_ROUTES."""
        db = SessionLocal()
        try:
            client = TravelpayoutsClient()
            months = route_cache_service._next_n_months(6)
            route_cache_service.refresh_top_routes(
                db, client,
                routes=route_cache_service.TOP_BR_ROUTES,
                months=months,
            )
        finally:
            db.close()


    def init_scheduler():
        # ... jobs existentes ...
        scheduler.add_job(
            run_travelpayouts_refresh,
            trigger=CronTrigger(hour="0,6,12,18", minute=30),  # 4x/dia, 30 min apos o polling SerpAPI
            id="travelpayouts_refresh",
            name="Travelpayouts cache refresh (6h)",
            replace_existing=True,
        )
        # ... scheduler.start() ...
    ```

    Rodar `pytest tests/test_flight_search_route_cache.py -v` — devem passar.
    Rodar `pytest tests/ -x` — suite completa verde.
  </behavior>
  <action>
    Implementar integracao em 2 arquivos (flight_search.py, scheduler.py) + testes.

    **Ordem de lookup em search_flights_ex**: in-memory cache → route_cache persistente → SerpAPI. Essa ordem e critica para CACHE-05.

    Garantir que `flight_cache.clear()` e chamado nas fixtures de teste (ja deve ser padrao; conferir conftest).
  </action>
  <verify>
    <automated>pytest tests/test_flight_search_route_cache.py tests/test_route_cache_service.py -v && pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_flight_search_route_cache.py com >=2 testes passando
    - `grep -n "route_cache_service.get_cached_price" app/services/flight_search.py` retorna 1 match
    - `grep -n "travelpayouts_cached" app/services/flight_search.py` retorna >=1 match
    - `grep -n "travelpayouts_refresh" app/scheduler.py` retorna >=2 matches (id do job + funcao)
    - Suite completa pytest -x passa sem regressao
  </acceptance_criteria>
  <done>search_flights_ex consulta route_cache antes de SerpAPI; cron de refresh agendado 4x/dia; testes verdes.</done>
</task>

</tasks>

<verification>
Suite completa sem regressao + cenario de cache hit manual:
```bash
pytest -x
```

Verificacao funcional opcional (dev local com TRAVELPAYOUTS_TOKEN no .env):
```bash
python -c "
from app.database import SessionLocal
from app.services.travelpayouts_client import TravelpayoutsClient
from app.services import route_cache_service
db = SessionLocal()
client = TravelpayoutsClient()
res = route_cache_service.refresh_top_routes(db, client, routes=[('GRU','LIS')], months=['2026-09'])
print(res)
"
```
</verification>

<success_criteria>
- `search_flights_ex` consulta route_cache persistente antes de SerpAPI (CACHE-05)
- Job `travelpayouts_refresh` roda 4x/dia (00:30, 06:30, 12:30, 18:30 UTC) populando route_cache (CACHE-04)
- Cache hit retorna `source="travelpayouts_cached"` e was_cache_hit=True
- Fallback para SerpAPI funciona em cache miss
- Testes novos passam; suite completa verde
- Quota SerpAPI cai para <500/mes apos ciclos de polling rodarem em producao (CACHE-07, medicao em producao)
</success_criteria>

<output>
Apos completar, criar `.planning/phases/32-cache-layer-travelpayouts/32-03-SUMMARY.md`.
</output>
