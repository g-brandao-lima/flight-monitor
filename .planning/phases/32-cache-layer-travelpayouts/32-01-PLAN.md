---
phase: 32-cache-layer-travelpayouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/config.py
  - alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py
  - tests/test_route_cache_model.py
  - .env.example
autonomous: true
requirements: [CACHE-03]
must_haves:
  truths:
    - "Tabela route_cache existe em producao apos alembic upgrade head"
    - "Indice composto em (origin, destination, departure_date, return_date) acelera lookup"
    - "Settings.travelpayouts_token carrega TRAVELPAYOUTS_TOKEN do .env"
  artifacts:
    - path: "app/models.py"
      provides: "Classe RouteCache com 8 colunas"
      contains: "class RouteCache"
    - path: "alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py"
      provides: "Migration Alembic criando tabela route_cache"
      contains: "op.create_table"
    - path: "app/config.py"
      provides: "Campo travelpayouts_token na classe Settings"
      contains: "travelpayouts_token"
  key_links:
    - from: "app/models.py"
      to: "alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py"
      via: "SQLAlchemy model → migration schema"
      pattern: "route_cache"
    - from: "app/config.py"
      to: ".env"
      via: "pydantic-settings env_file"
      pattern: "travelpayouts_token"
---

<objective>
Criar modelo SQLAlchemy RouteCache, migration Alembic para a tabela `route_cache` e adicionar o setting `travelpayouts_token` em app/config.py.

Purpose: Base de dados para Phase 32 — Plans 02, 03 e 04 dependem desta tabela existir e do token configurado.
Output: Modelo RouteCache, migration aplicavel, config estendido, testes de criacao + indice + lookup passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@app/models.py
@app/config.py
@app/database.py

# Migration existentes como referencia de estilo:
@alembic/versions/e5f6g7h8i9j0_add_source_to_snapshots.py
@alembic/versions/f6g7h8i9j0k1_drop_booking_class_snapshots.py

<interfaces>
Base SQLAlchemy (de app/database.py):
```python
class Base(DeclarativeBase):
    pass
```

Padrao atual de model (de app/models.py):
```python
class FlightSnapshot(Base):
    __tablename__ = "flight_snapshots"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    origin: Mapped[str] = mapped_column(String(3))
    destination: Mapped[str] = mapped_column(String(3))
    departure_date: Mapped[datetime.date] = mapped_column(Date)
    return_date: Mapped[datetime.date] = mapped_column(Date)
    price: Mapped[float] = mapped_column(Float)
    currency: Mapped[str] = mapped_column(String(3), default="BRL")
    source: Mapped[str | None] = mapped_column(String(30), nullable=True)
    collected_at: Mapped[datetime.datetime] = mapped_column(DateTime, server_default=func.now())
```

Padrao Settings (de app/config.py):
```python
class Settings(BaseSettings):
    database_url: str = "sqlite:///./flight_monitor.db"
    serpapi_api_key: str = ""
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — testes de RouteCache + config travelpayouts_token</name>
  <files>tests/test_route_cache_model.py</files>
  <read_first>
    - app/models.py (estilo de model SQLAlchemy 2.0 Mapped)
    - app/config.py (estilo Settings)
    - tests/conftest.py (fixtures db_session, se existir)
  </read_first>
  <behavior>
    - Test 1 (test_route_cache_insert_and_query): insere RouteCache(origin="GRU", destination="LIS", departure_date=2026-09-01, return_date=2026-09-15, min_price=2800.50, currency="BRL", cached_at=datetime.utcnow(), source="travelpayouts") e faz query; deve retornar 1 row com os mesmos valores.
    - Test 2 (test_route_cache_index_lookup): insere 3 registros diferentes e faz query com filtro origin+destination+departure_date+return_date; deve retornar exatamente 1 row (smoke test do indice).
    - Test 3 (test_route_cache_nullable_return_date): insere registro com return_date=None (one-way futuro); deve persistir sem erro.
    - Test 4 (test_settings_travelpayouts_token): `from app.config import Settings; s = Settings(travelpayouts_token="abc123"); assert s.travelpayouts_token == "abc123"`.
    - Test 5 (test_settings_travelpayouts_token_default_empty): `s = Settings(); assert s.travelpayouts_token == ""`.
  </behavior>
  <action>
    Criar tests/test_route_cache_model.py seguindo o padrao AAA do projeto (pytest, SQLite in-memory via fixture existente).

    Imports esperados:
    ```python
    import datetime
    import pytest
    from app.models import RouteCache
    from app.config import Settings
    ```

    Usar a fixture `db_session` do conftest.py (mesma usada em testes de FlightSnapshot). Se a fixture nao existir com esse nome, olhar como outros testes (ex: tests/test_snapshot_service.py) criam a sessao e replicar.

    Rodar `pytest tests/test_route_cache_model.py -x` e CONFIRMAR que todos os 5 testes falham com ImportError ou AttributeError (model e setting ainda nao existem).
  </action>
  <verify>
    <automated>pytest tests/test_route_cache_model.py -x 2>&1 | grep -E "(ImportError|AttributeError|FAILED)"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/test_route_cache_model.py existe com 5 testes
    - Todos os 5 testes rodam e FALHAM (ImportError de RouteCache ou AttributeError de travelpayouts_token)
  </acceptance_criteria>
  <done>5 testes escritos e confirmados falhando por RouteCache/travelpayouts_token inexistentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — RouteCache model + travelpayouts_token em Settings</name>
  <files>app/models.py, app/config.py, .env.example</files>
  <read_first>
    - app/models.py (padrao atual de FlightSnapshot)
    - app/config.py (padrao atual de Settings)
    - .env.example (se existir, para espelhar entrada do novo token)
  </read_first>
  <action>
    1. Em app/models.py, apos a classe ApiUsage, adicionar:
    ```python
    class RouteCache(Base):
        __tablename__ = "route_cache"

        id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
        origin: Mapped[str] = mapped_column(String(3), index=True)
        destination: Mapped[str] = mapped_column(String(3), index=True)
        departure_date: Mapped[datetime.date] = mapped_column(Date, index=True)
        return_date: Mapped[datetime.date | None] = mapped_column(Date, nullable=True, index=True)
        min_price: Mapped[float] = mapped_column(Float)
        currency: Mapped[str] = mapped_column(String(3), default="BRL")
        cached_at: Mapped[datetime.datetime] = mapped_column(DateTime, server_default=func.now())
        source: Mapped[str] = mapped_column(String(30), default="travelpayouts")


    Index(
        "ix_route_cache_lookup",
        RouteCache.origin,
        RouteCache.destination,
        RouteCache.departure_date,
        RouteCache.return_date,
    )
    ```

    2. Em app/config.py, adicionar campo `travelpayouts_token: str = ""` na classe Settings (antes de `model_config`).

    3. Em .env.example (criar se nao existir, ou append), adicionar a linha:
    ```
    TRAVELPAYOUTS_TOKEN=
    ```

    Rodar `pytest tests/test_route_cache_model.py -x` — deve passar.
  </action>
  <verify>
    <automated>pytest tests/test_route_cache_model.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 testes em tests/test_route_cache_model.py passam
    - `grep -n "class RouteCache" app/models.py` retorna 1 match
    - `grep -n "travelpayouts_token" app/config.py` retorna 1 match
    - `grep -n "ix_route_cache_lookup" app/models.py` retorna 1 match
  </acceptance_criteria>
  <done>Modelo RouteCache criado, indice composto declarado, settings estendido, testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Migration Alembic para route_cache</name>
  <files>alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py</files>
  <read_first>
    - alembic/versions/e5f6g7h8i9j0_add_source_to_snapshots.py (estilo op.add_column)
    - alembic/versions/86a799448829_add_users_table.py (estilo op.create_table com multiplas colunas + indices)
    - alembic/versions/f6g7h8i9j0k1_drop_booking_class_snapshots.py (para identificar a ultima revision como down_revision)
  </read_first>
  <action>
    1. Abrir a ultima migration existente em alembic/versions/ (f6g7h8i9j0k1_drop_booking_class_snapshots.py) e anotar o `revision = "..."`. Esse valor sera o `down_revision` da nova migration.

    2. Criar arquivo alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py:

    ```python
    """add route_cache table

    Revision ID: g7h8i9j0k1l2
    Revises: <down_revision_real>
    Create Date: 2026-04-21

    """
    from alembic import op
    import sqlalchemy as sa


    revision = "g7h8i9j0k1l2"
    down_revision = "<down_revision_real>"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        op.create_table(
            "route_cache",
            sa.Column("id", sa.Integer(), primary_key=True, autoincrement=True),
            sa.Column("origin", sa.String(length=3), nullable=False),
            sa.Column("destination", sa.String(length=3), nullable=False),
            sa.Column("departure_date", sa.Date(), nullable=False),
            sa.Column("return_date", sa.Date(), nullable=True),
            sa.Column("min_price", sa.Float(), nullable=False),
            sa.Column("currency", sa.String(length=3), nullable=False, server_default="BRL"),
            sa.Column("cached_at", sa.DateTime(), server_default=sa.func.now(), nullable=False),
            sa.Column("source", sa.String(length=30), nullable=False, server_default="travelpayouts"),
        )
        op.create_index("ix_route_cache_origin", "route_cache", ["origin"])
        op.create_index("ix_route_cache_destination", "route_cache", ["destination"])
        op.create_index("ix_route_cache_departure_date", "route_cache", ["departure_date"])
        op.create_index("ix_route_cache_return_date", "route_cache", ["return_date"])
        op.create_index(
            "ix_route_cache_lookup",
            "route_cache",
            ["origin", "destination", "departure_date", "return_date"],
        )


    def downgrade() -> None:
        op.drop_index("ix_route_cache_lookup", table_name="route_cache")
        op.drop_index("ix_route_cache_return_date", table_name="route_cache")
        op.drop_index("ix_route_cache_departure_date", table_name="route_cache")
        op.drop_index("ix_route_cache_destination", table_name="route_cache")
        op.drop_index("ix_route_cache_origin", table_name="route_cache")
        op.drop_table("route_cache")
    ```

    3. Rodar `alembic upgrade head` num ambiente de teste (SQLite local temporario) para confirmar que a migration aplica sem erro.

    4. Rodar `alembic downgrade -1 && alembic upgrade head` para testar roundtrip.
  </action>
  <verify>
    <automated>cd "$(pwd)" && python -c "from alembic.config import Config; from alembic import command; cfg = Config('alembic.ini'); command.upgrade(cfg, 'head'); command.downgrade(cfg, '-1'); command.upgrade(cfg, 'head')" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` executa sem erro
    - `alembic downgrade -1 && alembic upgrade head` executa sem erro (roundtrip)
    - Arquivo alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py existe e cria a tabela com as 9 colunas especificadas
    - down_revision aponta para a ultima migration anterior (f6g7h8i9j0k1 ou o que existir)
  </acceptance_criteria>
  <done>Migration aplicavel e reversivel, tabela route_cache criada com indice composto.</done>
</task>

</tasks>

<verification>
Rodar suite completa para garantir nenhuma regressao:
```bash
pytest -x
```
Esperado: todos os testes previos + 5 novos de RouteCache passando.
</verification>

<success_criteria>
- Classe RouteCache definida em app/models.py com 9 campos e indice composto
- Campo travelpayouts_token adicionado em Settings
- Migration Alembic `g7h8i9j0k1l2_add_route_cache_table.py` criada, upgrade e downgrade funcionam
- 5 testes de tests/test_route_cache_model.py passam
- Suite completa (304+ testes + 5 novos) verde
</success_criteria>

<output>
Apos completar, criar `.planning/phases/32-cache-layer-travelpayouts/32-01-SUMMARY.md` com o template padrao.
</output>
