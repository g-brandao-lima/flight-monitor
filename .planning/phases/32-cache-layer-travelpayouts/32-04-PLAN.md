---
phase: 32-cache-layer-travelpayouts
plan: 04
type: execute
wave: 3
depends_on: [32-03]
files_modified:
  - app/models.py
  - alembic/versions/h8i9j0k1l2m3_add_cache_lookup_log.py
  - app/services/flight_search.py
  - app/services/admin_stats_service.py
  - app/templates/admin/stats.html
  - tests/test_admin_cache_metrics.py
autonomous: false
requirements: [CACHE-06, CACHE-07]
must_haves:
  truths:
    - "Admin panel /admin/stats mostra cache hit rate percentual dos ultimos 7 dias"
    - "Admin panel /admin/stats mostra quota Travelpayouts restante (rastreada internamente)"
    - "Cada chamada a search_flights_ex incrementa um contador de hit/miss persistido"
  artifacts:
    - path: "app/models.py"
      provides: "Classe CacheLookupLog (origin, destination, hit, source, looked_up_at)"
      contains: "class CacheLookupLog"
    - path: "app/services/admin_stats_service.py"
      provides: "get_cache_hit_rate_7d() e get_travelpayouts_quota_info()"
      contains: "def get_cache_hit_rate_7d"
    - path: "app/templates/admin/stats.html"
      provides: "Secao de cache hit rate + quota Travelpayouts"
      contains: "cache_hit_rate"
  key_links:
    - from: "app/services/flight_search.py"
      to: "app/models.CacheLookupLog"
      via: "insert apos cada lookup"
      pattern: "CacheLookupLog"
    - from: "app/templates/admin/stats.html"
      to: "admin_stats_service.get_cache_hit_rate_7d"
      via: "context do route admin"
      pattern: "cache_hit_rate_7d"
---

<objective>
Instrumentar `search_flights_ex` para logar hit/miss em tabela `cache_lookup_log`, calcular hit rate dos ultimos 7 dias em `admin_stats_service`, rastrear quota Travelpayouts internamente (contador de calls por mes), e expor ambos no template `/admin/stats`.

Purpose: Dar visibilidade operacional a CACHE-06 (dashboard admin) e CACHE-07 (validar que quota SerpAPI realmente caiu).
Output: Metrica persistida, template atualizado, checkpoint visual para confirmar renderizacao no navegador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/services/admin_stats_service.py
@app/services/flight_search.py
@app/models.py
@app/services/quota_service.py

# Plan anterior (contexto necessario):
@.planning/phases/32-cache-layer-travelpayouts/32-03-PLAN.md

<interfaces>
admin_stats_service atual (app/services/admin_stats_service.py):
```python
def get_quota_stats(db: Session) -> dict: ...
def get_source_distribution(db: Session, days: int = 7) -> dict: ...
def get_cache_info() -> dict: ...  # in-memory cache size
```

Padrao de quota_service (para rastrear travelpayouts quota):
```python
# app/services/quota_service.py ja tem increment_usage/get_monthly_usage para SerpAPI
# Reutilizar mesmo padrao com chave distinta (ex: year_month="2026-04:travelpayouts")
```

Template admin/stats.html atual: Jinja2 com secoes de quota SerpAPI, source distribution, cache info in-memory. Adicionar nova secao "Cache Travelpayouts".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CacheLookupLog model + migration + instrumentacao em search_flights_ex</name>
  <files>app/models.py, alembic/versions/h8i9j0k1l2m3_add_cache_lookup_log.py, app/services/flight_search.py, tests/test_admin_cache_metrics.py</files>
  <read_first>
    - app/models.py (padrao RouteCache criado em Plan 01)
    - app/services/flight_search.py (com integracao route_cache do Plan 03)
    - alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py (estilo migration Plan 01)
  </read_first>
  <behavior>
    RED — tests/test_admin_cache_metrics.py:
    - test_cache_lookup_log_insert: cria CacheLookupLog(origin="GRU", destination="LIS", hit=True, source="travelpayouts_cached", looked_up_at=now). Persiste sem erro.
    - test_search_flights_ex_logs_cache_hit: popular route_cache com GRU-LIS 2026-09-01/15 2800. Chamar search_flights_ex. Apos retorno, query CacheLookupLog — deve ter 1 row com hit=True, source="travelpayouts_cached".
    - test_search_flights_ex_logs_cache_miss: route_cache vazio, mockar SerpApiClient ok. Chamar search_flights_ex. Query CacheLookupLog — deve ter 1 row com hit=False, source="serpapi".
    - test_get_cache_hit_rate_7d: inserir 7 CacheLookupLog (3 hit, 4 miss) com looked_up_at dentro dos ultimos 7d + 2 rows com looked_up_at=now-10d. `get_cache_hit_rate_7d(db)` retorna dict com {"total": 7, "hits": 3, "misses": 4, "hit_rate_pct": round(3/7*100, 1) = 42.9}.
    - test_get_cache_hit_rate_7d_empty: sem logs → retorna {"total": 0, "hits": 0, "misses": 0, "hit_rate_pct": 0.0}.
    - test_get_travelpayouts_quota_info_default: sem calls rastreadas → retorna {"used": 0, "month": "YYYY-MM"}.
    - test_get_travelpayouts_quota_info_after_increment: chamar `increment_travelpayouts_usage(db)` 3x; get_travelpayouts_quota_info retorna used=3.

    GREEN — implementar:

    1. **app/models.py** — nova classe:
    ```python
    class CacheLookupLog(Base):
        __tablename__ = "cache_lookup_log"

        id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
        origin: Mapped[str] = mapped_column(String(3), index=True)
        destination: Mapped[str] = mapped_column(String(3), index=True)
        hit: Mapped[bool] = mapped_column(Boolean, index=True)
        source: Mapped[str] = mapped_column(String(30))
        looked_up_at: Mapped[datetime.datetime] = mapped_column(
            DateTime, server_default=func.now(), index=True,
        )
    ```

    2. **alembic/versions/h8i9j0k1l2m3_add_cache_lookup_log.py** — migration criando tabela com colunas e 4 indices (origin, destination, hit, looked_up_at). down_revision = "g7h8i9j0k1l2".

    3. **app/services/flight_search.py** — apos cada um dos 3 retornos possiveis em search_flights_ex, gravar log:
    ```python
    def _log_lookup(origin: str, destination: str, hit: bool, source: str) -> None:
        """Best-effort log de hit/miss. Nunca propaga erro (nao bloqueia search)."""
        try:
            from app.database import SessionLocal
            from app.models import CacheLookupLog
            db = SessionLocal()
            try:
                db.add(CacheLookupLog(
                    origin=origin, destination=destination,
                    hit=hit, source=source,
                ))
                db.commit()
            finally:
                db.close()
        except Exception as e:
            logger.warning("cache_lookup_log insert failed: %s", e)
    ```

    Chamar `_log_lookup(...)` antes de cada `return` em search_flights_ex:
    - in-memory hit → `_log_lookup(origin, destination, hit=True, source=orig_source)`
    - route_cache hit → `_log_lookup(origin, destination, hit=True, source="travelpayouts_cached")`
    - SerpAPI fallback → `_log_lookup(origin, destination, hit=False, source="serpapi")`

    4. **app/services/admin_stats_service.py** — adicionar:
    ```python
    from app.models import CacheLookupLog, ApiUsage


    def get_cache_hit_rate_7d(db: Session) -> dict:
        cutoff = datetime.datetime.utcnow() - datetime.timedelta(days=7)
        rows = (
            db.query(CacheLookupLog.hit)
            .filter(CacheLookupLog.looked_up_at >= cutoff)
            .all()
        )
        total = len(rows)
        hits = sum(1 for (h,) in rows if h)
        misses = total - hits
        hit_rate = round((hits / total * 100), 1) if total else 0.0
        return {"total": total, "hits": hits, "misses": misses, "hit_rate_pct": hit_rate}


    def get_travelpayouts_quota_info(db: Session) -> dict:
        """Quota Travelpayouts rastreada internamente (sem API oficial de quota).

        Cada chamada a fetch_calendar/fetch_cheap/fetch_month_matrix incrementa
        ApiUsage com year_month 'YYYY-MM:travelpayouts'.
        """
        now = datetime.datetime.utcnow()
        key = f"{now:%Y-%m}:travelpayouts"
        row = db.query(ApiUsage).filter(ApiUsage.year_month == key).first()
        used = row.search_count if row else 0
        return {"used": used, "month": f"{now:%Y-%m}"}


    def increment_travelpayouts_usage(db: Session) -> None:
        now = datetime.datetime.utcnow()
        key = f"{now:%Y-%m}:travelpayouts"
        row = db.query(ApiUsage).filter(ApiUsage.year_month == key).first()
        if row:
            row.search_count += 1
        else:
            db.add(ApiUsage(year_month=key, search_count=1))
        db.commit()
    ```

    5. **app/services/travelpayouts_client.py** — (ajuste minimo) apos cada call HTTP bem-sucedida em `_get`, chamar `increment_travelpayouts_usage` para rastrear quota. Como o client nao tem Session, expor metrica via callback opcional ou chamar do route_cache_service apos cada fetch_calendar. **Escolha simples:** em `route_cache_service.refresh_top_routes`, apos cada `client.fetch_calendar`, chamar `increment_travelpayouts_usage(db)`.
  </behavior>
  <action>
    Executar RED→GREEN para os 7 testes. Aplicar migration, atualizar flight_search, admin_stats_service e route_cache_service conforme detalhado em behavior.
  </action>
  <verify>
    <automated>pytest tests/test_admin_cache_metrics.py -v && pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_admin_cache_metrics.py com 7 testes passando
    - `grep -n "class CacheLookupLog" app/models.py` retorna 1 match
    - `grep -n "def get_cache_hit_rate_7d" app/services/admin_stats_service.py` retorna 1 match
    - `grep -n "def get_travelpayouts_quota_info" app/services/admin_stats_service.py` retorna 1 match
    - `grep -n "increment_travelpayouts_usage" app/services/route_cache_service.py` retorna >=1 match
    - Suite completa pytest -x verde
    - `alembic upgrade head` aplica sem erro
  </acceptance_criteria>
  <done>Instrumentacao completa: modelo, migration, log em search_flights_ex, 2 metricas novas em admin_stats_service, rastreio de quota Travelpayouts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exibir metricas no template /admin/stats + checkpoint visual</name>
  <files>app/templates/admin/stats.html</files>
  <read_first>
    - app/templates/admin/stats.html (estrutura atual)
    - app/routes/admin.py ou equivalente (handler que passa context; confirmar quais chaves sao usadas hoje)
  </read_first>
  <behavior>
    1. Localizar o route handler que renderiza `/admin/stats` (provavelmente em app/routes/admin.py ou app/main.py). Adicionar ao context:
    ```python
    context["cache_hit_rate_7d"] = admin_stats_service.get_cache_hit_rate_7d(db)
    context["travelpayouts_quota"] = admin_stats_service.get_travelpayouts_quota_info(db)
    ```

    2. Em `app/templates/admin/stats.html`, adicionar nova secao apos quota SerpAPI:
    ```html
    <section class="admin-card">
      <h2>Cache Travelpayouts (ultimos 7 dias)</h2>
      <ul>
        <li>Total de lookups: {{ cache_hit_rate_7d.total }}</li>
        <li>Hits: {{ cache_hit_rate_7d.hits }}</li>
        <li>Misses: {{ cache_hit_rate_7d.misses }}</li>
        <li><strong>Hit rate: {{ cache_hit_rate_7d.hit_rate_pct }}%</strong></li>
      </ul>
      <h3>Uso Travelpayouts ({{ travelpayouts_quota.month }})</h3>
      <p>Chamadas feitas este mes: <strong>{{ travelpayouts_quota.used }}</strong></p>
    </section>
    ```
  </action>
  <verify>
    <automated>grep -n "cache_hit_rate_7d" app/templates/admin/stats.html && grep -n "travelpayouts_quota" app/templates/admin/stats.html && pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - Template contem as chaves cache_hit_rate_7d.hit_rate_pct e travelpayouts_quota.used
    - Route handler de /admin/stats passa ambas no context
    - Suite pytest -x continua verde
  </acceptance_criteria>
  <done>Metricas novas renderizam no template admin/stats.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — confirmar render de /admin/stats</name>
  <what-built>
    Secao nova "Cache Travelpayouts (ultimos 7 dias)" em /admin/stats mostrando:
    - Total de lookups, hits, misses, hit rate percentual dos ultimos 7d
    - Uso Travelpayouts do mes corrente
  </what-built>
  <how-to-verify>
    1. Rodar localmente: `uvicorn app.main:app --reload`
    2. Fazer login com Google (user admin)
    3. Acessar http://localhost:8000/admin/stats
    4. Verificar que a secao "Cache Travelpayouts" aparece com as 4 metricas
    5. Opcional: disparar manualmente `python -c "from app.scheduler import run_travelpayouts_refresh; run_travelpayouts_refresh()"` (com TRAVELPAYOUTS_TOKEN no .env) e recarregar — uso deve incrementar
    6. Opcional: fazer uma busca qualquer via polling (/api/v1/polling/run manual ou dashboard) e recarregar — lookups devem incrementar
  </how-to-verify>
  <resume-signal>Digite "approved" apos validar render visualmente ou descreva discrepancia.</resume-signal>
</task>

</tasks>

<verification>
Rodar suite completa:
```bash
pytest -x
```

E validacao funcional manual pelo checkpoint visual (Task 3).
</verification>

<success_criteria>
- Admin panel /admin/stats mostra cache hit rate percentual dos ultimos 7 dias (CACHE-06)
- Admin panel mostra uso Travelpayouts do mes corrente (CACHE-06)
- Todos os testes novos passam; suite completa verde
- Checkpoint visual aprovado pelo humano
- Metrica coletada em producao permite medir queda de quota SerpAPI ao longo do tempo (CACHE-07)
</success_criteria>

<output>
Apos completar, criar `.planning/phases/32-cache-layer-travelpayouts/32-04-SUMMARY.md`.
</output>
