---
phase: 07-consolidated-email
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/services/polling_service.py
  - tests/test_polling_service.py
autonomous: true
requirements:
  - EMAIL-01

must_haves:
  truths:
    - "polling_service acumula todos os sinais detectados durante _poll_group antes de enviar email"
    - "Ao final de _poll_group, se houver sinais, envia 1 email consolidado via compose_consolidated_email"
    - "Se nenhum sinal foi detectado no ciclo do grupo, nenhum email e enviado"
    - "should_alert ainda e verificado antes de enviar (silenciamento respeitado)"
  artifacts:
    - path: "app/services/polling_service.py"
      provides: "Polling refatorado com acumulo de sinais"
      contains: "compose_consolidated_email"
    - path: "tests/test_polling_service.py"
      provides: "Testes do fluxo consolidado"
      contains: "test_poll_group_sends_consolidated"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/alert_service.py"
      via: "import compose_consolidated_email"
      pattern: "compose_consolidated_email"
    - from: "app/services/polling_service.py"
      to: "app/services/alert_service.py"
      via: "import should_alert, send_email"
      pattern: "should_alert"
---

<objective>
Refatorar polling_service para acumular sinais por grupo e enviar 1 email consolidado ao final de _poll_group, substituindo o envio individual por sinal.

Purpose: Implementar D-01/D-03 - 1 email por grupo por ciclo, so quando ha sinais detectados.
Output: polling_service.py refatorado e testado.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consolidated-email/07-CONTEXT.md
@.planning/phases/07-consolidated-email/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From app/services/alert_service.py (after Plan 01):
```python
def compose_consolidated_email(
    signals: list[DetectedSignal],
    snapshots: list[FlightSnapshot],
    group: RouteGroup,
) -> MIMEMultipart:
    """Compoe email consolidado com rota mais barata, top 3 datas e resumo."""

def compose_alert_email(signal: DetectedSignal, group: RouteGroup) -> MIMEMultipart:
    """Antigo - mantido para backwards compat."""

def send_email(msg: MIMEMultipart) -> None:
def should_alert(group: RouteGroup) -> bool:
```

From app/services/polling_service.py (current):
```python
def run_polling_cycle():
    """Executa um ciclo de polling para todos os grupos ativos."""

def _poll_group(db, client: SerpApiClient, group: RouteGroup):
    """Polling de um grupo: gera combinacoes, busca voos."""

def _process_flight(db, group, origin, destination, dep_date, ret_date, flight, insights):
    """Processa um voo: classifica preco e persiste snapshot."""
    # Atualmente: detect_signals -> compose_alert_email -> send_email POR SINAL
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD refatorar polling_service para email consolidado</name>
  <files>app/services/polling_service.py, tests/test_polling_service.py</files>
  <read_first>
    - app/services/polling_service.py
    - tests/test_polling_service.py
    - app/services/alert_service.py (para ver assinatura de compose_consolidated_email)
  </read_first>
  <behavior>
    - test_poll_group_sends_consolidated_email_when_signals_detected: quando _poll_group processa voos e detect_signals retorna sinais, deve chamar compose_consolidated_email (nao compose_alert_email) com lista de sinais + snapshots + grupo, e send_email 1 vez
    - test_poll_group_no_email_when_no_signals: quando nenhum sinal e detectado, send_email nao e chamado
    - test_poll_group_no_email_when_silenced: quando should_alert retorna False, send_email nao e chamado mesmo com sinais
    - test_poll_group_accumulates_all_signals: quando multiplos voos geram sinais, todos sao acumulados e enviados no email unico
    - test_process_flight_returns_signal_and_snapshot: _process_flight retorna (snapshot, signals) ao inves de enviar email diretamente
  </behavior>
  <action>
    RED: Em tests/test_polling_service.py, adicionar testes para o novo fluxo consolidado. Mockar detect_signals, compose_consolidated_email, send_email, should_alert. Verificar que compose_consolidated_email e chamado com lista completa de sinais e snapshots ao final do grupo. Rodar e confirmar falha.

    GREEN: Refatorar app/services/polling_service.py:
    1. Trocar import: compose_alert_email -> compose_consolidated_email (per D-03)
    2. Em _process_flight: REMOVER o bloco de envio de email (linhas 134-147 atuais). Fazer _process_flight retornar tupla (snapshot, list[DetectedSignal]) ao inves de None
    3. Em _poll_group: criar listas accumulated_signals = [] e accumulated_snapshots = []
    4. No loop de flights, chamar _process_flight e coletar retorno: (snapshot, signals). Se snapshot nao None, append em accumulated_snapshots. Extend accumulated_signals com signals.
    5. APOS o loop completo (fim de _poll_group): se accumulated_signals nao vazio E should_alert(group), chamar compose_consolidated_email(accumulated_signals, accumulated_snapshots, group) e send_email(msg)
    6. Manter logging de cada sinal individual dentro do loop
    7. Tratar excecoes no envio com try/except e logger.error

    REFACTOR: Remover import de compose_alert_email se nao houver mais uso direto. Limpar variaveis nao usadas. Rodar todos os testes.
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_polling_service.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "compose_consolidated_email" app/services/polling_service.py
    - grep -q "accumulated_signals" app/services/polling_service.py
    - grep -qv "compose_alert_email" app/services/polling_service.py || true (pode ter sido removido)
    - grep -q "test_poll_group_sends_consolidated" tests/test_polling_service.py
    - grep -q "test_poll_group_no_email_when_no_signals" tests/test_polling_service.py
    - python -m pytest tests/test_polling_service.py retorna 0
  </acceptance_criteria>
  <done>polling_service acumula sinais por grupo e envia 1 email consolidado via compose_consolidated_email ao final de _poll_group. Envio individual por sinal eliminado. Todos os testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: Validacao cruzada - rodar toda a suite de testes</name>
  <files>tests/test_alert_service.py, tests/test_polling_service.py</files>
  <read_first>
    - tests/test_alert_service.py
    - tests/test_polling_service.py
  </read_first>
  <action>
    Rodar a suite completa de testes do projeto para garantir que a refatoracao do polling_service nao quebrou nada. Especificamente:
    1. python -m pytest tests/ -v --tb=short
    2. Se algum teste falhar que nao seja dos novos, investigar e corrigir a causa (provavel: testes antigos do polling_service que esperam compose_alert_email sendo chamado - atualizar para esperar compose_consolidated_email)
    3. Verificar que NENHUM teste importa compose_alert_email de forma que dependa do fluxo antigo de envio por sinal
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/ retorna 0 (todos os testes passam)
  </acceptance_criteria>
  <done>Toda a suite de testes passa sem falhas. Nenhuma regressao introduzida pela refatoracao.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/ passa 100%
- polling_service usa compose_consolidated_email (nao compose_alert_email)
- Fluxo: _poll_group acumula sinais -> envia 1 email consolidado ao final
</verification>

<success_criteria>
- 1 email por grupo por ciclo de polling (nao 1 por sinal)
- Email so enviado quando ha sinais detectados no ciclo
- Silenciamento ainda respeitado
- Toda suite de testes verde
</success_criteria>

<output>
After completion, create `.planning/phases/07-consolidated-email/07-02-SUMMARY.md`
</output>
