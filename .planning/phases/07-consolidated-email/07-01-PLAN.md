---
phase: 07-consolidated-email
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/services/alert_service.py
  - tests/test_alert_service.py
autonomous: true
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03

must_haves:
  truths:
    - "compose_consolidated_email recebe grupo + sinais + snapshots e retorna MIMEMultipart com 1 email"
    - "Email contem rota mais barata em destaque com preco, companhia, datas ida/volta"
    - "Email contem tabela com top 3 melhores datas/precos ordenadas por preco crescente"
    - "Email contem resumo das demais rotas monitoradas"
    - "Todas as datas no email usam formato dd/mm/aaaa"
    - "Link de silenciar presente no rodape"
  artifacts:
    - path: "app/services/alert_service.py"
      provides: "compose_consolidated_email function"
      exports: ["compose_consolidated_email"]
    - path: "tests/test_alert_service.py"
      provides: "Testes TDD para email consolidado"
      contains: "test_consolidated"
  key_links:
    - from: "app/services/alert_service.py"
      to: "app/services/dashboard_service.py"
      via: "import format_price_brl"
      pattern: "from app.services.dashboard_service import format_price_brl"
---

<objective>
Criar a funcao compose_consolidated_email via TDD que monta 1 email por grupo com rota mais barata em destaque, tabela de melhores datas, resumo de outras rotas e datas em formato brasileiro.

Purpose: Substituir o email individual por sinal por um email consolidado util para decisao de compra (per D-01, D-02).
Output: Funcao compose_consolidated_email testada e funcional em alert_service.py.
</objective>

<execution_context>
@C:\Users\GustavoBrandão\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\GustavoBrandão\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consolidated-email/07-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/models.py:
```python
class RouteGroup(Base):
    id: Mapped[int]
    name: Mapped[str]  # String(100)
    origins: Mapped[list]  # JSON
    destinations: Mapped[list]  # JSON
    silenced_until: Mapped[datetime.datetime | None]

class FlightSnapshot(Base):
    id: Mapped[int]
    route_group_id: Mapped[int]
    origin: Mapped[str]  # String(3)
    destination: Mapped[str]  # String(3)
    departure_date: Mapped[datetime.date]
    return_date: Mapped[datetime.date]
    price: Mapped[float]
    currency: Mapped[str]  # "BRL"
    airline: Mapped[str]  # String(2)
    price_classification: Mapped[str | None]

class DetectedSignal(Base):
    id: Mapped[int]
    route_group_id: Mapped[int]
    origin: Mapped[str]
    destination: Mapped[str]
    departure_date: Mapped[datetime.date]
    return_date: Mapped[datetime.date]
    signal_type: Mapped[str]
    urgency: Mapped[str]
    details: Mapped[str]
    price_at_detection: Mapped[float]
```

From app/services/alert_service.py:
```python
def compose_alert_email(signal: DetectedSignal, group: RouteGroup) -> MIMEMultipart:
def send_email(msg: MIMEMultipart) -> None:
def generate_silence_token(group_id: int) -> str:
def should_alert(group: RouteGroup) -> bool:
```

From app/services/dashboard_service.py:
```python
def format_price_brl(price: float) -> str:
    """Format a float price as Brazilian Real string: R$ X.XXX,XX."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD compose_consolidated_email - testes e implementacao</name>
  <files>tests/test_alert_service.py, app/services/alert_service.py</files>
  <read_first>
    - app/services/alert_service.py
    - tests/test_alert_service.py
    - app/models.py
    - app/services/dashboard_service.py (lines 105-108, format_price_brl)
  </read_first>
  <behavior>
    - test_consolidated_email_subject_contains_group_name_and_best_price: assunto = "Flight Monitor: {group.name} - R$X.XXX (melhor preco)" (per D-02, specifics)
    - test_consolidated_email_cheapest_route_in_body: corpo HTML contem origem, destino, preco formatado BRL, companhia e datas dd/mm/aaaa da rota mais barata (per D-02 item 1, D-06)
    - test_consolidated_email_top3_dates_table: corpo HTML contem tabela com ate 3 entradas ordenadas por preco crescente, cada uma com origem->destino, data ida dd/mm, data volta dd/mm, preco, companhia (per D-04, D-05)
    - test_consolidated_email_other_routes_summary: corpo HTML contem secao de resumo com rotas que nao sao a mais barata, mostrando rota + preco atual (per D-02 item 3)
    - test_consolidated_email_silence_link: corpo HTML contem link /api/v1/alerts/silence/ com token HMAC (per D-02 item 4)
    - test_consolidated_email_dates_brazilian_format: nenhuma data no corpo aparece no formato YYYY-MM-DD; todas usam dd/mm/aaaa (per D-06)
    - test_consolidated_email_single_snapshot: funciona corretamente quando ha apenas 1 snapshot (edge case)
    - test_consolidated_email_plain_text_fallback: email contem parte text/plain com informacoes legíveis
  </behavior>
  <action>
    RED: Adicionar testes ao arquivo tests/test_alert_service.py (nao substituir os testes existentes, adicionar na sequencia). Criar helpers _make_snapshot() e _make_signals_list() usando MagicMock com spec de FlightSnapshot e DetectedSignal. Rodar pytest tests/test_alert_service.py e confirmar que os novos testes falham.

    GREEN: Em app/services/alert_service.py, criar funcao compose_consolidated_email(signals: list[DetectedSignal], snapshots: list[FlightSnapshot], group: RouteGroup) -> MIMEMultipart:
    1. Importar format_price_brl de app.services.dashboard_service
    2. Encontrar snapshot com menor preco (rota mais barata)
    3. Ordenar snapshots por preco e pegar top 3 (melhores datas, per D-04)
    4. Separar demais rotas (snapshots que nao sao a mais barata) para resumo
    5. Formatar todas as datas com strftime("%d/%m/%Y") (per D-06)
    6. Montar HTML com: (a) header com rota mais barata em destaque, (b) tabela top 3 melhores datas, (c) resumo outras rotas, (d) lista de sinais detectados, (e) link silenciar no rodape
    7. Assunto: f"Flight Monitor: {group.name} - {format_price_brl(cheapest.price)} (melhor preco)"
    8. Montar parte text/plain com mesmas informacoes em texto simples
    9. Reusar generate_silence_token e settings existentes

    REFACTOR: Garantir que _render_plain e _render_html antigos continuam existindo (compose_alert_email antigo ainda funciona para backwards compat). Rodar pytest tests/test_alert_service.py completo.
  </action>
  <verify>
    <automated>cd "C:/Users/GustavoBrandão/OneDrive/Área de Trabalho/projetos pessoais/flight-monitor" && python -m pytest tests/test_alert_service.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def compose_consolidated_email" app/services/alert_service.py
    - grep -q "format_price_brl" app/services/alert_service.py
    - grep -q "strftime.*%d/%m/%Y" app/services/alert_service.py
    - grep -q "test_consolidated_email_cheapest_route" tests/test_alert_service.py
    - grep -q "test_consolidated_email_top3_dates" tests/test_alert_service.py
    - grep -q "test_consolidated_email_dates_brazilian_format" tests/test_alert_service.py
    - python -m pytest tests/test_alert_service.py retorna 0
  </acceptance_criteria>
  <done>compose_consolidated_email existe, recebe lista de sinais + snapshots + grupo, retorna MIMEMultipart com rota mais barata em destaque, tabela top 3 datas, resumo outras rotas, datas dd/mm/aaaa e link silenciar. Todos os testes passando.</done>
</task>

</tasks>

<verification>
- Todos os testes em tests/test_alert_service.py passam (antigos + novos)
- compose_consolidated_email exportada e acessivel
- Datas formatadas em dd/mm/aaaa no output
</verification>

<success_criteria>
- compose_consolidated_email funcional com TDD completo
- 8+ testes novos cobrindo happy path, edge cases e formato brasileiro
- Funcao antiga compose_alert_email preservada (nao quebrar backwards compat)
</success_criteria>

<output>
After completion, create `.planning/phases/07-consolidated-email/07-01-SUMMARY.md`
</output>
