---
phase: 02-data-collection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/amadeus_client.py
  - tests/test_amadeus_client.py
autonomous: true
requirements: [COLL-02, COLL-03, COLL-04]

must_haves:
  truths:
    - "AmadeusClient.search_cheapest_offers retorna top 5 ofertas ordenadas por preco"
    - "AmadeusClient.get_availability retorna booking classes com class_code e seats_available"
    - "AmadeusClient.get_price_metrics retorna quartis historicos"
    - "classify_price retorna LOW/MEDIUM/HIGH baseado em quartis"
    - "AmadeusClient funciona com mock do SDK (sem chamadas reais)"
    - "AmadeusClient.is_configured retorna False quando credenciais vazias"
  artifacts:
    - path: "app/services/amadeus_client.py"
      provides: "Wrapper do Amadeus SDK com metodos de alto nivel"
      exports: ["AmadeusClient", "classify_price"]
    - path: "tests/test_amadeus_client.py"
      provides: "Testes unitarios com mocks do SDK"
      contains: "test_search_cheapest_offers"
  key_links:
    - from: "app/services/amadeus_client.py"
      to: "amadeus.Client"
      via: "SDK wrapper"
      pattern: "from amadeus import Client"
    - from: "app/services/amadeus_client.py"
      to: "app/config.py"
      via: "settings.amadeus_client_id"
      pattern: "settings\\.amadeus_client"
---

<objective>
Criar o wrapper AmadeusClient que encapsula as 3 chamadas de API necessarias para coleta de dados: Flight Offers Search, Flight Availabilities Search e Itinerary Price Metrics.

Purpose: O AmadeusClient isola toda a logica de comunicacao com a Amadeus API em um unico modulo testavel com mocks. O polling_service (Plan 03) vai usar este client sem conhecer detalhes do SDK.
Output: amadeus_client.py com 3 metodos + classify_price, testes com mock do SDK passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-collection/02-CONTEXT.md
@.planning/phases/02-data-collection/02-RESEARCH.md

@app/config.py
@requirements.txt

<interfaces>
<!-- Amadeus SDK API surface (from RESEARCH.md code examples) -->

Amadeus SDK usage:
```python
from amadeus import Client, ResponseError

client = Client(client_id="...", client_secret="...")

# Flight Offers Search GET
response = client.shopping.flight_offers_search.get(
    originLocationCode="GRU", destinationLocationCode="GIG",
    departureDate="2026-05-01", returnDate="2026-05-08",
    adults=1, currencyCode="BRL", max=250
)
# response.data = list of flight-offer dicts with price.grandTotal

# Flight Availabilities POST
response = client.shopping.availability.flight_availabilities.post(body)
# response.data[].segments[].availabilityClasses = [{"class": "Y", "numberOfBookableSeats": 9}]

# Itinerary Price Metrics GET
response = client.analytics.itinerary_price_metrics.get(
    originIataCode="GRU", destinationIataCode="GIG",
    departureDate="2026-05-01", currencyCode="BRL"
)
# response.data = [{"priceMetrics": [{"amount": "150.00", "quartileRanking": "MINIMUM"}]}]
```

From app/config.py:
```python
class Settings(BaseSettings):
    amadeus_client_id: str = ""
    amadeus_client_secret: str = ""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Testes do AmadeusClient com mocks do SDK</name>
  <files>tests/test_amadeus_client.py</files>
  <read_first>
    - app/config.py
    - .planning/phases/02-data-collection/02-RESEARCH.md (secoes Code Examples e Pattern 1)
    - tests/conftest.py
  </read_first>
  <behavior>
    - test_client_not_configured_without_credentials: AmadeusClient com credenciais vazias tem is_configured == False
    - test_client_configured_with_credentials: AmadeusClient com credenciais preenchidas tem is_configured == True
    - test_search_cheapest_offers_returns_top_5: mock do SDK retorna 10 ofertas com precos variados; search_cheapest_offers retorna as 5 mais baratas ordenadas por preco crescente
    - test_search_cheapest_offers_returns_less_if_fewer_available: mock retorna apenas 2 ofertas; funcao retorna 2 (nao erro)
    - test_get_availability_returns_booking_classes: mock retorna response com segments e availabilityClasses; get_availability retorna lista de dicts com class/numberOfBookableSeats
    - test_get_price_metrics_returns_data: mock retorna priceMetrics com quartis; get_price_metrics retorna a lista de metrics
    - test_get_price_metrics_returns_none_on_404: mock levanta ResponseError 404; get_price_metrics retorna None
    - test_classify_price_low: price=200, first_quartile=250 -> retorna "LOW"
    - test_classify_price_medium: price=300, first=250, medium=400 -> retorna "MEDIUM"
    - test_classify_price_high: price=500, first=250, medium=400 -> retorna "HIGH"
    - test_classify_price_no_metrics: metrics vazio -> retorna None
  </behavior>
  <action>
    1. Criar tests/test_amadeus_client.py com os 11 testes descritos em behavior
    2. Usar unittest.mock.patch e MagicMock para mockar amadeus.Client
    3. Para testes de classify_price: funcao pura, sem mock necessario
    4. Para testes do client: mockar settings para ter credenciais preenchidas, mockar Client() para retornar mock de response
    5. Mock de response deve ter .data com estrutura real do Amadeus (conforme RESEARCH.md Code Examples)
    6. Importar AmadeusClient e classify_price de app.services.amadeus_client (vai falhar - RED)
    7. Executar testes e confirmar que TODOS FALHAM

    Mock de Flight Offers response.data:
    ```python
    [
        {"price": {"grandTotal": "450.00"}, "itineraries": [...], "validatingAirlineCodes": ["LA"]},
        {"price": {"grandTotal": "380.00"}, "itineraries": [...], "validatingAirlineCodes": ["G3"]},
        # ... mais ofertas
    ]
    ```

    Mock de Availability response.data:
    ```python
    [
        {"segments": [{"availabilityClasses": [
            {"class": "Y", "numberOfBookableSeats": 9},
            {"class": "B", "numberOfBookableSeats": 4},
        ]}]}
    ]
    ```

    Mock de Price Metrics response.data:
    ```python
    [{"priceMetrics": [
        {"amount": "150.00", "quartileRanking": "MINIMUM"},
        {"amount": "250.00", "quartileRanking": "FIRST"},
        {"amount": "400.00", "quartileRanking": "MEDIUM"},
        {"amount": "600.00", "quartileRanking": "THIRD"},
        {"amount": "900.00", "quartileRanking": "MAXIMUM"},
    ]}]
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_amadeus_client.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_amadeus_client.py existe com 11 testes
    - python -m pytest tests/test_amadeus_client.py retorna 11 FAILED ou ERROR (todos vermelhos)
    - Testes importam de app.services.amadeus_client que ainda nao existe
  </acceptance_criteria>
  <done>11 testes escritos e confirmados FALHANDO (RED). Mocks seguem estrutura real do Amadeus SDK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implementar AmadeusClient e classify_price</name>
  <files>app/services/amadeus_client.py</files>
  <read_first>
    - tests/test_amadeus_client.py
    - app/config.py
    - .planning/phases/02-data-collection/02-RESEARCH.md (secoes Pattern 1 e Code Examples)
  </read_first>
  <action>
    1. Criar app/services/amadeus_client.py com:

    class AmadeusClient:
        def __init__(self):
            - Se settings.amadeus_client_id ou amadeus_client_secret vazios: self._client = None, log warning
            - Senao: self._client = Client(client_id=settings.amadeus_client_id, client_secret=settings.amadeus_client_secret)

        @property
        def is_configured(self) -> bool:
            return self._client is not None

        def search_cheapest_offers(self, origin: str, destination: str, departure_date: str, return_date: str, max_results: int = 5) -> list[dict]:
            - Chama self._client.shopping.flight_offers_search.get(originLocationCode=origin, destinationLocationCode=destination, departureDate=departure_date, returnDate=return_date, adults=1, currencyCode="BRL", max=250)
            - Ordena response.data por float(x["price"]["grandTotal"])
            - Retorna offers[:max_results]

        def get_availability(self, origin: str, destination: str, departure_date: str, return_date: str) -> list[dict]:
            - Monta body com originDestinations (ida id="1", volta id="2"), travelers, sources=["GDS"]
            - Chama self._client.shopping.availability.flight_availabilities.post(body)
            - Retorna response.data

        def get_price_metrics(self, origin: str, destination: str, departure_date: str) -> list[dict] | None:
            - try: chama self._client.analytics.itinerary_price_metrics.get(originIataCode=origin, destinationIataCode=destination, departureDate=departure_date, currencyCode="BRL")
            - except ResponseError: log warning, return None
            - Retorna response.data

    def classify_price(price: float, metrics: list[dict]) -> str | None:
        - Se metrics vazio ou None: return None
        - Extrair quartiles dict: {m["quartileRanking"]: float(m["amount"]) for m in metrics}
        - first = quartiles.get("FIRST"), medium = quartiles.get("MEDIUM")
        - Se first ou medium None: return None
        - Se price <= first: return "LOW"
        - Se price <= medium: return "MEDIUM"
        - Senao: return "HIGH"

    2. Importar: from amadeus import Client, ResponseError; from app.config import settings; import logging
    3. Executar testes e confirmar que TODOS os 11 testes passam (GREEN)
    4. Executar suite completa
  </action>
  <verify>
    <automated>python -m pytest tests/test_amadeus_client.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/services/amadeus_client.py existe com class AmadeusClient e funcao classify_price
    - grep "from amadeus import Client" app/services/amadeus_client.py retorna match
    - grep "def classify_price" app/services/amadeus_client.py retorna match
    - python -m pytest tests/test_amadeus_client.py retorna 11 PASSED
    - python -m pytest tests/ retorna todos os testes passando
  </acceptance_criteria>
  <done>AmadeusClient com 3 metodos de API + classify_price implementados. 11 testes verdes. Suite completa verde.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_amadeus_client.py -v retorna 11 passed
- python -m pytest tests/ -v retorna todos os testes passando
- grep "class AmadeusClient" app/services/amadeus_client.py retorna match
- grep "def classify_price" app/services/amadeus_client.py retorna match
- grep "def search_cheapest_offers" app/services/amadeus_client.py retorna match
- grep "def get_availability" app/services/amadeus_client.py retorna match
- grep "def get_price_metrics" app/services/amadeus_client.py retorna match
</verification>

<success_criteria>
- AmadeusClient encapsula SDK com 3 metodos de alto nivel (offers, availability, metrics)
- classify_price deriva LOW/MEDIUM/HIGH a partir de quartis
- is_configured retorna False quando credenciais ausentes
- get_price_metrics retorna None em caso de erro (nao explode)
- 11 testes novos passando + testes existentes continuam verdes
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-collection/02-02-SUMMARY.md`
</output>
