---
phase: 02-data-collection
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - app/services/polling_service.py
  - app/scheduler.py
  - main.py
  - tests/test_polling_service.py
  - tests/test_scheduler.py
autonomous: true
requirements: [COLL-01, COLL-06]

must_haves:
  truths:
    - "Scheduler registra job de polling com intervalo de 6 horas"
    - "Polling cycle busca todos os grupos ativos e executa coleta para cada um"
    - "Falha em um grupo nao impede polling dos demais grupos"
    - "AmadeusClient nao configurado faz polling cycle pular sem erro"
    - "Scheduler inicia no startup da app e para no shutdown"
    - "Polling de um grupo gera combinacoes origem x destino, busca top 5, captura availability e price metrics, persiste snapshots"
  artifacts:
    - path: "app/services/polling_service.py"
      provides: "Orquestracao do ciclo de polling"
      exports: ["run_polling_cycle"]
    - path: "app/scheduler.py"
      provides: "BackgroundScheduler com job de 6 horas"
      exports: ["init_scheduler", "shutdown_scheduler"]
    - path: "main.py"
      provides: "Lifespan atualizado com scheduler start/stop"
      contains: "init_scheduler"
    - path: "tests/test_polling_service.py"
      provides: "Testes do ciclo de polling com mocks"
      contains: "test_polling_cycle"
    - path: "tests/test_scheduler.py"
      provides: "Testes de registro do scheduler"
      contains: "test_scheduler_registers_job"
  key_links:
    - from: "app/services/polling_service.py"
      to: "app/services/amadeus_client.py"
      via: "AmadeusClient instance"
      pattern: "AmadeusClient\\(\\)"
    - from: "app/services/polling_service.py"
      to: "app/services/snapshot_service.py"
      via: "save_flight_snapshot"
      pattern: "save_flight_snapshot"
    - from: "app/services/polling_service.py"
      to: "app/models.py"
      via: "RouteGroup query"
      pattern: "RouteGroup"
    - from: "app/scheduler.py"
      to: "app/services/polling_service.py"
      via: "run_polling_cycle job"
      pattern: "run_polling_cycle"
    - from: "main.py"
      to: "app/scheduler.py"
      via: "lifespan init/shutdown"
      pattern: "init_scheduler"
---

<objective>
Criar o polling_service que orquestra a coleta de dados para todos os grupos ativos, o scheduler que dispara o polling a cada 6 horas, e integrar com o lifespan do FastAPI.

Purpose: Este plano conecta todos os componentes: usa AmadeusClient (Plan 02) para buscar dados e snapshot_service (Plan 01) para persistir. O scheduler garante execucao autonoma a cada 6 horas sem intervencao manual (COLL-01). Tratamento de erros por grupo garante resiliencia (COLL-06).
Output: polling_service.py, scheduler.py, main.py atualizado, testes passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-collection/02-CONTEXT.md
@.planning/phases/02-data-collection/02-RESEARCH.md

@.planning/phases/02-data-collection/02-01-SUMMARY.md
@.planning/phases/02-data-collection/02-02-SUMMARY.md

@app/models.py
@app/services/amadeus_client.py
@app/services/snapshot_service.py
@app/database.py
@main.py
@tests/conftest.py

<interfaces>
<!-- Contracts from Plan 01 and Plan 02 that this plan consumes -->

From app/services/amadeus_client.py (Plan 02):
```python
class AmadeusClient:
    @property
    def is_configured(self) -> bool: ...
    def search_cheapest_offers(self, origin: str, destination: str,
        departure_date: str, return_date: str, max_results: int = 5) -> list[dict]: ...
    def get_availability(self, origin: str, destination: str,
        departure_date: str, return_date: str) -> list[dict]: ...
    def get_price_metrics(self, origin: str, destination: str,
        departure_date: str) -> list[dict] | None: ...

def classify_price(price: float, metrics: list[dict]) -> str | None: ...
```

From app/services/snapshot_service.py (Plan 01):
```python
def save_flight_snapshot(db: Session, data: dict) -> FlightSnapshot:
    # data keys: route_group_id, origin, destination, departure_date, return_date,
    # price, currency, airline, price_min, price_first_quartile, price_median,
    # price_third_quartile, price_max, price_classification, booking_classes
    # booking_classes: list of {class_code, seats_available, segment_direction}
```

From app/models.py:
```python
class RouteGroup(Base):
    id, name, origins (JSON list), destinations (JSON list),
    duration_days (int), travel_start (date), travel_end (date),
    target_price (float|None), is_active (bool)

class FlightSnapshot(Base):
    id, route_group_id, origin, destination, departure_date, return_date,
    price, currency, airline, price_* (nullable), collected_at,
    booking_classes (relationship)
```

From app/database.py:
```python
SessionLocal = sessionmaker(...)
```

From main.py:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    Base.metadata.create_all(bind=engine)
    yield
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Testes do polling_service e scheduler</name>
  <files>tests/test_polling_service.py, tests/test_scheduler.py</files>
  <read_first>
    - app/services/amadeus_client.py
    - app/services/snapshot_service.py
    - app/models.py
    - app/database.py
    - tests/conftest.py
    - .planning/phases/02-data-collection/02-RESEARCH.md (secoes Pattern 2, Pattern 3, Pitfalls)
  </read_first>
  <behavior>
    tests/test_polling_service.py:
    - test_polling_cycle_skips_when_not_configured: AmadeusClient.is_configured=False; run_polling_cycle nao levanta excecao, nao cria snapshots
    - test_polling_cycle_processes_active_groups: 2 grupos ativos no banco, AmadeusClient mockado retornando ofertas/availability/metrics; run_polling_cycle cria snapshots para ambos os grupos
    - test_polling_cycle_skips_inactive_groups: 1 grupo ativo + 1 inativo; apenas 1 grupo recebe polling
    - test_polling_cycle_continues_after_group_failure: 2 grupos ativos, AmadeusClient levanta excecao no 1o grupo; 2o grupo ainda recebe polling e snapshot e criado
    - test_poll_group_generates_origin_dest_combinations: grupo com origins=["GRU","VCP"] e destinations=["GIG"]; polling gera 2 combinacoes (GRU-GIG, VCP-GIG)
    - test_poll_group_generates_date_combinations: grupo com travel_start=2026-05-01, travel_end=2026-05-15, duration_days=7; polling gera datas de partida a cada 3 dias dentro do periodo, respeitando return_date = departure + duration_days
    - test_poll_group_saves_snapshot_with_booking_classes: mock AmadeusClient retorna oferta + availability + metrics; verificar que save_flight_snapshot e chamado com dados corretos incluindo booking_classes
    - test_poll_group_handles_missing_price_metrics: get_price_metrics retorna None; snapshot e salvo com price_classification=None e price_min/etc=None

    tests/test_scheduler.py:
    - test_scheduler_registers_job_with_6h_interval: init_scheduler registra job "polling_cycle" com IntervalTrigger de 6 horas
    - test_scheduler_shutdown_stops_cleanly: shutdown_scheduler nao levanta excecao
  </behavior>
  <action>
    1. Criar tests/test_polling_service.py com os 8 testes descritos
       - Usar fixture db do conftest.py para criar RouteGroup de teste
       - Mockar AmadeusClient com unittest.mock.patch
       - Mockar save_flight_snapshot para verificar chamadas sem tocar banco real (ou usar db fixture para integracao)
       - Para test_poll_group_generates_date_combinations: mockar AmadeusClient e verificar que search_cheapest_offers e chamado com as datas corretas
       - Para test_polling_cycle_continues_after_group_failure: mockar search_cheapest_offers para levantar Exception no primeiro grupo

    2. Criar tests/test_scheduler.py com os 2 testes descritos
       - Mockar BackgroundScheduler para verificar add_job com IntervalTrigger(hours=6)
       - Verificar que job id e "polling_cycle"

    3. Executar testes e confirmar que TODOS FALHAM (RED)
    4. NAO criar nenhum arquivo de implementacao nesta task

    Dados de mock para AmadeusClient.search_cheapest_offers:
    ```python
    [
        {
            "price": {"grandTotal": "450.00"},
            "validatingAirlineCodes": ["LA"],
            "itineraries": [
                {"segments": [{"departure": {"iataCode": "GRU", "at": "2026-05-01T10:00"}, "arrival": {"iataCode": "GIG", "at": "2026-05-01T11:00"}}]},
                {"segments": [{"departure": {"iataCode": "GIG", "at": "2026-05-08T10:00"}, "arrival": {"iataCode": "GRU", "at": "2026-05-08T11:00"}}]},
            ],
        }
    ]
    ```

    Dados de mock para AmadeusClient.get_availability:
    ```python
    [
        {"segments": [{"availabilityClasses": [
            {"class": "Y", "numberOfBookableSeats": 9},
            {"class": "B", "numberOfBookableSeats": 4},
            {"class": "M", "numberOfBookableSeats": 3},
        ]}]},
        {"segments": [{"availabilityClasses": [
            {"class": "Y", "numberOfBookableSeats": 7},
            {"class": "B", "numberOfBookableSeats": 2},
        ]}]},
    ]
    ```

    Dados de mock para AmadeusClient.get_price_metrics:
    ```python
    [{"priceMetrics": [
        {"amount": "150.00", "quartileRanking": "MINIMUM"},
        {"amount": "250.00", "quartileRanking": "FIRST"},
        {"amount": "400.00", "quartileRanking": "MEDIUM"},
        {"amount": "600.00", "quartileRanking": "THIRD"},
        {"amount": "900.00", "quartileRanking": "MAXIMUM"},
    ]}]
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_polling_service.py tests/test_scheduler.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_polling_service.py existe com 8 testes
    - tests/test_scheduler.py existe com 2 testes
    - python -m pytest tests/test_polling_service.py tests/test_scheduler.py retorna 10 FAILED ou ERROR
    - Testes existentes (Phase 1 + Plan 01 + Plan 02) continuam passando
  </acceptance_criteria>
  <done>10 testes escritos e confirmados FALHANDO (RED). Cobertura inclui happy path, erro por grupo, client nao configurado, date generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implementar polling_service, scheduler e atualizar main.py</name>
  <files>app/services/polling_service.py, app/scheduler.py, main.py</files>
  <read_first>
    - tests/test_polling_service.py
    - tests/test_scheduler.py
    - app/services/amadeus_client.py
    - app/services/snapshot_service.py
    - app/models.py
    - app/database.py
    - main.py
    - .planning/phases/02-data-collection/02-RESEARCH.md (secoes Pattern 2, Pattern 3, Budget Analysis, Pitfalls)
  </read_first>
  <action>
    1. Criar app/services/polling_service.py:

    import logging
    from datetime import date, timedelta
    from app.database import SessionLocal
    from app.models import RouteGroup
    from app.services.amadeus_client import AmadeusClient, classify_price
    from app.services.snapshot_service import save_flight_snapshot

    logger = logging.getLogger(__name__)

    def run_polling_cycle():
        """Executa um ciclo de polling para todos os grupos ativos."""
        client = AmadeusClient()
        if not client.is_configured:
            logger.warning("Amadeus not configured. Skipping polling cycle.")
            return

        db = SessionLocal()
        try:
            groups = db.query(RouteGroup).filter(RouteGroup.is_active == True).all()
            logger.info(f"Polling {len(groups)} active groups")
            for group in groups:
                try:
                    _poll_group(db, client, group)
                except Exception as e:
                    logger.error(f"Polling failed for group {group.id} ({group.name}): {e}")
                    continue
        finally:
            db.close()

    def _generate_date_pairs(travel_start: date, travel_end: date, duration_days: int) -> list[tuple[date, date]]:
        """Gera pares (departure_date, return_date) a cada 3 dias dentro do periodo."""
        pairs = []
        current = travel_start
        while current + timedelta(days=duration_days) <= travel_end:
            pairs.append((current, current + timedelta(days=duration_days)))
            current += timedelta(days=3)
        # Se nenhum par gerado (periodo muito curto), usar travel_start
        if not pairs and travel_start + timedelta(days=duration_days) <= travel_end + timedelta(days=duration_days):
            pairs.append((travel_start, travel_start + timedelta(days=duration_days)))
        return pairs

    def _poll_group(db, client: AmadeusClient, group: RouteGroup):
        """Polling de um grupo especifico: gera combinacoes, busca top 5, captura availability e metrics, persiste."""
        origins = group.origins  # JSON list, ex: ["GRU", "VCP"]
        destinations = group.destinations  # JSON list, ex: ["GIG"]
        date_pairs = _generate_date_pairs(group.travel_start, group.travel_end, group.duration_days)

        for origin in origins:
            for destination in destinations:
                for dep_date, ret_date in date_pairs:
                    try:
                        offers = client.search_cheapest_offers(
                            origin=origin, destination=destination,
                            departure_date=dep_date.isoformat(), return_date=ret_date.isoformat(),
                            max_results=5,
                        )
                    except Exception as e:
                        logger.error(f"Offers search failed for {origin}->{destination} {dep_date}: {e}")
                        continue

                    for offer in offers:
                        _process_offer(db, client, group, origin, destination, dep_date, ret_date, offer)

    def _process_offer(db, client, group, origin, destination, dep_date, ret_date, offer):
        """Processa uma oferta: busca availability, price metrics e persiste snapshot."""
        price = float(offer["price"]["grandTotal"])
        airline = offer.get("validatingAirlineCodes", ["??"])[0]

        # Availability
        try:
            avail_data = client.get_availability(origin, destination, dep_date.isoformat(), ret_date.isoformat())
        except Exception as e:
            logger.error(f"Availability failed for {origin}->{destination}: {e}")
            avail_data = []

        booking_classes = _extract_booking_classes(avail_data)

        # Price metrics
        metrics_data = client.get_price_metrics(origin, destination, dep_date.isoformat())
        price_metrics = {}
        classification = None
        if metrics_data and len(metrics_data) > 0 and "priceMetrics" in metrics_data[0]:
            raw_metrics = metrics_data[0]["priceMetrics"]
            quartiles = {m["quartileRanking"]: float(m["amount"]) for m in raw_metrics}
            price_metrics = {
                "price_min": quartiles.get("MINIMUM"),
                "price_first_quartile": quartiles.get("FIRST"),
                "price_median": quartiles.get("MEDIUM"),
                "price_third_quartile": quartiles.get("THIRD"),
                "price_max": quartiles.get("MAXIMUM"),
            }
            classification = classify_price(price, raw_metrics)

        snapshot_data = {
            "route_group_id": group.id,
            "origin": origin,
            "destination": destination,
            "departure_date": dep_date,
            "return_date": ret_date,
            "price": price,
            "currency": "BRL",
            "airline": airline,
            "price_classification": classification,
            "booking_classes": booking_classes,
            **price_metrics,
        }
        save_flight_snapshot(db, snapshot_data)

    def _extract_booking_classes(avail_data: list) -> list[dict]:
        """Extrai booking classes de todos os segmentos do availability response."""
        classes = []
        directions = ["OUTBOUND", "INBOUND"]
        for i, flight in enumerate(avail_data):
            direction = directions[i] if i < len(directions) else f"SEGMENT_{i}"
            for segment in flight.get("segments", []):
                for ac in segment.get("availabilityClasses", []):
                    classes.append({
                        "class_code": ac["class"],
                        "seats_available": ac["numberOfBookableSeats"],
                        "segment_direction": direction,
                    })
        return classes

    2. Criar app/scheduler.py:
    from apscheduler.schedulers.background import BackgroundScheduler
    from apscheduler.triggers.interval import IntervalTrigger
    from app.services.polling_service import run_polling_cycle
    import logging

    logger = logging.getLogger(__name__)
    scheduler = BackgroundScheduler()

    def init_scheduler():
        scheduler.add_job(
            run_polling_cycle,
            trigger=IntervalTrigger(hours=6),
            id="polling_cycle",
            name="Amadeus polling cycle",
            replace_existing=True,
        )
        scheduler.start()
        logger.info("Scheduler started with 6-hour polling interval")

    def shutdown_scheduler():
        scheduler.shutdown(wait=False)
        logger.info("Scheduler stopped")

    3. Atualizar main.py: adicionar init_scheduler/shutdown_scheduler no lifespan
    ```python
    @asynccontextmanager
    async def lifespan(app: FastAPI):
        Base.metadata.create_all(bind=engine)
        from app.scheduler import init_scheduler, shutdown_scheduler
        init_scheduler()
        yield
        shutdown_scheduler()
    ```

    4. Executar testes e confirmar que TODOS os 10 novos testes passam (GREEN)
    5. Executar suite completa para garantir que nada quebrou
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/services/polling_service.py existe com funcoes run_polling_cycle, _poll_group, _generate_date_pairs, _extract_booking_classes
    - app/scheduler.py existe com funcoes init_scheduler e shutdown_scheduler
    - main.py contem "init_scheduler" e "shutdown_scheduler" no lifespan
    - grep "IntervalTrigger(hours=6)" app/scheduler.py retorna match
    - grep "run_polling_cycle" app/scheduler.py retorna match
    - python -m pytest tests/test_polling_service.py tests/test_scheduler.py retorna 10 PASSED
    - python -m pytest tests/ retorna todos os testes passando (Phase 1 + Phase 2 inteiros)
  </acceptance_criteria>
  <done>Polling cycle orquestra coleta completa para todos os grupos ativos. Scheduler dispara a cada 6h. Falhas por grupo nao propagam. main.py integra scheduler via lifespan. Todos os testes verdes.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/ -v retorna todos os testes passando
- grep "init_scheduler" main.py retorna match
- grep "shutdown_scheduler" main.py retorna match
- grep "IntervalTrigger(hours=6)" app/scheduler.py retorna match
- grep "run_polling_cycle" app/services/polling_service.py retorna match
- grep "except Exception" app/services/polling_service.py retorna match (error handling por grupo)
</verification>

<success_criteria>
- Scheduler registra job de polling com intervalo de 6 horas e integra com lifespan do FastAPI
- Polling cycle busca todos os grupos ativos, gera combinacoes origem x destino x data, busca top 5 ofertas
- Para cada oferta: captura availability (booking classes) e price metrics, persiste snapshot completo
- Falha em um grupo nao impede polling dos demais
- Client nao configurado faz cycle pular graciosamente
- Todos os testes passando (Phase 1 + Phase 2 completa)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-collection/02-03-SUMMARY.md`
</output>
