---
phase: 36-multi-leg
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - app/services/dashboard_service.py
  - app/services/alert_service.py
  - app/templates/dashboard/index.html
  - app/templates/dashboard/detail.html
  - tests/test_multi_leg_email.py
  - tests/test_dashboard.py
autonomous: false
requirements: [MULTI-03, MULTI-04]
must_haves:
  truths:
    - "Dashboard home lista grupo multi como card com badge MULTI em ciano, cadeia GRU -> FCO -> MAD -> GRU, label 'Preco total do roteiro', preco total em mono 28px, recomendacao Phase 34"
    - "Pagina /groups/{id} mostra breakdown trecho-a-trecho com preco, data e companhia de cada leg + total"
    - "Cada leg no detalhe tem cluster 'Comparar este trecho em' com 4 botoes (Google Flights, Decolar, Skyscanner, Kayak) para busca individual (D-17)"
    - "Email consolidado multi tem subject 'Orbita multi: {nome} R$ X,XX (N trechos)' e corpo com recomendacao Phase 34 OBRIGATORIA no topo, cadeia, total em destaque, tabela breakdown"
    - "Card multi mantem paridade de altura com card roundtrip"
    - "Empty state (grupo multi sem snapshot) mostra 'Primeira busca em andamento...'"
  artifacts:
    - path: "app/services/dashboard_service.py"
      provides: "get_groups_with_summary retorna legs_breakdown quando group.mode=multi_leg"
      contains: "if group.mode == \"multi_leg\""
    - path: "app/services/alert_service.py"
      provides: "_render_consolidated_html_multi + subject Orbita multi; bloco de recomendacao D-19 renderizado mandatoriamente no topo"
      contains: "Orbita multi:"
    - path: "app/templates/dashboard/index.html"
      provides: "Card variant multi com badge + cadeia + total"
      contains: "card-multi"
    - path: "app/templates/dashboard/detail.html"
      provides: "Layout breakdown trecho-a-trecho com comparadores por leg"
      contains: "multi-breakdown"
  key_links:
    - from: "dashboard_service.get_groups_with_summary"
      to: "FlightSnapshot.details JSON"
      via: "parse details.legs em legs_breakdown dict"
      pattern: "snapshot\\.details\\[\"legs\"\\]"
    - from: "alert_service.compose_consolidated_email"
      to: "FlightSnapshot.details + group.mode"
      via: "branch por group.mode, template multi com recomendacao D-19 + cadeia + tabela"
      pattern: "if group.mode == \"multi_leg\""
    - from: "template detail.html"
      to: "booking_urls por leg"
      via: "dashboard_service expoe URL comparadores por leg individual (one-way)"
      pattern: "leg.compare_urls"
---

<objective>
Adaptar dashboard (home + detalhe) e email consolidado para o modo multi-trecho. Renderizar card com cadeia de trechos, badge MULTI, preco total; pagina de detalhe com breakdown trecho-a-trecho; email consolidado com subject e corpo multi conforme D-19, D-20, incluindo recomendacao Phase 34 MANDATORIAMENTE no topo do corpo.

Purpose: Cobrir D-16 (card), D-17 (comparadores por trecho), D-18 (detalhe breakdown), D-19 (email multi com recomendacao mandatoria), D-20 (subject). Sem este plano, usuario nao tem como ver/receber informacao dos grupos multi criados e busca-dos pelos plans anteriores.

Output: Extensao em `dashboard_service` e `alert_service` + render em `index.html`/`detail.html` + testes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-multi-leg/36-CONTEXT.md
@.planning/phases/36-multi-leg/36-UI-SPEC.md
@.planning/phases/36-multi-leg/36-RESEARCH.md
@.planning/phases/36-multi-leg/36-02-SUMMARY.md
@.planning/phases/36-multi-leg/36-03-SUMMARY.md
@app/services/dashboard_service.py
@app/services/alert_service.py
@app/templates/dashboard/index.html
@app/templates/dashboard/detail.html
@app/templates/base.html

<interfaces>
<!-- Contratos-alvo para dashboard e email multi. -->

dashboard_service.get_groups_with_summary — cada item do retorno ganha (quando mode=multi_leg):
```python
{
  # campos roundtrip existentes...
  "mode": "multi_leg",
  "legs_chain": ["GRU", "FCO", "MAD", "GRU"],  # lista de IATA para render de cadeia
  "legs_count": 3,
  "total_price": 5432.10,  # = snapshot.price quando snapshot nao None
  "legs_breakdown": [  # None quando sem snapshot
    {"order": 1, "origin": "GRU", "destination": "FCO", "date": "2026-06-10", "date_br": "10/06/2026",
     "price": 2100.00, "airline": "AZ",
     "compare_urls": {"google": "...", "decolar": "...", "skyscanner": "...", "kayak": "..."}},
    ...
  ],
  "recommendation": { ... }  # reuso Phase 34 (sobre total_price)
}
```

alert_service — nova funcao + ramo em compose:
```python
def compose_consolidated_email(db, group, snapshots, signals) -> EmailPayload:
    if group.mode == "multi_leg":
        return _render_consolidated_multi(db, group, snapshots[0], signals)
    # fluxo roundtrip existente

def _render_consolidated_multi(db, group, snapshot, signals) -> EmailPayload:
    # Subject: "Orbita multi: {group.name} R$ {total} ({N} trechos)" — D-20
    # HTML (MANDATORIO D-19):
    #   1. RECOMENDACAO Phase 34 NO TOPO (sempre renderizada; render inline se helper nao existir)
    #   2. Cadeia mono 20px 700
    #   3. Total mono 28px 700 em cor brand
    #   4. Tabela breakdown "Trecho · Rota · Data · Preco", linhas por leg
    #   5. Rodape total
    #   6. Cluster comparadores por leg
    #   7. CTA ver detalhes
    # Plain text fallback: idem ordem, com recomendacao antes da cadeia.
```

booking_urls helper — PRE-TASK DISCOVERY (Task 1):
- Arquivo esperado: `app/services/booking_urls.py` OU helper em `app/utils/` OU funcao em `app/services/dashboard_service.py`. Confirmar via `grep -rn "def booking_urls" app/`.
- Assinatura atual esperada (roundtrip): `booking_urls(origin, destination, departure_date, return_date)`.
- Questao critica: `return_date` aceita `None` para one-way? Se NAO, usar fallback `return_date=departure_date` com marcador one-way OU criar helper `booking_urls_oneway(origin, destination, departure_date)`. Decisao obrigatoria no Task 1, documentada no SUMMARY.

Template tokens permitidos (from UI-SPEC): ver Plan 03 `<interfaces>` (mesma lista).

Copy pt-BR exata (from 36-UI-SPEC.md, verbatim):
- Badge: `MULTI` (mono 12px 700, fundo `rgba(34,211,238,0.12)`, cor `--accent-500`)
- Cadeia: `GRU -> FCO -> MAD -> GRU` (seta ASCII ` -> ` em `--accent-500`)
- Label preco card: "Preco total do roteiro"
- Numero trechos: "N trechos"
- Titulo secao detalhe: "Combinacao mais barata encontrada"
- Header leg detalhe: "Trecho N: ORIG -> DEST"
- Label data leg: "Saida em {DD/MM/AAAA}"
- Via companhia: "via {AIRLINE}"
- Label total detalhe: "Total do roteiro"
- Cluster comparadores: "Comparar este trecho em"
- Empty state: "Primeira busca em andamento. O preco total aparece apos o proximo ciclo de polling."
- Subject email: `Orbita multi: {name} R$ {total_br} ({N} trechos)`
- Header cadeia email: mesmo formato do card
- Label total email topo: "Preco total do roteiro"
- Label recomendacao email topo: "Recomendacao: {label} — {reason}" (seta ASCII ` - ` se `—` proibido; UI-SPEC usa hifen simples)
- Tabela headers: `Trecho`, `Rota`, `Data`, `Preco`
- Rodape breakdown: "Total: R$ X.XXX,XX"
- CTA fallback: "Ver detalhes em https://orbita-flights.fly.dev/groups/{id}"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender dashboard_service.get_groups_with_summary com legs_breakdown, cadeia e booking_urls one-way</name>
  <read_first>
    - app/services/dashboard_service.py (ler inteiro: `get_groups_with_summary`, uso de `booking_urls` helper, TODOS os imports)
    - **Executar antes de editar:** `grep -rn "def booking_urls" app/` para localizar o arquivo EXATO que define `booking_urls`, e em seguida ler esse arquivo inteiro
    - app/services/price_prediction_service.py (funcao `predict_action` assinatura Phase 34)
    - app/models.py (FlightSnapshot.details apos Plan 01; RouteGroup.legs apos Plan 01)
    - .planning/phases/36-multi-leg/36-CONTEXT.md (D-15 first_leg_date; D-16 card)
    - .planning/phases/36-multi-leg/36-RESEARCH.md (Pitfall 3 days_to_departure; Pitfall 7 guard quando snapshot None)
    - tests/test_dashboard.py (padrao de teste)
  </read_first>
  <files>app/services/dashboard_service.py</files>
  <behavior>
    - Test: test_renders_multi_leg_card — get_groups_with_summary retorna grupo multi com legs_chain=[GRU,FCO,MAD,GRU], legs_count=3, total_price=snapshot.price, legs_breakdown com len=3
    - Test (Pitfall 7): grupo multi sem snapshot retorna legs_breakdown=None e total_price=None; nao raises
    - Test (Pitfall 3): prediction recebe days_to_departure calculado com snapshot.departure_date (primeiro leg)
    - Test: cada `legs_breakdown[i].compare_urls` tem 4 chaves (google, decolar, skyscanner, kayak) e representa busca ONE-WAY daquele trecho (nao roundtrip do grupo inteiro)
    - Fluxo roundtrip existente NAO altera retorno
  </behavior>
  <action>
    **Pre-task discovery OBRIGATORIA (antes de editar):**
    ```bash
    grep -rn "def booking_urls" app/
    ```
    Ler o arquivo retornado inteiro e documentar em comentario no codigo:
    - Caminho EXATO do arquivo
    - Assinatura EXATA atual
    - Se aceita `return_date=None` → usar diretamente nas chamadas one-way abaixo
    - Se NAO aceita `None` → escolher um dos dois caminhos e registrar no SUMMARY:
      (a) chamar com `return_date=departure_date` e passar um flag adicional se existir; OU
      (b) criar novo helper `booking_urls_oneway(origin, destination, departure_date)` no MESMO arquivo onde `booking_urls` vive, retornando as 4 URLs com parametros de one-way dos respectivos agregadores (Google Flights `tfs` com apenas outbound, Decolar `type=OW`, Skyscanner rota sem return, Kayak `/oneway/`).

    Em `app/services/dashboard_service.py::get_groups_with_summary`, apos montar o dict base do grupo:

    ```python
    if group.mode == "multi_leg":
        legs = sorted(group.legs, key=lambda l: l.order)
        chain = [legs[0].origin] + [leg.destination for leg in legs]
        result["mode"] = "multi_leg"
        result["legs_chain"] = chain
        result["legs_count"] = len(legs)

        latest_snapshot = result.get("latest_snapshot")
        if latest_snapshot is not None and latest_snapshot.details and latest_snapshot.airline == "MULTI":
            result["total_price"] = latest_snapshot.price
            breakdown = []
            for leg_detail in latest_snapshot.details.get("legs", []):
                dep_date = date.fromisoformat(leg_detail["date"])
                # One-way por leg: ver pre-task discovery. Assume booking_urls com return_date=None;
                # se nao aceitar, trocar pela alternativa registrada no SUMMARY.
                compare_urls = booking_urls(
                    origin=leg_detail["origin"],
                    destination=leg_detail["destination"],
                    departure_date=dep_date,
                    return_date=None,
                )
                breakdown.append({
                    "order": leg_detail["order"],
                    "origin": leg_detail["origin"],
                    "destination": leg_detail["destination"],
                    "date": leg_detail["date"],
                    "date_br": dep_date.strftime("%d/%m/%Y"),
                    "price": leg_detail["price"],
                    "airline": leg_detail["airline"],
                    "compare_urls": compare_urls,
                })
            result["legs_breakdown"] = breakdown

            # Recomendacao Phase 34 sobre total (D-15: days_to_departure do PRIMEIRO leg)
            if len(totals_history) >= MIN_SNAPSHOTS_FOR_PREDICTION:
                days_to_dep = (latest_snapshot.departure_date - date.today()).days
                result["recommendation"] = predict_action(
                    current_price=latest_snapshot.price,
                    median=statistics.median(totals_history),
                    stddev=statistics.stdev(totals_history) if len(totals_history) > 1 else 0,
                    days_to_departure=days_to_dep,
                    snapshot_count=len(totals_history),
                    departure_date=latest_snapshot.departure_date,
                )
        else:
            result["total_price"] = None
            result["legs_breakdown"] = None  # Pitfall 7: guard no template
            result["recommendation"] = None
    ```

    Notas:
    - `totals_history` = lista de `price` de snapshots multi do mesmo grupo nos ultimos 90 dias. Extrair via query similar ao fluxo roundtrip mas filtrando `airline == "MULTI"`.
    - Importar `date, statistics` se ainda nao importados.
    - NAO quebrar contrato existente de `get_groups_with_summary` para roundtrip.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Pre-task grep `grep -rn "def booking_urls" app/` retornou >=1 match e o executor documentou no SUMMARY: (arquivo, assinatura, decisao sobre one-way)
    - `grep -n "if group.mode == \"multi_leg\":" app/services/dashboard_service.py` retorna >=1 match
    - `grep -n "legs_chain" app/services/dashboard_service.py` retorna >=1 match
    - `grep -n "legs_breakdown" app/services/dashboard_service.py` retorna >=2 matches (set quando tem snapshot + None quando nao)
    - `grep -n "legs_count" app/services/dashboard_service.py` retorna >=1 match
    - `grep -n "details.get(\"legs\"" app/services/dashboard_service.py` retorna >=1 match OR equivalent access pattern
    - `grep -n "strftime(\"%d/%m/%Y\")" app/services/dashboard_service.py` retorna >=1 match
    - `grep -n "predict_action" app/services/dashboard_service.py` retorna >=1 match (Pitfall 3 usa first_leg)
    - `grep -nE "booking_urls(_oneway)?\(" app/services/dashboard_service.py` retorna >=1 match dentro da branch multi
    - Pytest `tests/test_dashboard.py` sem regressao; novos asserts de multi PASSED (ou adicionar se faltar). Adicionar teste `test_multi_leg_compare_urls_are_one_way` que verifica cada breakdown tem 4 URLs e nenhuma contem `return_date=` de 6+ meses no futuro (confirmacao heuristica de one-way).
  </acceptance_criteria>
  <done>dashboard_service expoe legs_breakdown + legs_chain + total_price; compare_urls por leg sao one-way reais (decisao documentada); Pitfalls 3 e 7 respeitados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar render multi em alert_service + subject D-20 + recomendacao D-19 mandatoria no topo</name>
  <read_first>
    - app/services/alert_service.py (ler inteiro: `compose_consolidated_email`, `_render_consolidated_html`, `_render_consolidated_plain`, geracao de subject, e QUALQUER helper existente de render de recomendacao — ex `_render_recommendation_block`)
    - **Executar antes de editar:** `grep -nE "def (_render_recommendation|render_recommendation|_recommendation_html|format_recommendation)" app/services/alert_service.py app/services/*.py app/utils/*.py` — localizar helper reutilizavel de render de recomendacao Phase 34
    - .planning/phases/36-multi-leg/36-UI-SPEC.md (secao "Email consolidado multi")
    - .planning/phases/36-multi-leg/36-CONTEXT.md (D-19, D-20)
    - tests/test_multi_leg_email.py (ver test_consolidated_multi_has_chain_and_total)
  </read_first>
  <files>app/services/alert_service.py, tests/test_multi_leg_email.py</files>
  <behavior>
    - Test: test_consolidated_multi_has_chain_and_total — compose para grupo multi retorna EmailPayload com subject no formato `Orbita multi: {name} R$ X,XX (N trechos)` e HTML contendo "GRU -> FCO" (arrow ASCII), "Preco total do roteiro", tabela com breakdown
    - Test NOVO: `test_consolidated_multi_has_recommendation_before_legs` — HTML contem bloco de recomendacao (classe/section identificavel) ANTES do primeiro "Trecho 1" E antes da cadeia. D-19 mandatorio.
    - Plain text tem linhas "Trecho N: ORIG -> DEST em DD/MM/AAAA - R$ X,XX"
    - Plain text tambem tem "Recomendacao:" ANTES da linha da cadeia
    - Fluxo roundtrip existente nao altera (regressao)
  </behavior>
  <action>
    **Pre-task discovery:**
    ```bash
    grep -nE "def (_render_recommendation|render_recommendation|_recommendation_html|format_recommendation)" app/services/alert_service.py app/services/*.py app/utils/*.py
    ```
    - Se helper existe: reusar. Chamar dentro de `_render_consolidated_multi` e inserir o retorno no topo do corpo.
    - Se NAO existe: criar INLINE dentro de `_render_consolidated_multi` um bloco minimo (HTML + plain) usando os mesmos campos que o dashboard ja computou (`recommendation.label`, `recommendation.reason`, `recommendation.class_slug`). A recomendacao e MANDATORIA no email multi por D-19 — nao pode ser omitida mesmo se o helper ainda nao existir.
    - Se `recommendation` for None (snapshots insuficientes), renderizar bloco com texto de fallback: `"Recomendacao: Coletando historico. Aguarde alguns ciclos para uma recomendacao confiavel."` — ainda OBRIGATORIO aparecer no topo para satisfazer D-19.

    Em `app/services/alert_service.py`:

    1. Adicionar `_render_consolidated_multi(db, group, snapshot, signals) -> EmailPayload`. Estrutura HTML (ordem FIXA por D-19):

       **Ordem obrigatoria no corpo HTML:**
       1. `<section class="recommendation-block">` — recomendacao (topo, sempre presente)
       2. `<div class="chain">` — cadeia mono
       3. `<div class="total-top">` — label "Preco total do roteiro" + valor mono 28px brand
       4. `<table class="legs-breakdown">` — tabela breakdown
       5. `<div class="legs-total-footer">` — rodape "Total: R$ X,XX"
       6. `<section class="compare-clusters">` — cluster comparadores por leg
       7. `<p class="cta">` — CTA "Ver detalhes em ..."

       Parse `snapshot.details["legs"]` ordenado por `order`. Computar chain string ASCII: `" -> ".join([legs[0].origin] + [l.destination for l in legs])`.

       Subject: `f"Orbita multi: {group.name} R$ {format_price_br(snapshot.price)} ({len(legs)} trechos)"` — usar helper ja existente de formatacao pt-BR com ponto de milhar e virgula decimal.

       Estilos inline (email nao suporta CSS vars): mapear tokens para hex conforme `base.html` — `--brand-500` = `#6366F1`, `--accent-500` = `#22D3EE`, `--text-0` = `#FFFFFF` ou preto sobre fundo claro, etc. Essa excecao e justificada e documentada.

       Plain text (ordem identica):
       ```
       Orbita multi: {name}

       Recomendacao: {recommendation.label} - {recommendation.reason}
       (ou fallback se recommendation is None)

       GRU -> FCO -> MAD -> GRU
       Preco total do roteiro: R$ X.XXX,XX

       Trecho 1: GRU -> FCO em 10/06/2026 - R$ 2.100,00 (via AZ)
       Trecho 2: FCO -> MAD em 24/06/2026 - R$ 450,00 (via IB)
       Trecho 3: MAD -> GRU em 04/07/2026 - R$ 2.882,10 (via LA)

       Total: R$ 5.432,10

       Ver detalhes em https://orbita-flights.fly.dev/groups/{id}
       ```

    2. Em `compose_consolidated_email`, adicionar branch no topo:
       ```python
       if group.mode == "multi_leg":
           return _render_consolidated_multi(db, group, snapshots[0] if snapshots else None, signals)
       # fluxo roundtrip existente...
       ```

    3. Em `tests/test_multi_leg_email.py`, ADICIONAR teste que valida ordem:
       ```python
       def test_consolidated_multi_has_recommendation_before_legs(db, multi_leg_group_factory, multi_leg_snapshot_factory):
           group = multi_leg_group_factory(num_legs=3)
           snapshot = multi_leg_snapshot_factory(group)
           payload = compose_consolidated_email(db, group, [snapshot], signals=[])
           html = payload.html
           # D-19: recomendacao mandatoria no TOPO
           recommendation_pos = html.find("recommendation-block")  # ou "Recomendacao" se classe nao existe
           chain_pos = html.find("GRU")  # comeco da cadeia
           first_trecho_pos = html.find("Trecho 1")
           assert recommendation_pos >= 0, "bloco de recomendacao ausente no corpo"
           assert recommendation_pos < chain_pos, "recomendacao deve vir antes da cadeia"
           assert recommendation_pos < first_trecho_pos, "recomendacao deve vir antes da tabela de legs"
           # Plain text idem
           text = payload.text
           assert "Recomendacao:" in text
           assert text.find("Recomendacao:") < text.find("Trecho 1")
           assert text.find("Recomendacao:") < text.find("Preco total do roteiro")
       ```

    Observacao sobre inline CSS em email: email clients (Gmail, Outlook) nao suportam CSS variables nem `<style>` externa confiavelmente. UI-SPEC reconhece isso implicitamente (tokens sao para o produto web; email pode usar hex direto). Documentar no SUMMARY essa excecao.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_multi_leg_email.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _render_consolidated_multi" app/services/alert_service.py` retorna 1 match
    - `grep -n "if group.mode == \"multi_leg\":" app/services/alert_service.py` retorna 1 match
    - `grep -n "Orbita multi:" app/services/alert_service.py` retorna 1 match
    - `grep -n "Preco total do roteiro" app/services/alert_service.py` retorna >=1 match
    - `grep -n " -> " app/services/alert_service.py` retorna >=1 match
    - `grep -n "trechos)" app/services/alert_service.py` retorna >=1 match (formato subject)
    - `grep -nE "(booking_urls|compare_urls)" app/services/alert_service.py` retorna >=1 match
    - `grep -nE "(recommendation-block|Recomendacao:|_render_recommendation)" app/services/alert_service.py` retorna >=2 matches (HTML + plain) — garante bloco de recomendacao presente (D-19 mandatorio)
    - No HTML gerado por `_render_consolidated_multi`, a posicao do bloco de recomendacao e MENOR que a posicao do primeiro "Trecho 1" e menor que o inicio da cadeia (validado pelo teste `test_consolidated_multi_has_recommendation_before_legs`)
    - Pytest `tests/test_multi_leg_email.py::test_consolidated_multi_has_chain_and_total` PASSED
    - Pytest `tests/test_multi_leg_email.py::test_consolidated_multi_has_recommendation_before_legs` PASSED
    - Testes de email roundtrip pre-existentes sem regressao
  </acceptance_criteria>
  <done>Email multi renderiza com RECOMENDACAO MANDATORIA no topo (D-19), cadeia, total, breakdown e subject D-20; branch nao toca fluxo roundtrip. Teste de ordem confirma posicionamento.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar card multi em index.html e breakdown em detail.html</name>
  <read_first>
    - app/templates/dashboard/index.html (ler inteiro: estrutura do card roundtrip atual — classes, tamanhos, borda colorida por recomendacao)
    - app/templates/dashboard/detail.html (ler inteiro: layout atual — tabela snapshots, grafico Chart.js, cluster comparadores)
    - app/templates/base.html (tokens CSS)
    - .planning/phases/36-multi-leg/36-UI-SPEC.md (secoes "Card do dashboard (multi)", "Pagina de detalhe", "Component Inventory")
  </read_first>
  <files>app/templates/dashboard/index.html, app/templates/dashboard/detail.html</files>
  <behavior>
    - index.html renderiza card com `class="card card-multi"` quando group.mode==multi_leg
    - Card mostra: badge MULTI (mono 12px 700, fundo rgba(34,211,238,0.12), cor --accent-500), cadeia (mono 14px 500 com setas em --accent-500), label "Preco total do roteiro" 13px 500 --text-2, preco mono 28px 700, "N trechos" 12px 500 --text-3, recomendacao Phase 34
    - Empty state (sem snapshot): "Primeira busca em andamento. O preco total aparece apos o proximo ciclo de polling."
    - detail.html renderiza secao `<section class="multi-breakdown">` com header "Combinacao mais barata encontrada", linhas por leg (header "Trecho N: ORIG -> DEST", "Saida em DD/MM/AAAA", preco mono 20px 700, "via AIRLINE" 13px 500 --text-2), separador --border-2, rodape "Total do roteiro" 14px 600 + valor mono 28px 700
    - Cada leg no detail tem cluster "Comparar este trecho em" com 4 botoes (D-17)
    - Guard `{% if group.legs_breakdown %}` protege contra None (Pitfall 7)
  </behavior>
  <action>
    **Em index.html:**

    Localizar o loop de grupos. Adicionar ramificacao por mode:
    ```html
    {% for group in groups %}
      {% if group.mode == "multi_leg" %}
        <article class="card card-multi" data-group-id="{{ group.id }}">
          <header class="card-multi-header">
            <span class="badge-multi">MULTI</span>
            <h3 class="card-title">{{ group.name }}</h3>
          </header>
          <div class="chain">
            {% for iata in group.legs_chain %}
              <span class="iata">{{ iata }}</span>
              {% if not loop.last %}<span class="arrow"> -&gt; </span>{% endif %}
            {% endfor %}
          </div>
          {% if group.total_price %}
            <p class="price-label">Preco total do roteiro</p>
            <p class="price-value">R$ {{ group.total_price | format_br }}</p>
            <p class="legs-count">{{ group.legs_count }} trechos</p>
            {% if group.recommendation %}
              <div class="recommendation recommendation-{{ group.recommendation.class_slug }}">
                <strong>{{ group.recommendation.label }}</strong>
                <span>{{ group.recommendation.reason }}</span>
              </div>
            {% endif %}
          {% else %}
            <p class="empty-state">Primeira busca em andamento. O preco total aparece apos o proximo ciclo de polling.</p>
          {% endif %}
          <footer class="card-actions">
            <a href="/groups/{{ group.id }}" class="btn-detail">Ver detalhes</a>
          </footer>
        </article>
      {% else %}
        {# card roundtrip existente inalterado #}
      {% endif %}
    {% endfor %}
    ```

    Adicionar ao `<style>` (ou arquivo CSS dedicado) apenas com tokens:
    ```css
    .card-multi { border-left: 3px solid var(--accent-500); }
    .card-multi-header { display: flex; align-items: center; gap: var(--sp-2); }
    .badge-multi { font-family: "JetBrains Mono", monospace; font-size: 12px; font-weight: 700; background: rgba(34,211,238,0.12); color: var(--accent-500); padding: var(--sp-1) var(--sp-2); border-radius: 4px; letter-spacing: 0.05em; }
    .chain { font-family: "JetBrains Mono", monospace; font-size: 14px; font-weight: 500; color: var(--text-0); margin: var(--sp-3) 0; display: flex; flex-wrap: wrap; gap: var(--sp-1); }
    .chain .arrow { color: var(--accent-500); }
    .price-label { font-size: 13px; font-weight: 500; color: var(--text-2); margin: 0; }
    .price-value { font-family: "JetBrains Mono", monospace; font-size: 28px; font-weight: 700; color: var(--text-0); margin: var(--sp-1) 0; }
    .legs-count { font-size: 12px; font-weight: 500; color: var(--text-3); }
    .empty-state { font-size: 14px; color: var(--text-2); background: var(--bg-2); padding: var(--sp-3); border-radius: 4px; }
    @media (max-width: 768px) { .chain { flex-direction: column; align-items: flex-start; } }
    ```

    **Em detail.html:**

    Apos bloco de metadados do grupo, adicionar (guarded por mode):
    ```html
    {% if group.mode == "multi_leg" and group.legs_breakdown %}
      <section class="multi-breakdown">
        <h2>Combinacao mais barata encontrada</h2>
        <div class="chain">
          {% for iata in group.legs_chain %}
            <span class="iata">{{ iata }}</span>
            {% if not loop.last %}<span class="arrow"> -&gt; </span>{% endif %}
          {% endfor %}
        </div>
        {% for leg in group.legs_breakdown %}
          <article class="leg-row">
            <header class="leg-row-header">
              <h3>Trecho {{ leg.order }}: {{ leg.origin }} -&gt; {{ leg.destination }}</h3>
              <span class="leg-date">Saida em {{ leg.date_br }}</span>
            </header>
            <p class="leg-price">R$ {{ leg.price | format_br }}</p>
            <p class="leg-airline">via {{ leg.airline }}</p>
            <div class="compare-cluster">
              <span class="compare-label">Comparar este trecho em</span>
              <a href="{{ leg.compare_urls.google }}" target="_blank" rel="noopener" class="compare-btn">Google Flights</a>
              <a href="{{ leg.compare_urls.decolar }}" target="_blank" rel="noopener" class="compare-btn">Decolar</a>
              <a href="{{ leg.compare_urls.skyscanner }}" target="_blank" rel="noopener" class="compare-btn">Skyscanner</a>
              <a href="{{ leg.compare_urls.kayak }}" target="_blank" rel="noopener" class="compare-btn">Kayak</a>
            </div>
          </article>
        {% endfor %}
        <hr class="total-separator">
        <div class="total-row">
          <span class="total-label">Total do roteiro</span>
          <span class="total-value">R$ {{ group.total_price | format_br }}</span>
        </div>
      </section>
    {% elif group.mode == "multi_leg" %}
      <section class="multi-empty">
        <p>Primeira busca em andamento. O preco total aparece apos o proximo ciclo de polling.</p>
      </section>
    {% endif %}
    ```

    CSS com tokens (inline ou em secao styles):
    ```css
    .multi-breakdown { margin: var(--sp-6) 0; }
    .multi-breakdown h2 { font-size: 16px; font-weight: 600; }
    .leg-row { background: var(--bg-2); border-radius: 6px; padding: var(--sp-4); margin-bottom: var(--sp-5); }
    .leg-row-header { display: flex; justify-content: space-between; align-items: baseline; flex-wrap: wrap; gap: var(--sp-2); }
    .leg-row-header h3 { font-size: 16px; font-weight: 600; margin: 0; }
    .leg-date { font-size: 13px; font-weight: 500; color: var(--text-2); }
    .leg-price { font-family: "JetBrains Mono", monospace; font-size: 20px; font-weight: 700; color: var(--text-0); margin: var(--sp-2) 0 var(--sp-1) 0; }
    .leg-airline { font-size: 13px; font-weight: 500; color: var(--text-2); margin-bottom: var(--sp-3); }
    .compare-cluster { display: flex; flex-wrap: wrap; gap: var(--sp-2); align-items: center; }
    .compare-label { font-size: 13px; font-weight: 500; color: var(--text-2); }
    .compare-btn { font-size: 13px; padding: var(--sp-1) var(--sp-3); border: 1px solid var(--border-2); border-radius: 4px; color: var(--text-1); text-decoration: none; }
    .compare-btn:hover { border-color: var(--accent-500); color: var(--accent-500); }
    .total-separator { border: 0; border-top: 1px solid var(--border-2); margin: var(--sp-4) 0; }
    .total-row { display: flex; justify-content: space-between; align-items: baseline; }
    .total-label { font-size: 14px; font-weight: 600; }
    .total-value { font-family: "JetBrains Mono", monospace; font-size: 28px; font-weight: 700; color: var(--text-0); }
    ```

    Notas:
    - Seta HTML `-&gt;` para renderizar `->` sem interpretacao HTML
    - `format_br` e filtro Jinja existente; se nao existir, adicionar: `{{ "%.2f" | format(group.total_price) | replace(",", "X") | replace(".", ",") | replace("X", ".") }}` ou usar helper existente
    - Guard Jinja `{% if group.legs_breakdown %}` protege Pitfall 7
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_dashboard.py -x -q && .venv/Scripts/python.exe -c "from fastapi.testclient import TestClient; from app.main import app; c = TestClient(app); print('templates importaveis')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class=\"card card-multi\"" app/templates/dashboard/index.html` retorna 1 match
    - `grep -n "badge-multi" app/templates/dashboard/index.html` retorna >=2 matches (CSS + markup)
    - `grep -n "Preco total do roteiro" app/templates/dashboard/index.html` retorna 1 match
    - `grep -n "Primeira busca em andamento" app/templates/dashboard/index.html` retorna 1 match
    - `grep -n "multi-breakdown" app/templates/dashboard/detail.html` retorna >=2 matches
    - `grep -n "Combinacao mais barata encontrada" app/templates/dashboard/detail.html` retorna 1 match
    - `grep -n "Comparar este trecho em" app/templates/dashboard/detail.html` retorna 1 match
    - `grep -n "Total do roteiro" app/templates/dashboard/detail.html` retorna 1 match
    - `grep -n "{% if group.legs_breakdown %}" app/templates/dashboard/detail.html` retorna 1 match
    - `grep -nE "#[0-9a-fA-F]{3,6}" app/templates/dashboard/index.html | grep -v "rgba(34,211,238"` — sem novos hex fora da excecao da rgba do badge
    - NAO ha travessao: `grep -nE " \u2013 | \u2014 " app/templates/dashboard/index.html app/templates/dashboard/detail.html` retorna 0 matches
  </acceptance_criteria>
  <done>Card e detalhe multi renderizam com tokens, copy pt-BR exata e guards para empty state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint visual dashboard multi + verificacao email em Gmail</name>
  <what-built>
    Card multi em `/` (dashboard home), pagina de detalhe `/groups/{id}` com breakdown, email consolidado multi enviado pelo alert_service (com recomendacao mandatoria no topo).
  </what-built>
  <how-to-verify>
    1. Com grupo multi ja criado (via Plan 03) e primeiro ciclo de polling executado (Plan 02), acessar `http://localhost:8000/`.
    2. Conferir card multi:
       - Borda esquerda ciano 3px (`--accent-500`)
       - Badge `MULTI` no canto, mono, fundo ciano transparente
       - Cadeia `GRU -> FCO -> MAD -> GRU` em JetBrains Mono 14px, setas em ciano
       - Label "Preco total do roteiro" 13px
       - Preco mono 28px bold
       - "N trechos" em cinza
       - Recomendacao Phase 34 renderizada se ja houver >=MIN_SNAPSHOTS
       - Altura do card similar ao card roundtrip (paridade visual)
    3. Sem snapshot ainda: aparece "Primeira busca em andamento..."
    4. Clicar "Ver detalhes" → pagina de detalhe:
       - Titulo "Combinacao mais barata encontrada"
       - Cadeia no topo
       - N blocos (um por trecho): "Trecho N: ORIG -> DEST", data BR, preco mono 20px, via AIRLINE, cluster 4 botoes comparadores
       - Clicar cada botao abre busca ONE-WAY do leg em nova aba (Google Flights, Decolar, Skyscanner, Kayak) — confirmar que nao ha return_date nas URLs
       - Separador horizontal + "Total do roteiro" + valor mono 28px
    5. Email: disparar `_send_consolidated_alert` manualmente (via shell ou trigger de sinal). Abrir email em Gmail:
       - Subject: "Orbita multi: {nome} R$ X,XX (N trechos)" — exatamente
       - Corpo: **RECOMENDACAO Phase 34 NO TOPO** (primeira secao visivel, D-19 mandatorio), depois cadeia em mono 20px, total em brand 28px mono, tabela Trecho · Rota · Data · Preco com linhas, rodape "Total: R$ X,XX", cluster comparadores por leg, CTA "Ver detalhes em ..."
       - Plain text fallback tem mesmas informacoes em formato simples com `->` ASCII e "Recomendacao:" antes da cadeia
    6. Mobile devtools: card empilha cadeia, detalhe empilha colunas
    7. A11y Lighthouse: sem warnings novos; contraste OK
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list failing items with screenshot.</resume-signal>
</task>

</tasks>

<verification>
1. `.venv/Scripts/python.exe -m pytest tests/test_multi_leg_email.py tests/test_dashboard.py -x -q` — GREEN (incluindo novo teste de ordem da recomendacao)
2. Full suite `.venv/Scripts/python.exe -m pytest -q` — sem regressao
3. Checkpoint humano (Task 4) aprovado
</verification>

<success_criteria>
- D-16 card multi renderiza em dashboard home
- D-17 comparadores por leg no detalhe (URLs one-way confirmadas)
- D-18 breakdown no detalhe (so combinacao mais barata)
- D-19 email consolidado multi COM RECOMENDACAO MANDATORIA NO TOPO (HTML + plain)
- D-20 subject Orbita multi: ...
- Pitfall 7 respeitado (guard empty state)
- Pitfall 3 respeitado (days_to_departure do primeiro leg na prediction)
- Paridade visual mantida entre card multi e roundtrip
- Zero travessao, zero emoji, zero hex hardcoded em templates web (hex em email inline e excecao documentada)
</success_criteria>

<output>
Criar `.planning/phases/36-multi-leg/36-04-SUMMARY.md` com: servicos estendidos, templates atualizados, estrutura do email multi, decisao sobre booking_urls one-way (caminho escolhido), decisao sobre render helper de recomendacao (reuso vs inline), snapshot do checkpoint.

No final da fase, criar `.planning/phases/36-multi-leg/36-PHASE-SUMMARY.md` agregando os 4 plans com: MULTI-01..04 coberto, arquivos totais criados/modificados, testes GREEN no full suite, proximo passo.
</output>
</content>
</invoke>