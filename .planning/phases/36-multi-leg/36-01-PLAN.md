---
phase: 36-multi-leg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/schemas.py
  - alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py
  - tests/conftest.py
  - tests/test_multi_leg_model.py
  - tests/test_multi_leg_service.py
  - tests/test_multi_leg_routes.py
  - tests/test_multi_leg_polling.py
  - tests/test_multi_leg_email.py
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04]
must_haves:
  truths:
    - "Tabela route_group_legs existe com colunas (id, route_group_id FK cascade, order, origin, destination, window_start, window_end, min_stay_days, max_stay_days nullable, max_stops nullable)"
    - "FlightSnapshot tem coluna details JSON nullable"
    - "RouteGroup.mode aceita valor 'multi_leg'"
    - "Schema RouteGroupMultiCreate rejeita <2 ou >5 trechos com mensagem pt-BR"
    - "Schema RouteGroupMultiCreate rejeita leg[N+1].window_start < leg[N].window_end + leg[N].min_stay_days"
    - "alembic upgrade head + alembic downgrade -1 + alembic upgrade head funciona sem erro"
    - "Wave 0: 5 arquivos de teste RED existem e falham (import/fixtures validos mas asserts falham por codigo ausente)"
  artifacts:
    - path: "app/models.py"
      provides: "Classe RouteGroupLeg e relationship RouteGroup.legs; coluna FlightSnapshot.details"
      contains: "class RouteGroupLeg"
    - path: "app/schemas.py"
      provides: "LegCreate, LegOut, RouteGroupMultiCreate com model_validator"
      contains: "class RouteGroupMultiCreate"
    - path: "alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py"
      provides: "Migration Alembic com upgrade/downgrade reversiveis"
      contains: "down_revision = \"i9j0k1l2m3n4\""
    - path: "tests/conftest.py"
      provides: "Fixtures multi_leg_group_factory e multi_leg_snapshot_factory"
      contains: "def multi_leg_group_factory"
    - path: "tests/test_multi_leg_model.py"
      provides: "Testes RED de model + validacao temporal (MULTI-01, MULTI-02)"
    - path: "tests/test_multi_leg_service.py"
      provides: "Testes RED de servico (produto cartesiano, cache, persistencia) — MULTI-03"
    - path: "tests/test_multi_leg_routes.py"
      provides: "Testes RED de POST /groups com legs[] — MULTI-01"
    - path: "tests/test_multi_leg_polling.py"
      provides: "Testes RED de branch multi no polling — MULTI-04"
    - path: "tests/test_multi_leg_email.py"
      provides: "Testes RED de render email multi — D-19"
  key_links:
    - from: "app/models.py (RouteGroup)"
      to: "app/models.py (RouteGroupLeg)"
      via: "relationship legs com cascade all, delete-orphan"
      pattern: "back_populates=\"route_group\".*cascade=\"all, delete-orphan\""
    - from: "alembic migration"
      to: "app/models.py schema"
      via: "down_revision aponta para i9j0k1l2m3n4 (ultima revision atual)"
      pattern: "down_revision = \"i9j0k1l2m3n4\""
---

<objective>
Criar a fundacao de dados e contratos para roteiros multi-trecho: modelo `RouteGroupLeg`, coluna `FlightSnapshot.details`, migration Alembic, schemas Pydantic com validacao temporal e suite de testes RED (Wave 0) cobrindo MULTI-01, MULTI-02, MULTI-03 e MULTI-04 (testes de service/polling/email sao escritos agora e viram GREEN nas phases 02/04).

Purpose: Honrar SDD+TDD do CLAUDE.md. Todo servico/rota/UI das fases seguintes sera implementado contra contratos e testes definidos aqui. Sem isso, Plans 02/03/04 nao tem como validar GREEN.

Output: 3 arquivos de codigo novos/modificados + 1 migration + 5 arquivos de teste + atualizacao em conftest.py. Testes devem rodar e FALHAR (RED) com mensagens claras de `ImportError` ou `AttributeError` esperadas — nao erros de sintaxe ou fixtures quebradas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-leg/36-CONTEXT.md
@.planning/phases/36-multi-leg/36-RESEARCH.md
@.planning/phases/36-multi-leg/36-UI-SPEC.md
@app/models.py
@app/schemas.py
@tests/conftest.py
@alembic/versions/i9j0k1l2m3n4_add_affiliate_click.py

<interfaces>
<!-- Contratos-alvo que este plano cria. Executor implementa exatamente como descrito. -->

From app/models.py (NOVO):
```python
class RouteGroupLeg(Base):
    __tablename__ = "route_group_legs"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    route_group_id: Mapped[int] = mapped_column(
        ForeignKey("route_groups.id", ondelete="CASCADE"), index=True, nullable=False
    )
    order: Mapped[int] = mapped_column(Integer, nullable=False)
    origin: Mapped[str] = mapped_column(String(3), nullable=False)
    destination: Mapped[str] = mapped_column(String(3), nullable=False)
    window_start: Mapped[datetime.date] = mapped_column(Date, nullable=False)
    window_end: Mapped[datetime.date] = mapped_column(Date, nullable=False)
    min_stay_days: Mapped[int] = mapped_column(Integer, nullable=False, server_default="1")
    max_stay_days: Mapped[int | None] = mapped_column(Integer, nullable=True)
    max_stops: Mapped[int | None] = mapped_column(Integer, nullable=True)

    route_group: Mapped["RouteGroup"] = relationship("RouteGroup", back_populates="legs")

    __table_args__ = (
        Index("ix_route_group_legs_group_order", "route_group_id", "order", unique=True),
    )
```

Em `RouteGroup`:
```python
legs: Mapped[list["RouteGroupLeg"]] = relationship(
    "RouteGroupLeg",
    back_populates="route_group",
    cascade="all, delete-orphan",
    order_by="RouteGroupLeg.order",
)
```

Em `FlightSnapshot`:
```python
details: Mapped[dict | None] = mapped_column(JSON, nullable=True, default=None)
```

From app/schemas.py (NOVO):
```python
class LegCreate(BaseModel):
    order: int
    origin: str
    destination: str
    window_start: date
    window_end: date
    min_stay_days: int
    max_stay_days: int | None = None
    max_stops: int | None = None

class LegOut(LegCreate):
    id: int
    model_config = ConfigDict(from_attributes=True)

class RouteGroupMultiCreate(BaseModel):
    name: str
    passengers: int = 1
    target_price: float | None = None
    legs: list[LegCreate]

    @model_validator(mode="after")
    def validate_chain(self) -> "RouteGroupMultiCreate":
        if not (2 <= len(self.legs) <= 5):
            raise ValueError("Roteiro multi-trecho exige entre 2 e 5 trechos.")
        sorted_legs = sorted(self.legs, key=lambda l: l.order)
        for leg in sorted_legs:
            if leg.min_stay_days < 1:
                raise ValueError("Estadia minima precisa ser de pelo menos 1 dia.")
            if leg.max_stay_days is not None and leg.max_stay_days < leg.min_stay_days:
                raise ValueError("Estadia maxima nao pode ser menor que a minima.")
            if len(leg.origin) != 3 or len(leg.destination) != 3:
                raise ValueError("Codigo IATA deve ter 3 letras (exemplo: GRU, FCO, MAD).")
        for i in range(1, len(sorted_legs)):
            prev = sorted_legs[i - 1]
            cur = sorted_legs[i]
            min_start = prev.window_end + timedelta(days=prev.min_stay_days)
            if cur.window_start < min_start:
                raise ValueError(
                    f"Trecho {cur.order} precisa sair em ou apos {min_start.strftime('%d/%m/%Y')}. "
                    f"Ajuste a janela ou reduza a estadia minima do trecho {prev.order}."
                )
        return self
```

From alembic (ultima revision atual):
- `down_revision = "i9j0k1l2m3n4"` — ver `alembic/versions/i9j0k1l2m3n4_add_affiliate_click.py`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar model RouteGroupLeg + FlightSnapshot.details + RouteGroup.legs relationship</name>
  <read_first>
    - app/models.py (ler inteiro para seguir padrao SQLAlchemy 2.0 Mapped[] e convencao de __tablename__)
    - .planning/phases/36-multi-leg/36-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/36-multi-leg/36-RESEARCH.md (Exemplo 1 em Code Examples)
  </read_first>
  <files>app/models.py</files>
  <behavior>
    - Test: RouteGroupLeg pode ser persistido e recuperado com todos os campos (D-02)
    - Test: Deletar RouteGroup cascade-deleta todos os legs (cascade="all, delete-orphan")
    - Test: Constraint unique (route_group_id, order) rejeita 2 legs com mesmo order no mesmo grupo
    - Test: FlightSnapshot.details aceita dict JSON e retorna dict ao ler (nullable=True)
    - Test: RouteGroup.legs carrega legs ordenados por order ascendente
  </behavior>
  <action>
    Em app/models.py:
    1. Importar `JSON` de sqlalchemy se ainda nao importado (`from sqlalchemy import JSON, Index, ForeignKey, Date, Integer, String`).
    2. Adicionar classe `RouteGroupLeg(Base)` exatamente como definida no bloco `<interfaces>` acima (tablename "route_group_legs", 9 colunas, relationship back_populates, Index unique).
    3. Na classe `RouteGroup`, adicionar atributo `legs` como relationship conforme bloco `<interfaces>` (cascade="all, delete-orphan", order_by="RouteGroupLeg.order").
    4. Na classe `FlightSnapshot`, adicionar coluna `details: Mapped[dict | None] = mapped_column(JSON, nullable=True, default=None)`.
    5. NAO alterar nenhuma outra classe/coluna existente. NAO remover imports existentes.

    Justificativa por D-01/D-02/D-03 do CONTEXT.md. Evita tabela MultiLegSnapshot separada (Anti-Pattern do RESEARCH.md).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from app.models import RouteGroupLeg, RouteGroup, FlightSnapshot; assert hasattr(RouteGroup, 'legs'); assert hasattr(FlightSnapshot, 'details'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class RouteGroupLeg" app/models.py` retorna 1 match
    - `grep -n "back_populates=\"route_group\"" app/models.py` retorna >=1 match
    - `grep -n "cascade=\"all, delete-orphan\"" app/models.py` retorna >=1 match (linha do relationship legs)
    - `grep -n "details: Mapped\[dict | None\]" app/models.py` retorna 1 match
    - `grep -n "ix_route_group_legs_group_order" app/models.py` retorna 1 match
  </acceptance_criteria>
  <done>Model carrega sem erro de sintaxe; import de RouteGroupLeg/RouteGroup.legs/FlightSnapshot.details funciona.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar migration Alembic jXXXX com route_group_legs + flight_snapshots.details</name>
  <read_first>
    - alembic/versions/i9j0k1l2m3n4_add_affiliate_click.py (template de migration anterior, usada como down_revision)
    - alembic/versions/g7h8i9j0k1l2_add_route_cache_table.py (padrao de create_table + create_index)
    - .planning/phases/36-multi-leg/36-RESEARCH.md (Exemplo 2 em Code Examples; Pitfall 1 sobre nullable=True)
  </read_first>
  <files>alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py</files>
  <behavior>
    - Test: `alembic upgrade head` cria tabela `route_group_legs` com todas as 9 colunas
    - Test: `alembic upgrade head` adiciona coluna `details` em `flight_snapshots` (nullable=True)
    - Test: `alembic downgrade -1` remove tabela e coluna sem erro
    - Test: Reaplicar `alembic upgrade head` apos downgrade funciona (migration idempotente em ciclos)
  </behavior>
  <action>
    Criar `alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` com:

    ```python
    """add route_group_legs table and flight_snapshots.details column

    Revision ID: j0k1l2m3n4o5
    Revises: i9j0k1l2m3n4
    Create Date: 2026-04-22 ...
    """
    from alembic import op
    import sqlalchemy as sa

    revision = "j0k1l2m3n4o5"
    down_revision = "i9j0k1l2m3n4"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        op.create_table(
            "route_group_legs",
            sa.Column("id", sa.Integer(), primary_key=True, autoincrement=True),
            sa.Column("route_group_id", sa.Integer(),
                      sa.ForeignKey("route_groups.id", ondelete="CASCADE"), nullable=False),
            sa.Column("order", sa.Integer(), nullable=False),
            sa.Column("origin", sa.String(3), nullable=False),
            sa.Column("destination", sa.String(3), nullable=False),
            sa.Column("window_start", sa.Date(), nullable=False),
            sa.Column("window_end", sa.Date(), nullable=False),
            sa.Column("min_stay_days", sa.Integer(), nullable=False, server_default="1"),
            sa.Column("max_stay_days", sa.Integer(), nullable=True),
            sa.Column("max_stops", sa.Integer(), nullable=True),
        )
        op.create_index(
            "ix_route_group_legs_group_order",
            "route_group_legs", ["route_group_id", "order"], unique=True,
        )
        op.add_column(
            "flight_snapshots",
            sa.Column("details", sa.JSON(), nullable=True),
        )


    def downgrade() -> None:
        op.drop_column("flight_snapshots", "details")
        op.drop_index("ix_route_group_legs_group_order", table_name="route_group_legs")
        op.drop_table("route_group_legs")
    ```

    Per D-02, D-03 e Pitfall 1 do RESEARCH.md (nullable=True explicito). Nome da migration e discretion do planner conforme CONTEXT.md.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m alembic upgrade head && .venv/Scripts/python.exe -m alembic downgrade -1 && .venv/Scripts/python.exe -m alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "down_revision = \"i9j0k1l2m3n4\"" alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` retorna 1 match
    - `grep -n "revision = \"j0k1l2m3n4o5\"" alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` retorna 1 match
    - `grep -n "create_table\(\s*\"route_group_legs\"" alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` retorna match (multiline)
    - `grep -n "ondelete=\"CASCADE\"" alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` retorna 1 match
    - `grep -n "add_column\(\"flight_snapshots\"" alembic/versions/j0k1l2m3n4o5_add_route_group_leg_and_details.py` retorna match
    - Comando `alembic current` apos upgrade retorna `j0k1l2m3n4o5 (head)`
  </acceptance_criteria>
  <done>Migration aplica, reverte e reaplica sem erro; schema real bate com model de Task 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar schemas Pydantic LegCreate/LegOut/RouteGroupMultiCreate com validacao temporal</name>
  <read_first>
    - app/schemas.py (ler inteiro para seguir padrao Pydantic 2.x + ConfigDict(from_attributes=True))
    - .planning/phases/36-multi-leg/36-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/36-multi-leg/36-RESEARCH.md (Pattern 3 Pydantic; Pitfall 5 sort antes de validar)
    - .planning/phases/36-multi-leg/36-UI-SPEC.md (secao "Mensagens de erro (validacao temporal)" — copy exata pt-BR)
  </read_first>
  <files>app/schemas.py</files>
  <behavior>
    - Test: RouteGroupMultiCreate com 1 leg raise ValueError contendo "entre 2 e 5 trechos"
    - Test: RouteGroupMultiCreate com 6 legs raise ValueError contendo "entre 2 e 5 trechos"
    - Test: leg[1].window_start < leg[0].window_end + leg[0].min_stay_days raise ValueError contendo "precisa sair em ou apos"
    - Test: min_stay_days=0 raise ValueError contendo "pelo menos 1 dia"
    - Test: max_stay_days < min_stay_days raise ValueError contendo "nao pode ser menor que a minima"
    - Test: origin com 2 ou 4 letras raise ValueError contendo "3 letras"
    - Test: Legs passadas em ordem embaralhada no JSON sao ordenadas por `order` antes da validacao cruzada (Pitfall 5)
    - Test: LegOut.model_validate(leg_orm) serializa RouteGroupLeg com from_attributes=True
  </behavior>
  <action>
    Em app/schemas.py:
    1. Garantir imports no topo: `from datetime import date, timedelta`, `from pydantic import BaseModel, ConfigDict, model_validator`.
    2. Adicionar as 3 classes (LegCreate, LegOut, RouteGroupMultiCreate) exatamente como no bloco `<interfaces>` do context (copy de mensagens de erro corresponde a 36-UI-SPEC.md linhas "Mensagens de erro").
    3. NAO remover/alterar schemas existentes (RouteGroupCreate, RouteGroupUpdate, etc).
    4. Usar mensagens pt-BR exatas do UI-SPEC (acentuacao obrigatoria nos textos visiveis mas sem acento no codigo Python — strings sao UTF-8, escrever sem acento seguindo a copy literal do UI-SPEC que ja nao usa travessao).

    Justificativa por D-04/D-05/D-06 e Pitfall 5 (sort antes de iterar).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from app.schemas import LegCreate, LegOut, RouteGroupMultiCreate; from datetime import date; import pytest; exec('try:\n RouteGroupMultiCreate(name=\"x\", legs=[LegCreate(order=1, origin=\"GRU\", destination=\"FCO\", window_start=date(2026,6,1), window_end=date(2026,6,10), min_stay_days=1)])\n raise AssertionError(\"should have raised\")\nexcept ValueError as e:\n assert \"entre 2 e 5 trechos\" in str(e)'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class LegCreate(BaseModel)" app/schemas.py` retorna 1 match
    - `grep -n "class LegOut" app/schemas.py` retorna 1 match
    - `grep -n "class RouteGroupMultiCreate(BaseModel)" app/schemas.py` retorna 1 match
    - `grep -n "@model_validator(mode=\"after\")" app/schemas.py` retorna >=1 match
    - `grep -n "sorted(self.legs, key=lambda l: l.order)" app/schemas.py` retorna 1 match (Pitfall 5)
    - `grep -n "entre 2 e 5 trechos" app/schemas.py` retorna 1 match
    - `grep -n "precisa sair em ou apos" app/schemas.py` retorna 1 match
    - `grep -n "from_attributes=True" app/schemas.py` retorna >=1 match
  </acceptance_criteria>
  <done>Schemas importam sem erro; validacao temporal rejeita combinacoes invalidas com mensagens pt-BR exatas do UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 4: Criar fixtures multi_leg_group_factory e multi_leg_snapshot_factory em conftest.py</name>
  <read_first>
    - tests/conftest.py (ler inteiro para reusar fixtures db, test_user)
    - app/models.py (apos Task 1: ver RouteGroupLeg)
    - .planning/phases/36-multi-leg/36-RESEARCH.md (secao "Wave 0 Gaps")
  </read_first>
  <files>tests/conftest.py</files>
  <behavior>
    - Fixture `multi_leg_group_factory(db, test_user)` retorna callable que cria RouteGroup com mode="multi_leg" e N legs (default 2)
    - Fixture `multi_leg_snapshot_factory(db, multi_leg_group)` cria FlightSnapshot com airline="MULTI", details JSON preenchido
    - Ambas usam SQLite in-memory existente (nao alterar engine fixture)
  </behavior>
  <action>
    Em tests/conftest.py, adicionar ao final (ou junto das outras factories existentes):

    ```python
    @pytest.fixture
    def multi_leg_group_factory(db, test_user):
        from app.models import RouteGroup, RouteGroupLeg
        from datetime import date, timedelta

        def _factory(num_legs: int = 2, name: str = "Multi Trip Test"):
            group = RouteGroup(
                user_id=test_user.id,
                name=name,
                mode="multi_leg",
                passengers=1,
                active=True,
            )
            db.add(group)
            db.flush()
            base_date = date(2026, 6, 1)
            for i in range(num_legs):
                leg = RouteGroupLeg(
                    route_group_id=group.id,
                    order=i + 1,
                    origin=["GRU", "FCO", "MAD", "LIS", "CDG"][i],
                    destination=["FCO", "MAD", "LIS", "CDG", "GRU"][i],
                    window_start=base_date + timedelta(days=i * 20),
                    window_end=base_date + timedelta(days=i * 20 + 7),
                    min_stay_days=7,
                    max_stay_days=14,
                )
                db.add(leg)
            db.commit()
            db.refresh(group)
            return group
        return _factory


    @pytest.fixture
    def multi_leg_snapshot_factory(db):
        from app.models import FlightSnapshot
        from datetime import date

        def _factory(group, total_price: float = 6000.0):
            legs = sorted(group.legs, key=lambda l: l.order)
            snapshot = FlightSnapshot(
                route_group_id=group.id,
                origin=legs[0].origin,
                destination=legs[-1].destination,
                departure_date=legs[0].window_start,
                return_date=legs[-1].window_start,
                price=total_price,
                currency="BRL",
                airline="MULTI",
                source="multi_leg",
                details={
                    "total_price": total_price,
                    "legs": [
                        {"order": leg.order, "origin": leg.origin, "destination": leg.destination,
                         "date": leg.window_start.isoformat(),
                         "price": total_price / len(legs),
                         "airline": "AZ"}
                        for leg in legs
                    ],
                },
            )
            db.add(snapshot)
            db.commit()
            db.refresh(snapshot)
            return snapshot
        return _factory
    ```

    Depende de Task 1 (model) para imports. NAO duplicar decorators `@pytest.fixture` ja existentes.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/conftest.py --collect-only -q 2>&1 | grep -i "error" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def multi_leg_group_factory" tests/conftest.py` retorna 1 match
    - `grep -n "def multi_leg_snapshot_factory" tests/conftest.py` retorna 1 match
    - `grep -n "mode=\"multi_leg\"" tests/conftest.py` retorna >=1 match
    - `grep -n "airline=\"MULTI\"" tests/conftest.py` retorna >=1 match
  </acceptance_criteria>
  <done>Fixtures coletaveis pelo pytest; suporte in-memory SQLite preservado.</done>
</task>

<task type="auto">
  <name>Task 5: Criar 5 arquivos de teste RED (Wave 0) cobrindo MULTI-01..04 e D-19</name>
  <read_first>
    - .planning/phases/36-multi-leg/36-RESEARCH.md (secao "Phase Requirements → Test Map" e "Wave 0 Gaps")
    - .planning/phases/36-multi-leg/36-VALIDATION.md
    - tests/test_dashboard.py (padrao de teste de rota usando client)
    - tests/conftest.py (apos Task 4: fixtures disponiveis)
  </read_first>
  <files>tests/test_multi_leg_model.py, tests/test_multi_leg_service.py, tests/test_multi_leg_routes.py, tests/test_multi_leg_polling.py, tests/test_multi_leg_email.py</files>
  <behavior>
    Tests must be written as RED (failing on missing implementation, not on syntax).
    Mapeamento por requirement (copiar do RESEARCH.md > Phase Requirements → Test Map):
    - MULTI-01: test_multi_leg_routes.py::test_create_multi_leg_group_valid, test_multi_leg_model.py::test_leg_cascade_delete
    - MULTI-02: test_multi_leg_model.py::test_chain_validation_rejects_overlap, test_multi_leg_model.py::test_min_max_legs, test_multi_leg_service.py::test_is_valid_chain
    - MULTI-03: test_multi_leg_service.py::test_uses_route_cache_before_serpapi, test_multi_leg_service.py::test_persists_multi_snapshot_with_details, test_multi_leg_service.py::test_picks_cheapest_total
    - MULTI-04: test_multi_leg_polling.py::test_signal_on_total_price, test_multi_leg_service.py::test_prediction_uses_total_median
    - D-19: test_multi_leg_email.py::test_consolidated_multi_has_chain_and_total
  </behavior>
  <action>
    Criar 5 arquivos de teste. Cada arquivo segue padrao AAA, usa fixtures de conftest.py (db, test_user, client, multi_leg_group_factory, multi_leg_snapshot_factory).

    **tests/test_multi_leg_model.py:**
    - `test_leg_cascade_delete`: cria group com 3 legs, deleta group, assert `db.query(RouteGroupLeg).filter_by(route_group_id=group.id).count() == 0`.
    - `test_unique_order_constraint`: tenta criar 2 legs com mesmo order no mesmo group, assert IntegrityError.
    - `test_chain_validation_rejects_overlap`: instancia `RouteGroupMultiCreate(legs=[leg1_end=6/10 min_stay=5, leg2_start=6/12])`, assert raises ValueError com "precisa sair em ou apos".
    - `test_min_max_legs`: instancia com 1 leg → ValueError; com 6 legs → ValueError; com 2 legs validos → OK.
    - `test_flight_snapshot_details_json`: cria FlightSnapshot com details={"total_price": 5000}, refresh, assert snapshot.details["total_price"] == 5000.

    **tests/test_multi_leg_service.py:**
    - `test_is_valid_chain`: testa helper `_is_valid_chain` (sera implementado em Plan 02) — SKIP com `pytest.importorskip("app.services.multi_leg_service")` ou use try/except ImportError para manter RED sem quebrar collection.
    - `test_uses_route_cache_before_serpapi`: mock route_cache retorna preco, asserta que SerpAPI nao foi chamado.
    - `test_persists_multi_snapshot_with_details`: chama `search_multi_leg_prices`, assert snapshot criado com airline="MULTI" e details["legs"] com N entradas.
    - `test_picks_cheapest_total`: dado 2 combinacoes validas com totais 5000 e 4500, assert snapshot.price == 4500.
    - `test_prediction_uses_total_median`: mock `price_prediction_service.predict_action`, assert chamado com `days_to_departure` = dias ate primeiro leg (nao ultimo) — ver Pitfall 3.

    Para os testes que dependem de multi_leg_service (Plan 02), use import dentro do teste com try/except + `pytest.fail` para garantir RED agora:
    ```python
    def test_uses_route_cache_before_serpapi(...):
        try:
            from app.services.multi_leg_service import search_multi_leg_prices
        except ImportError:
            pytest.fail("multi_leg_service nao implementado (esperado RED ate Plan 02)")
        ...
    ```

    **tests/test_multi_leg_routes.py:**
    - `test_create_multi_leg_group_valid`: POST form em `/groups` com `mode=multi_leg` e legs[] encoded como form, assert 201 ou 302, group criado no DB com legs.
    - `test_create_multi_leg_group_invalid_chain`: POST com overlap temporal, assert 422 ou redirect com flash error contendo "precisa sair em ou apos".

    **tests/test_multi_leg_polling.py:**
    - `test_signal_on_total_price`: cria 10 snapshots multi historicos com precos 5000-6000, novo snapshot com 4000. Chama `signal_service.detect_signals`, assert sinal PRECO ABAIXO DO HISTORICO disparado sobre total.

    **tests/test_multi_leg_email.py:**
    - `test_consolidated_multi_has_chain_and_total`: cria group multi + snapshot, chama `alert_service.compose_consolidated_email` (ou funcao de render multi). Assert HTML contem "GRU -> FCO" (ASCII arrow), "Preco total do roteiro", subject == `Orbita multi: {name} R$ {total} (N trechos)` no formato D-20.

    Todos os testes devem FALHAR ao rodar (RED esperado): model/schema ok mas service/route/email ainda nao implementados (Plans 02/03/04). Falha aceitavel: AssertionError ou ImportError dentro do teste. NAO e aceitavel: SyntaxError, fixture nao encontrada, collection error.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_multi_leg_model.py tests/test_multi_leg_service.py tests/test_multi_leg_routes.py tests/test_multi_leg_polling.py tests/test_multi_leg_email.py --collect-only -q</automated>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_multi_leg_service.py tests/test_multi_leg_routes.py tests/test_multi_leg_polling.py tests/test_multi_leg_email.py --tb=no -q 2>&1 | grep -E "[0-9]+ failed" || (echo "expected RED state (>0 failed), none found" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "def test_leg_cascade_delete" tests/test_multi_leg_model.py` retorna 1 match
    - `grep -rn "def test_chain_validation_rejects_overlap" tests/test_multi_leg_model.py` retorna 1 match
    - `grep -rn "def test_min_max_legs" tests/test_multi_leg_model.py` retorna 1 match
    - `grep -rn "def test_uses_route_cache_before_serpapi" tests/test_multi_leg_service.py` retorna 1 match
    - `grep -rn "def test_persists_multi_snapshot_with_details" tests/test_multi_leg_service.py` retorna 1 match
    - `grep -rn "def test_picks_cheapest_total" tests/test_multi_leg_service.py` retorna 1 match
    - `grep -rn "def test_prediction_uses_total_median" tests/test_multi_leg_service.py` retorna 1 match
    - `grep -rn "def test_create_multi_leg_group_valid" tests/test_multi_leg_routes.py` retorna 1 match
    - `grep -rn "def test_signal_on_total_price" tests/test_multi_leg_polling.py` retorna 1 match
    - `grep -rn "def test_consolidated_multi_has_chain_and_total" tests/test_multi_leg_email.py` retorna 1 match
    - Pytest `--collect-only` retorna 0 collection errors (arquivos importam sem erro)
    - Pytest rodado SEM `--collect-only` sobre test_multi_leg_service/routes/polling/email reporta >=1 FAILED (RED esperado; garante que nao passam por acidente). Comando: `.venv/Scripts/python.exe -m pytest tests/test_multi_leg_service.py tests/test_multi_leg_routes.py tests/test_multi_leg_polling.py tests/test_multi_leg_email.py --tb=no -q` grep `failed` retorna pelo menos 1 linha.
  </acceptance_criteria>
  <done>5 arquivos de teste coletaveis e em estado RED (falham por implementacao ausente, nao por erro de arquivo). Pytest reporta >0 failed quando rodado sem --collect-only.</done>
</task>

</tasks>

<verification>
1. `.venv/Scripts/python.exe -m alembic upgrade head` aplica a nova migration sem erro.
2. `.venv/Scripts/python.exe -m alembic downgrade -1 && alembic upgrade head` reverte e reaplica.
3. `.venv/Scripts/python.exe -m pytest tests/test_multi_leg_model.py -x -q` executa os 5 testes de model — testes de Pydantic/model devem PASSAR; testes que dependem de service ficam RED.
4. `.venv/Scripts/python.exe -m pytest tests/test_multi_leg_*.py --collect-only -q` coleta sem erros.
5. `.venv/Scripts/python.exe -m pytest tests/test_multi_leg_service.py tests/test_multi_leg_routes.py tests/test_multi_leg_polling.py tests/test_multi_leg_email.py --tb=no -q` reporta >=1 failed (confirma RED).
6. `.venv/Scripts/python.exe -m pytest -q` (full suite) continua verde nos testes pre-existentes.
</verification>

<success_criteria>
- RouteGroupLeg persiste com cascade delete, unique constraint e relationship bidirecional
- FlightSnapshot.details JSON nullable adicionado
- Migration reversivel (upgrade -> downgrade -> upgrade)
- RouteGroupMultiCreate rejeita encadeamentos invalidos com mensagens pt-BR do UI-SPEC
- Fixtures multi_leg_group_factory e multi_leg_snapshot_factory disponiveis no conftest
- 5 arquivos de teste em RED, mapeados 1:1 com MULTI-01..04 + D-19 conforme RESEARCH.md
- Full suite pre-existente continua verde (nenhuma regressao)
</success_criteria>

<output>
Apos completar, criar `.planning/phases/36-multi-leg/36-01-SUMMARY.md` documentando:
- Model, migration e schemas criados
- Lista de testes RED e o requirement que cada um cobre
- Confirmacao de que alembic upgrade/downgrade funciona
- Fixtures adicionadas e como usar
</output>
</content>
</invoke>