---
phase: 31.9-price-fidelity-hygiene
plan: 02
type: execute
wave: 2
depends_on: [31.9-01]
files_modified:
  - app/services/flight_search.py
  - requirements.txt
  - tests/test_flight_search.py
autonomous: true
requirements: [HYG-03]

must_haves:
  truths:
    - "app/services/flight_search.py nao importa fast_flights nem TFSData/FlightData/Passengers"
    - "requirements.txt nao contem fast-flights"
    - "search_flights_ex() sempre usa SerpAPI (apos cache miss); nao existe branch fast-flights no codigo"
    - "Todos os 299+ testes continuam passando apos remocao"
  artifacts:
    - path: "app/services/flight_search.py"
      provides: "Orchestrator limpo usando apenas cache + SerpAPI"
      contains: "SerpApiClient"
      must_not_contain: "fast_flights"
    - path: "requirements.txt"
      provides: "Dependencias sem fast-flights"
      must_not_contain: "fast-flights"
  key_links:
    - from: "app/services/flight_search.py"
      to: "app/services/serpapi_client.py"
      via: "direct call after cache miss"
      pattern: "SerpApiClient\\(\\)"
    - from: "tests/test_flight_search.py"
      to: "app/services/flight_search.py"
      via: "assert no fast_flights import"
      pattern: "fast_flights"
---

<objective>
Remover fast-flights (scraping Google Flights) do codigo: imports, funcao _search_fast_flights, dependencia em requirements.txt, e qualquer branch que dependa dela. Fonte de preco passa a ser exclusivamente SerpAPI + cache in-memory.

Purpose: Eliminar fonte fragil que retorna vazio silenciosamente; simplificar pipeline e preparar terreno para Phase 32 (Travelpayouts cache).
Output: flight_search.py enxuto, requirements.txt sem fast-flights, testes atualizados e verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@app/services/flight_search.py
@app/services/serpapi_client.py
@app/services/flight_cache.py
@requirements.txt
@tests/test_flight_search.py
</context>

<interfaces>
<!-- Interfaces que permanecem apos remocao -->

From app/services/flight_search.py (assinaturas a preservar):
```python
def search_flights(
    origin: str, destination: str, departure_date: str, return_date: str,
    max_results: int = 5, max_stops: int | None = None,
    adults: int = 1, use_cache: bool = True,
) -> tuple[list[dict], dict | None, str]: ...

def search_flights_ex(
    origin: str, destination: str, departure_date: str, return_date: str,
    max_results: int = 5, max_stops: int | None = None,
    adults: int = 1, use_cache: bool = True,
) -> tuple[list[dict], dict | None, str, bool]: ...
```

From app/services/serpapi_client.py:
```python
class SerpApiClient:
    def search_flights_with_insights(
        self, origin, destination, departure_date, return_date,
        max_results, max_stops, adults,
    ) -> tuple[list[dict], dict | None]: ...
```

From app/services/flight_cache.py:
```python
def make_key(origin, destination, dep, ret, max_stops, adults) -> str: ...
def get(key) -> tuple | None: ...
def put(key, value) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Teste que proibe import de fast_flights</name>
  <files>tests/test_flight_search.py</files>
  <read_first>
    - tests/test_flight_search.py (ver padrao de testes existentes, especialmente mocks de SerpApiClient)
    - app/services/flight_search.py (linhas 9-15 — import atual do fast_flights)
  </read_first>
  <behavior>
    - Test 1: ler conteudo de app/services/flight_search.py como texto → NAO contem "fast_flights" nem "FlightData" nem "Passengers" nem "TFSData"
    - Test 2: cache miss → search_flights_ex chama SerpApiClient diretamente (mock SerpApiClient, verificar que foi chamado uma vez; fast-flights nao existe mais como branch intermediario)
    - Test 3: `importlib.import_module("app.services.flight_search")` e inspecionar `sys.modules["app.services.flight_search"].__dict__` → nao contem `_search_fast_flights` nem `_FF_AVAILABLE`
  </behavior>
  <action>
    Adicionar em tests/test_flight_search.py:

    ```python
    def test_flight_search_nao_importa_fast_flights():
        """HYG-03: fast-flights foi removido do orchestrator."""
        from pathlib import Path
        source = Path("app/services/flight_search.py").read_text(encoding="utf-8")
        assert "fast_flights" not in source
        assert "FlightData" not in source
        assert "Passengers" not in source
        assert "TFSData" not in source

    def test_flight_search_nao_expoe_helpers_fast_flights():
        """HYG-03: funcoes auxiliares de fast-flights foram removidas."""
        import app.services.flight_search as fs
        assert not hasattr(fs, "_search_fast_flights")
        assert not hasattr(fs, "_FF_AVAILABLE")

    def test_cache_miss_chama_serpapi_diretamente(monkeypatch):
        """HYG-03: sem fast-flights intermediario, cache miss vai direto pra SerpAPI."""
        from app.services import flight_search, flight_cache
        flight_cache.clear() if hasattr(flight_cache, "clear") else None

        calls = {"count": 0}
        def fake_search(self, **kwargs):
            calls["count"] += 1
            return ([{"price": 1000.0, "airline": "TEST", "flights": [], "type": "Round trip"}], {"any": "insight"})
        monkeypatch.setattr(
            "app.services.serpapi_client.SerpApiClient.search_flights_with_insights",
            fake_search,
        )
        flights, insights, source, cache_hit = flight_search.search_flights_ex(
            "GRU", "LIS", "2026-06-01", "2026-06-15", max_results=3, adults=1,
        )
        assert source == "serpapi"
        assert cache_hit is False
        assert calls["count"] == 1
        assert len(flights) >= 1
    ```

    Rodar: `pytest tests/test_flight_search.py -k "nao_importa_fast_flights or nao_expoe_helpers or cache_miss_chama_serpapi" -x`. Todos devem FALHAR agora (fast-flights ainda presente). Commit: `test(31.9-02): add failing tests proibindo fast-flights em flight_search (HYG-03)`.
  </action>
  <verify>
    <automated>pytest tests/test_flight_search.py -k "nao_importa_fast_flights or nao_expoe_helpers or cache_miss_chama_serpapi" -x 2>&1 | grep -E "(FAILED|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nao_importa_fast_flights\|nao_expoe_helpers\|cache_miss_chama_serpapi" tests/test_flight_search.py` retorna >= 3
    - `pytest tests/test_flight_search.py -k "nao_importa_fast_flights or nao_expoe_helpers"` exit != 0
    - Commit test() criado
  </acceptance_criteria>
  <done>3 testes RED adicionados e falhando.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Remover fast-flights de flight_search.py</name>
  <files>app/services/flight_search.py</files>
  <read_first>
    - app/services/flight_search.py (arquivo inteiro)
  </read_first>
  <action>
    Reescrever app/services/flight_search.py removendo fast-flights completamente. Novo conteudo:

    ```python
    import logging
    import re

    from app.services import flight_cache
    from app.services.serpapi_client import SerpApiClient

    logger = logging.getLogger(__name__)


    def search_flights(
        origin: str,
        destination: str,
        departure_date: str,
        return_date: str,
        max_results: int = 5,
        max_stops: int | None = None,
        adults: int = 1,
        use_cache: bool = True,
    ) -> tuple[list[dict], dict | None, str]:
        """Busca voos via SerpAPI com cache in-memory (30 min TTL).

        Retorna (flights, insights_or_none, source).
        Para saber se foi cache hit, use search_flights_ex.
        """
        flights, insights, source, _ = search_flights_ex(
            origin, destination, departure_date, return_date,
            max_results, max_stops, adults, use_cache,
        )
        return flights, insights, source


    def search_flights_ex(
        origin: str,
        destination: str,
        departure_date: str,
        return_date: str,
        max_results: int = 5,
        max_stops: int | None = None,
        adults: int = 1,
        use_cache: bool = True,
    ) -> tuple[list[dict], dict | None, str, bool]:
        """Versao estendida: retorna (flights, insights, source, was_cache_hit).

        Fonte unica: SerpAPI. Cache in-memory evita chamadas duplicadas no mesmo
        ciclo de polling (HYG-03: fast-flights removido).
        """
        pax = max(1, int(adults))

        cache_key = flight_cache.make_key(
            origin, destination, departure_date, return_date, max_stops, pax
        )
        if use_cache:
            hit = flight_cache.get(cache_key)
            if hit is not None:
                flights_cached, insights_cached, orig_source = hit
                logger.info(
                    "flight_cache HIT %s->%s %s (orig=%s)",
                    origin, destination, departure_date, orig_source,
                )
                return flights_cached[:max_results], insights_cached, orig_source, True

        client = SerpApiClient()
        flights, insights = client.search_flights_with_insights(
            origin=origin,
            destination=destination,
            departure_date=departure_date,
            return_date=return_date,
            max_results=max_results,
            max_stops=max_stops,
            adults=pax,
        )
        if use_cache:
            flight_cache.put(cache_key, (flights, insights, "serpapi"))
        return flights, insights, "serpapi", False


    def _parse_price(price_str: str | None) -> float | None:
        """Helper preservado para compat com possiveis callers; parse de string BRL."""
        if not price_str:
            return None
        cleaned = re.sub(r"[^\d.,]", "", price_str)
        if "," in cleaned and "." in cleaned:
            cleaned = cleaned.replace(".", "").replace(",", ".")
        elif "," in cleaned:
            cleaned = cleaned.replace(",", ".")
        elif "." in cleaned and cleaned.count(".") == 1:
            parts = cleaned.split(".")
            if len(parts[1]) == 3:
                cleaned = cleaned.replace(".", "")
        try:
            return float(cleaned)
        except (ValueError, TypeError):
            return None
    ```

    NOTA: manter _parse_price caso algum teste existente ainda o importe (verificar com grep). Se nenhum teste usa, pode remover tambem.

    Rodar testes do Task 1: `pytest tests/test_flight_search.py -k "nao_importa or nao_expoe or cache_miss_chama"`. Devem passar.

    Commit: `feat(31.9-02): remove fast-flights from flight_search orchestrator (HYG-03)`.
  </action>
  <verify>
    <automated>pytest tests/test_flight_search.py -k "nao_importa_fast_flights or nao_expoe_helpers or cache_miss_chama_serpapi" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fast_flights\|FlightData\|Passengers\|TFSData\|_FF_AVAILABLE" app/services/flight_search.py` retorna 0
    - `pytest tests/test_flight_search.py -k "nao_importa_fast_flights or nao_expoe_helpers or cache_miss_chama_serpapi" -x` exit 0
  </acceptance_criteria>
  <done>flight_search.py sem fast-flights. 3 testes RED agora GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — Remover fast-flights de requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (linha 18: `fast-flights==2.2`)
  </read_first>
  <action>
    Remover a linha `fast-flights==2.2` de requirements.txt (linha 18 no estado atual).

    Opcional: `pip uninstall -y fast-flights` no ambiente local para evitar que Python consiga importar o pacote residual. Nao bloqueante — os testes do Task 1 validam o arquivo fonte, nao o import em runtime.

    Commit: `chore(31.9-02): remove fast-flights from requirements (HYG-03)`.
  </action>
  <verify>
    <automated>grep -c "fast-flights" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fast-flights" requirements.txt` retorna 0
    - `grep -c "fast_flights" requirements.txt` retorna 0
    - requirements.txt ainda contem `fastapi`, `google-search-results`, `slowapi` (remocao nao afetou outras deps)
  </acceptance_criteria>
  <done>requirements.txt sem fast-flights, demais deps intactas.</done>
</task>

<task type="auto">
  <name>Task 4: REFACTOR — Validar regressao global e grep de sanidade</name>
  <files>tests/, app/</files>
  <read_first>
    - Nenhum (comando de verificacao global)
  </read_first>
  <action>
    1. Rodar suite completa: `pytest -x`. Todos os 299+ testes devem passar.

    2. Grep global no codigo fonte (fora de .planning e fora de .git) para confirmar zero residuos de fast-flights:
       `grep -rn "fast_flights\|fast-flights\|FlightData\|TFSData" app/ tests/ scripts/ requirements.txt`

       Resultado esperado: zero matches (ou matches apenas em comentarios historicos/documentacao acidentalmente — remover se houver).

    3. Se suite falhar em algum teste NAO relacionado ao plano (ex: teste que mockava _search_fast_flights), atualizar o teste para usar SerpAPI mock diretamente. Commit separado se for o caso: `fix(31.9-02): update legacy test to mock SerpAPI instead of fast-flights`.

    4. Commit final do plano: `refactor(31.9-02): verify no fast-flights residue in app or tests (HYG-03)`.
  </action>
  <verify>
    <automated>pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` exit 0 (suite verde)
    - `grep -rn "fast_flights\|fast-flights" app/ tests/ scripts/ requirements.txt` retorna 0 linhas (exceto eventualmente arquivos .pyc ou caches — limpar se necessario)
    - `grep -rn "TFSData\|FlightData\|Passengers" app/services/` retorna 0 linhas (Passengers pode existir em outros contextos nao-fastflights; validar que match restante nao vem de fast_flights)
  </acceptance_criteria>
  <done>Suite verde, zero residuos de fast-flights no codigo fonte.</done>
</task>

</tasks>

<verification>
- `grep "fast_flights" app/services/flight_search.py` retorna 0
- `grep "fast-flights" requirements.txt` retorna 0
- `pytest` suite completa verde (299+ testes)
- SerpAPI + cache sao as unicas fontes de preco
</verification>

<success_criteria>
1. HYG-03 — fast-flights removido de imports (flight_search.py), de requirements.txt e de qualquer branch de codigo
2. Suite pytest completa verde apos remocao (sem regressao)
3. SerpAPI e cache in-memory sao as unicas fontes de preco; Phase 32 podera adicionar Travelpayouts sem conflito
</success_criteria>

<output>
After completion, create `.planning/phases/31.9-price-fidelity-hygiene/31.9-02-SUMMARY.md`
</output>
