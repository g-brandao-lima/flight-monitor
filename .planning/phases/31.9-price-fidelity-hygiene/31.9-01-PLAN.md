---
phase: 31.9-price-fidelity-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/dashboard/index.html
  - app/templates/dashboard/detail.html
  - app/services/alert_service.py
  - tests/test_dashboard.py
  - tests/test_alert_service.py
autonomous: true
requirements: [HYG-01, HYG-02]

must_haves:
  truths:
    - "Usuario ve rotulo 'preco de referencia Google Flights' no card do dashboard (nao apenas 'preco' ou 'por pessoa')"
    - "Usuario ve rotulo 'preco de referencia Google Flights' na pagina de detalhe do grupo"
    - "Usuario ve rotulo 'preco de referencia Google Flights' no email consolidado (HTML e plain)"
    - "Usuario ve disclaimer 'pode divergir ate 5% do valor final; bagagem e taxas nao incluidas' proximo a cada preco nos tres contextos (card, detalhe, email)"
  artifacts:
    - path: "app/templates/dashboard/index.html"
      provides: "Rotulo + disclaimer dentro de .card-price-area"
      contains: "preco de referencia Google Flights"
    - path: "app/templates/dashboard/detail.html"
      provides: "Rotulo + disclaimer no bloco de historico de precos"
      contains: "preco de referencia Google Flights"
    - path: "app/services/alert_service.py"
      provides: "Rotulo + disclaimer em _render_consolidated_html e _render_consolidated_plain"
      contains: "preco de referencia Google Flights"
  key_links:
    - from: "app/templates/dashboard/index.html"
      to: "tests/test_dashboard.py"
      via: "HTML response body assertion"
      pattern: "preco de referencia Google Flights"
    - from: "app/services/alert_service.py"
      to: "tests/test_alert_service.py"
      via: "MIMEMultipart payload assertion"
      pattern: "pode divergir ate 5%"
---

<objective>
Adicionar rotulo "preco de referencia Google Flights" e disclaimer de divergencia/bagagem em cards do dashboard, pagina de detalhe do grupo e email consolidado.

Purpose: Alinhar expectativa do usuario — o preco e uma referencia, nao um valor final de checkout.
Output: Templates e email service atualizados com copy nova, testes cobrindo as strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@app/templates/dashboard/index.html
@app/templates/dashboard/detail.html
@app/services/alert_service.py
@tests/test_dashboard.py
@tests/test_alert_service.py
</context>

<copy_strings>
<!-- Strings exatas a usar. Sem acento em "preco" e "referencia" (projeto usa pt-BR sem acentos em templates HTML existentes para consistencia com strings como "preco" ja presente). -->

LABEL_PRINCIPAL: "Preco de referencia Google Flights"
DISCLAIMER: "Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas."
</copy_strings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Testes para rotulo e disclaimer no dashboard e detalhe</name>
  <files>tests/test_dashboard.py</files>
  <read_first>
    - tests/test_dashboard.py (padrao de fixtures e client TestClient existente)
    - app/templates/dashboard/index.html (local onde a copy sera inserida — .card-price-area)
    - app/templates/dashboard/detail.html (bloco de chart_data com h2 "Historico de Precos")
  </read_first>
  <behavior>
    - Test 1: GET "/" autenticado com 1 grupo tendo snapshot → response.text contem "Preco de referencia Google Flights"
    - Test 2: GET "/" autenticado com 1 grupo tendo snapshot → response.text contem "Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas"
    - Test 3: GET "/groups/{id}" autenticado com chart_data populado → response.text contem "Preco de referencia Google Flights"
    - Test 4: GET "/groups/{id}" autenticado → response.text contem "Pode divergir ate 5%"
  </behavior>
  <action>
    Adicionar 4 testes em tests/test_dashboard.py (ou arquivo equivalente que ja tenha fixtures de grupo+snapshot logados). Nomes sugeridos:
    - test_dashboard_card_exibe_rotulo_preco_referencia (HYG-01 per phase requirement)
    - test_dashboard_card_exibe_disclaimer_divergencia (HYG-02)
    - test_detail_page_exibe_rotulo_preco_referencia (HYG-01)
    - test_detail_page_exibe_disclaimer_divergencia (HYG-02)

    Usar fixtures ja existentes (authenticated_client + grupo com snapshot). Asserts do tipo:
      assert "Preco de referencia Google Flights" in response.text
      assert "Pode divergir ate 5% do valor final" in response.text

    Rodar: `pytest tests/test_dashboard.py -k "preco_referencia or disclaimer" -x` e CONFIRMAR que todos os 4 falham (copy ainda nao existe). Commit com mensagem `test(31.9-01): add failing tests for preco referencia label and disclaimer`.
  </action>
  <verify>
    <automated>pytest tests/test_dashboard.py -k "preco_referencia or disclaimer" -x 2>&1 | grep -E "(FAILED|4 failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preco_referencia\|disclaimer" tests/test_dashboard.py` retorna >= 4
    - `pytest tests/test_dashboard.py -k "preco_referencia or disclaimer"` sai com exit code != 0 (testes falham como esperado no RED)
    - Commit existe com prefixo `test(31.9-01):`
  </acceptance_criteria>
  <done>4 testes novos adicionados e falhando. Commit test() criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — Testes para rotulo e disclaimer no email consolidado</name>
  <files>tests/test_alert_service.py</files>
  <read_first>
    - tests/test_alert_service.py (padrao de teste de compose_consolidated_email)
    - app/services/alert_service.py (_render_consolidated_html e _render_consolidated_plain — locais a modificar)
  </read_first>
  <behavior>
    - Test 1: compose_consolidated_email(signals=[], snapshots=[snap], group) → payload HTML contem "Preco de referencia Google Flights"
    - Test 2: Mesmo setup → payload HTML contem "Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas"
    - Test 3: Mesmo setup → payload plain contem "Preco de referencia Google Flights"
    - Test 4: Mesmo setup → payload plain contem "Pode divergir ate 5%"
  </behavior>
  <action>
    Adicionar 4 testes em tests/test_alert_service.py. Nomes sugeridos:
    - test_consolidated_email_html_contem_rotulo_preco_referencia (HYG-01)
    - test_consolidated_email_html_contem_disclaimer (HYG-02)
    - test_consolidated_email_plain_contem_rotulo_preco_referencia (HYG-01)
    - test_consolidated_email_plain_contem_disclaimer (HYG-02)

    Extrair payload com msg.get_payload()[0].get_payload() (plain) e msg.get_payload()[1].get_payload() (html). Reutilizar fixtures/factories de FlightSnapshot e RouteGroup ja existentes no arquivo.

    Rodar: `pytest tests/test_alert_service.py -k "preco_referencia or disclaimer" -x` e CONFIRMAR que todos falham. Commit `test(31.9-01): add failing tests for consolidated email disclaimer copy`.
  </action>
  <verify>
    <automated>pytest tests/test_alert_service.py -k "preco_referencia or disclaimer" -x 2>&1 | grep -E "(FAILED|4 failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preco_referencia\|disclaimer" tests/test_alert_service.py` retorna >= 4
    - `pytest tests/test_alert_service.py -k "preco_referencia or disclaimer"` sai != 0
    - Commit test() criado
  </acceptance_criteria>
  <done>4 testes novos de email adicionados e falhando. Commit test() criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Inserir rotulo + disclaimer no card do dashboard</name>
  <files>app/templates/dashboard/index.html</files>
  <read_first>
    - app/templates/dashboard/index.html linhas 400-455 (bloco .card-price-area com .card-price e .card-price-sub)
  </read_first>
  <action>
    Dentro de `<div class="card-price-area">` em app/templates/dashboard/index.html, IMEDIATAMENTE ACIMA de `<div class="card-price ...">` (linha ~403), inserir rotulo:

    ```html
    <div class="card-price-sub" style="font-size:11px;color:#64748b;margin-bottom:4px;letter-spacing:0.2px;">
        Preco de referencia Google Flights
    </div>
    ```

    Depois, APOS o ultimo `<div class="card-price-sub">` do bloco (apos o snap.source mas antes do sparkline — linha ~443), inserir disclaimer:

    ```html
    <div class="card-price-sub" style="margin-top:6px;font-size:10px;color:#475569;line-height:1.4;font-style:italic;" title="Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas.">
        Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas.
    </div>
    ```

    Rodar testes do Task 1 parte dashboard: `pytest tests/test_dashboard.py -k "dashboard_card"`. Os 2 testes de card devem passar. Commit: `feat(31.9-01): add preco referencia label and disclaimer to dashboard card (HYG-01, HYG-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_dashboard.py -k "dashboard_card_exibe" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Preco de referencia Google Flights" app/templates/dashboard/index.html` retorna >= 1
    - `grep -c "Pode divergir ate 5% do valor final" app/templates/dashboard/index.html` retorna >= 1
    - `pytest tests/test_dashboard.py -k "dashboard_card_exibe"` exit 0
  </acceptance_criteria>
  <done>Card do dashboard exibe rotulo e disclaimer. 2 testes GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — Inserir rotulo + disclaimer na pagina de detalhe</name>
  <files>app/templates/dashboard/detail.html</files>
  <read_first>
    - app/templates/dashboard/detail.html linhas 23-30 (bloco de chart_data com h2 "Historico de Precos" e paragrafos de rota)
  </read_first>
  <action>
    Em app/templates/dashboard/detail.html, dentro do bloco `{% if chart_data.prices %}` (apos linha 27, imediatamente antes do `<canvas id="priceChart">`), adicionar:

    ```html
    <p style="color: #64748b; margin-bottom: 0.25rem; font-size: 12px; font-weight: 600;">Preco de referencia Google Flights</p>
    <p style="color: #475569; margin-bottom: 1rem; font-size: 11px; font-style: italic;">Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas.</p>
    ```

    Rodar: `pytest tests/test_dashboard.py -k "detail_page"`. Os 2 testes de detalhe devem passar. Commit: `feat(31.9-01): add preco referencia label and disclaimer to group detail page (HYG-01, HYG-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_dashboard.py -k "detail_page_exibe" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Preco de referencia Google Flights" app/templates/dashboard/detail.html` retorna >= 1
    - `grep -c "Pode divergir ate 5%" app/templates/dashboard/detail.html` retorna >= 1
    - `pytest tests/test_dashboard.py -k "detail_page_exibe"` exit 0
  </acceptance_criteria>
  <done>Pagina de detalhe exibe rotulo e disclaimer. 2 testes GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: GREEN — Inserir rotulo + disclaimer no email consolidado (HTML e plain)</name>
  <files>app/services/alert_service.py</files>
  <read_first>
    - app/services/alert_service.py linhas 317-497 (_render_consolidated_html e _render_consolidated_plain)
  </read_first>
  <action>
    Em `_render_consolidated_html` (app/services/alert_service.py):

    Dentro do header verde (apos o h2 "Melhor preco: ..." e antes de header_sub — aproximadamente na concatenacao do parts.append do header, linha ~358), injetar rotulo ANTES do header_sub:

    ```python
    label_html = '<p style="margin:4px 0 0;font-size:12px;opacity:0.9;font-weight:600;">Preco de referencia Google Flights</p>'
    ```

    E ao final do header_sub (depois do context_html, antes de fechar o `</div>`), injetar disclaimer:

    ```python
    disclaimer_html = '<p style="margin:8px 0 0;font-size:11px;opacity:0.8;font-style:italic;">Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas.</p>'
    ```

    Resultado: bloco do header fica `... + label_html + header_sub + source_html + context_html + disclaimer_html + '</div>'`.

    Em `_render_consolidated_plain`:

    APOS a linha `lines.append(f"MELHOR PRECO: ...")` (linha ~453), inserir DUAS linhas:

    ```python
    lines.append("Preco de referencia Google Flights")
    lines.append("(Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas)")
    ```

    Rodar: `pytest tests/test_alert_service.py -k "consolidated_email"`. Os 4 testes de email devem passar. Commit: `feat(31.9-01): add preco referencia label and disclaimer to consolidated email (HYG-01, HYG-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_alert_service.py -k "consolidated_email" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Preco de referencia Google Flights" app/services/alert_service.py` retorna >= 2 (html e plain)
    - `grep -c "Pode divergir ate 5%" app/services/alert_service.py` retorna >= 2
    - `pytest tests/test_alert_service.py -k "consolidated_email"` exit 0
  </acceptance_criteria>
  <done>Email consolidado (html e plain) contem rotulo e disclaimer. 4 testes GREEN.</done>
</task>

<task type="auto">
  <name>Task 6: REFACTOR — Verificar regressao e ausencia de "preco" solto</name>
  <files>tests/</files>
  <read_first>
    - tests/ (rodar suite completa)
  </read_first>
  <action>
    Rodar suite completa: `pytest -x`. Todos os 299+ testes devem passar (sem regressao).

    Grep de sanidade: confirmar que os 3 arquivos modificados contem as strings canonicas em portugues:
      - grep "Preco de referencia Google Flights" app/templates/dashboard/index.html app/templates/dashboard/detail.html app/services/alert_service.py

    Deve retornar >= 4 ocorrencias (1 index, 1 detail, 2 alert_service — html + plain).

    Commit final: `refactor(31.9-01): consolidate preco referencia copy across dashboard, detail and email (HYG-01, HYG-02)`.
  </action>
  <verify>
    <automated>pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` exit 0 (suite completa verde)
    - `grep -c "Preco de referencia Google Flights" app/templates/dashboard/index.html app/templates/dashboard/detail.html app/services/alert_service.py` soma >= 4
    - `grep -c "Pode divergir ate 5%" app/templates/dashboard/index.html app/templates/dashboard/detail.html app/services/alert_service.py` soma >= 4
  </acceptance_criteria>
  <done>Suite verde, strings canonicas presentes nos 3 arquivos, sem regressao.</done>
</task>

</tasks>

<verification>
- Dashboard card mostra "Preco de referencia Google Flights" + disclaimer (grep em index.html)
- Pagina de detalhe mostra rotulo + disclaimer (grep em detail.html)
- Email consolidado (html + plain) contem rotulo + disclaimer (grep em alert_service.py)
- Suite `pytest` inteira passa
</verification>

<success_criteria>
1. HYG-01 — Rotulo "Preco de referencia Google Flights" visivel em card, detalhe e email
2. HYG-02 — Disclaimer "Pode divergir ate 5% do valor final; bagagem e taxas nao incluidas" visivel proximo a cada preco nos tres contextos
3. Suite pytest completa verde (sem regressao nos 299+ testes existentes)
</success_criteria>

<output>
After completion, create `.planning/phases/31.9-price-fidelity-hygiene/31.9-01-SUMMARY.md`
</output>
