---
phase: 05-web-dashboard
plan: 03
type: tdd
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/routes/dashboard.py
  - app/templates/dashboard/create.html
  - app/templates/dashboard/edit.html
  - tests/test_dashboard.py
autonomous: false
requirements:
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Usuario pode criar novo Grupo de Rota pelo formulario no dashboard sem usar a API diretamente"
    - "Usuario pode editar grupo existente pelo formulario pre-preenchido no dashboard"
    - "Usuario pode desativar/ativar grupo pelo botao toggle no dashboard"
    - "Formularios usam POST padrao com redirect 303 apos sucesso (PRG pattern)"
    - "IATA codes sao convertidos para uppercase server-side antes de validar"
    - "Origins e destinations sao inseridos como string separada por virgula e splitados server-side"
    - "Erros de validacao re-renderizam o formulario com mensagem de erro"
  artifacts:
    - path: "app/templates/dashboard/create.html"
      provides: "Formulario HTML para criar grupo"
      contains: "method=\"POST\""
    - path: "app/templates/dashboard/edit.html"
      provides: "Formulario HTML pre-preenchido para editar grupo"
      contains: "method=\"POST\""
    - path: "app/routes/dashboard.py"
      provides: "Rotas POST para criar, editar e toggle grupo"
      exports: ["router"]
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/models.py"
      via: "db.add(RouteGroup), db.commit() for create/edit"
      pattern: "RouteGroup\\("
    - from: "app/templates/dashboard/create.html"
      to: "app/routes/dashboard.py"
      via: "form action=/groups/create method=POST"
      pattern: "action.*groups/create"
---

<objective>
Criar formularios HTML (criar e editar grupo) com POST server-side, botao toggle ativo/inativo, e checkpoint de verificacao visual. Segue PRG pattern (Post-Redirect-Get).

Purpose: Entregar DASH-03 (criar grupo) e DASH-04 (editar/desativar grupo) via formularios no dashboard. Checkpoint final para verificacao humana de toda a interface.
Output: Formularios funcionais + checkpoint humano para aprovar a interface completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-web-dashboard/05-CONTEXT.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md
@.planning/phases/05-web-dashboard/05-02-SUMMARY.md

@app/models.py
@app/routes/dashboard.py
@app/schemas.py
@app/templates/base.html
@tests/conftest.py
@tests/test_dashboard.py

<interfaces>
<!-- From existing schemas.py (reuse validation logic) -->
```python
IATA_PATTERN = re.compile(r"^[A-Z]{3}$")

class RouteGroupCreate(BaseModel):
    name: str
    origins: list[str]
    destinations: list[str]
    duration_days: int
    travel_start: datetime.date
    travel_end: datetime.date
    target_price: float | None = None
```

<!-- From existing route_groups.py (reuse limit check) -->
```python
from app.services.route_group_service import check_active_group_limit
```

<!-- Form(...) pattern from RESEARCH.md -->
```python
from fastapi import Form
from fastapi.responses import RedirectResponse

@router.post("/groups/create")
def create_group_form(
    name: str = Form(...),
    origins: str = Form(...),  # "GRU,CGH" -> split to ["GRU", "CGH"]
    ...
    db: Session = Depends(get_db),
):
    # validate, create, redirect
    return RedirectResponse(url="/", status_code=303)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED/GREEN para formularios (criar, editar, toggle) + templates</name>
  <files>tests/test_dashboard.py, app/routes/dashboard.py, app/templates/dashboard/create.html, app/templates/dashboard/edit.html</files>
  <read_first>
    - app/routes/dashboard.py (rotas existentes do Plan 02 — adicionar novas rotas aqui)
    - tests/test_dashboard.py (testes existentes do Plan 02 — adicionar novos testes aqui)
    - app/schemas.py (IATA_PATTERN, RouteGroupCreate — referencia de validacao)
    - app/services/route_group_service.py (check_active_group_limit — reusar para limit de 10 grupos)
    - app/templates/base.html (template base para heranca)
    - app/models.py (RouteGroup campos para form)
  </read_first>
  <behavior>
    - test_create_form_page_returns_html: GET /groups/create retorna 200 com HTML contendo form e inputs
    - test_create_group_via_form: POST /groups/create com form data valido cria grupo no banco e redireciona para / (status 303)
    - test_create_group_uppercase_iata: POST com origins="gru,cgh" (lowercase) cria grupo com origins=["GRU","CGH"] no banco
    - test_create_group_validation_error: POST com name vazio re-renderiza form com mensagem de erro
    - test_create_group_invalid_iata: POST com origins="XX" re-renderiza form com mensagem sobre IATA invalido
    - test_edit_form_page_prefilled: GET /groups/{id}/edit retorna 200 com form pre-preenchido (nome do grupo no input value)
    - test_edit_group_via_form: POST /groups/{id}/edit com form data valido atualiza grupo no banco e redireciona para /
    - test_toggle_group_active: POST /groups/{id}/toggle em grupo ativo desativa e redireciona; POST novamente reativa
    - test_toggle_respects_limit: POST /groups/{id}/toggle em grupo inativo quando ja existem 10 ativos re-renderiza com erro
  </behavior>
  <action>
    1. Adicionar 9 testes a `tests/test_dashboard.py` conforme behavior.
       - Para POST forms: usar client.post("/groups/create", data={...}, follow_redirects=False) e verificar status_code==303
       - Para verificar redirect: checar response.headers["location"] == "/"
       - Para verificar criacao no banco: db.query(RouteGroup).filter_by(name="...").first() is not None
       - Para test_toggle_respects_limit: criar 10 grupos ativos + 1 inativo, POST toggle no inativo

    2. Criar `app/templates/dashboard/create.html` que extends base.html:
       - Titulo "Novo Grupo de Rota"
       - Form method="POST" action="/groups/create"
       - Campos: name (text, required), origins (text, placeholder "GRU,CGH", required, style text-transform uppercase), destinations (text, placeholder "LIS,OPO", required, style text-transform uppercase), duration_days (number, min=1, required), travel_start (date, required), travel_end (date, required), target_price (number, step=0.01, optional)
       - Labels em portugues: "Nome do Grupo", "Origens (codigos IATA separados por virgula)", etc.
       - Se error no context: exibir mensagem de erro em vermelho acima do form
       - Botao submit "Criar Grupo"
       - Link "Cancelar" para /

    3. Criar `app/templates/dashboard/edit.html` que extends base.html:
       - Titulo "Editar Grupo: {{ group.name }}"
       - Form method="POST" action="/groups/{{ group.id }}/edit"
       - Mesmos campos que create.html mas com values pre-preenchidos: value="{{ group.name }}", value="{{ group.origins | join(',') }}", etc.
       - Se error no context: exibir mensagem de erro em vermelho
       - Botao submit "Salvar Alteracoes"
       - Link "Cancelar" para /

    4. Adicionar rotas em `app/routes/dashboard.py`:
       - GET "/groups/create" -> create_group_page: retorna TemplateResponse("dashboard/create.html")
       - POST "/groups/create" -> create_group_form: recebe Form(...) fields, converte origins/destinations para uppercase e split por virgula, valida IATA (regex ^[A-Z]{3}$), valida campos obrigatorios, verifica limite de 10 grupos ativos. Se erro: re-renderiza create.html com error message. Se ok: cria RouteGroup, db.add, db.commit, RedirectResponse("/", 303)
       - GET "/groups/{group_id}/edit" -> edit_group_page: busca grupo por ID (404 se nao existe), retorna TemplateResponse("dashboard/edit.html", context={"group": group})
       - POST "/groups/{group_id}/edit" -> edit_group_form: recebe Form(...) fields, mesma validacao que create. Se erro: re-renderiza edit.html. Se ok: atualiza grupo, db.commit, RedirectResponse("/", 303)
       - POST "/groups/{group_id}/toggle" -> toggle_group: busca grupo, inverte is_active. Se ativando e ja tem 10 ativos: redirect com erro. Se ok: db.commit, RedirectResponse("/", 303)

    5. Na pagina index.html (do Plan 02), adicionar na card de cada grupo:
       - Link "Editar" -> /groups/{group.group.id}/edit
       - Form inline com botao "Desativar"/"Ativar" -> POST /groups/{group.group.id}/toggle
       - Indicador visual se grupo esta inativo (texto em cinza ou badge "Inativo")

    6. Executar testes: `python -m pytest tests/test_dashboard.py -x -q` — todos devem passar
    7. Executar suite completa: `python -m pytest tests/ -x -q` — verificar nada quebrou
    8. Aplicar checklist de revisao CLAUDE.md 4.1: DRY, SRP, nomes, sem print debug, complexidade max 5
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_dashboard.py -x -q mostra 0 falhas com 21+ testes (12 do Plan 02 + 9 novos)
    - python -m pytest tests/ -x -q suite completa passa
    - GET /groups/create retorna HTML com form contendo inputs name, origins, destinations, duration_days, travel_start, travel_end, target_price
    - POST /groups/create com dados validos cria RouteGroup no banco e retorna 303
    - POST /groups/create com origins="gru" cria grupo com origins=["GRU"] (uppercase)
    - GET /groups/{id}/edit retorna HTML com form pre-preenchido (value= nos inputs)
    - POST /groups/{id}/toggle inverte is_active e retorna 303
    - create.html e edit.html contem method="POST"
    - index.html contem links "Editar" e botao "Desativar"/"Ativar" por grupo
  </acceptance_criteria>
  <done>Formularios de criar e editar grupo funcionais com validacao server-side, toggle ativo/inativo, PRG pattern, IATA uppercase. Checklist de revisao aplicado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual completa do dashboard</name>
  <files>N/A</files>
  <action>
    Verificacao visual humana do dashboard completo. Nenhuma automacao nesta task.
    O usuario deve testar todas as paginas no navegador conforme passos em how-to-verify.
  </action>
  <what-built>
    Dashboard web completo com:
    - Pagina principal listando todos os grupos com melhor preco, rota mais barata e badge de sinal
    - Pagina de detalhe com grafico Chart.js de historico de precos (ultimas 2 semanas)
    - Formulario de criar novo grupo com validacao de IATA codes
    - Formulario de editar grupo pre-preenchido
    - Botao toggle ativo/inativo por grupo
    - Layout responsivo com meta viewport
    - Navegacao por links no topo (sem sidebar)
    - Interface em portugues
  </what-built>
  <how-to-verify>
    1. Iniciar servidor: `python main.py`
    2. Abrir http://localhost:8000/ no navegador
    3. Verificar que a pagina lista os grupos existentes (se houver) com preco e badges
    4. Clicar em um grupo para ver o detalhe com grafico de precos
    5. Clicar em "Novo Grupo" no topo e preencher formulario (ex: nome "Teste", origens "gru", destinos "lis", duracao 10, periodo qualquer)
    6. Verificar que o grupo foi criado e aparece na lista
    7. Clicar em "Editar" no grupo criado e verificar campos pre-preenchidos
    8. Clicar em "Desativar" no grupo e verificar que fica marcado como inativo
    9. Redimensionar janela para largura de celular (~375px) e verificar que layout se adapta
    10. Verificar que nao ha sidebar, apenas links no topo
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Usuario aprovou visualmente a interface completa do dashboard</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` suite completa verde (todos os testes, incluindo fases anteriores)
- Dashboard funcional no navegador com todas as paginas
- Formularios criam e editam grupos corretamente
- Layout responsivo verificado em largura mobile
</verification>

<success_criteria>
- Formulario de criar grupo funcional com validacao IATA (DASH-03)
- Formulario de editar e botao toggle funcional (DASH-04)
- Interface completa aprovada visualmente pelo usuario
- Suite de testes completa verde
- Checklist de revisao CLAUDE.md aplicado
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-03-SUMMARY.md`
</output>
