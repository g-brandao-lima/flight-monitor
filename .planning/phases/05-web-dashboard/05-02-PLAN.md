---
phase: 05-web-dashboard
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/routes/dashboard.py
  - app/templates/base.html
  - app/templates/dashboard/index.html
  - app/templates/dashboard/detail.html
  - tests/test_dashboard.py
  - main.py
autonomous: true
requirements:
  - ALRT-03
  - DASH-01
  - DASH-02
  - DASH-05

must_haves:
  truths:
    - "GET / retorna HTML com lista de todos os grupos, melhor preco atual formatado em BRL e badge de sinal ativo"
    - "GET /groups/{id} retorna HTML com dados JSON para Chart.js mostrando historico de precos da rota mais barata"
    - "Pagina de detalhe mostra mensagem 'Nenhum dado coletado ainda' quando grupo nao tem snapshots"
    - "Todas as paginas tem meta viewport e CSS responsivo (media query max-width 768px)"
    - "Navegacao via links no topo (Grupos, Novo Grupo) sem sidebar"
    - "Badge de sinal: cinza (nenhum), amarelo (MEDIA), laranja (ALTA), vermelho (MAXIMA)"
  artifacts:
    - path: "app/routes/dashboard.py"
      provides: "Rotas HTML para dashboard"
      exports: ["router"]
    - path: "app/templates/base.html"
      provides: "Template base com nav, viewport, CSS inline"
      contains: "meta name=\"viewport\""
    - path: "app/templates/dashboard/index.html"
      provides: "Lista de grupos com preco e badge"
      contains: "{% extends"
    - path: "app/templates/dashboard/detail.html"
      provides: "Detalhe do grupo com Chart.js"
      contains: "chart.js"
    - path: "tests/test_dashboard.py"
      provides: "Testes de integracao para rotas do dashboard"
      min_lines: 60
  key_links:
    - from: "app/routes/dashboard.py"
      to: "app/services/dashboard_service.py"
      via: "import get_groups_with_summary, get_price_history, format_price_brl"
      pattern: "from app\\.services\\.dashboard_service import"
    - from: "app/routes/dashboard.py"
      to: "app/templates/"
      via: "Jinja2Templates(directory=\"app/templates\")"
      pattern: "Jinja2Templates"
    - from: "main.py"
      to: "app/routes/dashboard.py"
      via: "app.include_router(dashboard_router)"
      pattern: "include_router.*dashboard"
---

<objective>
Criar rotas HTML do dashboard (index + detail), templates Jinja2 com heranca, e registrar no main.py. Pagina principal lista grupos com preco e badge de sinal. Pagina de detalhe mostra grafico Chart.js.

Purpose: Entregar a camada de apresentacao que consome o dashboard_service.py do Plan 01. Cobre ALRT-03, DASH-01, DASH-02, DASH-05.
Output: Dashboard funcional com lista de grupos e grafico de historico de precos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-web-dashboard/05-CONTEXT.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md

@app/models.py
@app/database.py
@main.py
@tests/conftest.py

<interfaces>
<!-- From Plan 01 dashboard_service.py -->
```python
from app.services.dashboard_service import (
    get_groups_with_summary,  # (db: Session) -> list[dict] with keys "group", "cheapest_snapshot", "signal"
    get_price_history,        # (db: Session, group_id: int, days: int = 14) -> dict with keys "labels", "prices", "route"
    format_price_brl,         # (price: float) -> str like "R$ 3.500,00"
)
```

<!-- From existing main.py -->
```python
# Current router registration pattern:
app.include_router(route_groups_router, prefix="/api/v1")
app.include_router(alerts_router, prefix="/api/v1")

# Current root endpoint (will be replaced by dashboard):
@app.get("/")
def root():
    return {"status": "ok", "app": "Flight Monitor"}
```

<!-- Jinja2Templates pattern (from RESEARCH.md) -->
```python
from fastapi.templating import Jinja2Templates
templates = Jinja2Templates(directory="app/templates")

@router.get("/", response_class=HTMLResponse)
def dashboard_index(request: Request, db: Session = Depends(get_db)):
    return templates.TemplateResponse(
        request=request,
        name="dashboard/index.html",
        context={"groups": groups},
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED para rotas do dashboard + templates base e index</name>
  <files>tests/test_dashboard.py, app/templates/base.html, app/templates/dashboard/index.html, app/templates/dashboard/detail.html, app/routes/dashboard.py, main.py</files>
  <read_first>
    - main.py (current root endpoint "/" que sera substituido; router registration pattern)
    - app/routes/dashboard.py (nao existe ainda — sera criado)
    - app/services/dashboard_service.py (funcoes disponiveis: get_groups_with_summary, get_price_history, format_price_brl)
    - tests/conftest.py (fixtures db e client disponiveis)
    - app/models.py (RouteGroup, FlightSnapshot, DetectedSignal — para criar dados de teste)
  </read_first>
  <behavior>
    - test_index_returns_html: GET / retorna status 200 com content-type text/html
    - test_index_shows_group_names: cria 2 grupos, GET / retorna HTML contendo ambos os nomes
    - test_index_shows_cheapest_price: cria grupo + snapshot com price=3500.0, GET / retorna HTML contendo "R$ 3.500,00"
    - test_index_shows_signal_badge_alta: cria grupo + sinal ALTA recente, GET / retorna HTML contendo "ALTA" e background-color laranja (#f97316)
    - test_index_shows_no_signal_badge: grupo sem sinal, GET / retorna HTML contendo "Sem sinal"
    - test_index_has_viewport_meta: GET / retorna HTML contendo meta name="viewport"
    - test_index_has_responsive_css: GET / retorna HTML contendo "@media" e "max-width"
    - test_index_has_nav_links: GET / retorna HTML contendo href="/" e href="/groups/create"
    - test_detail_returns_html: GET /groups/1 retorna 200 com text/html (grupo existe)
    - test_detail_shows_chart_data: cria grupo + snapshots, GET /groups/{id} retorna HTML contendo "chart.js" CDN e dados JSON
    - test_detail_empty_group_message: grupo sem snapshots, GET /groups/{id} retorna HTML contendo "Nenhum dado coletado ainda"
    - test_detail_404_nonexistent: GET /groups/999 retorna 404
  </behavior>
  <action>
    1. Criar `tests/test_dashboard.py` com os 12 testes listados em behavior. Todos usam fixture `client` e `db` do conftest.py.
       - Para testes com dados: criar RouteGroup, FlightSnapshot, DetectedSignal via db.add() antes do request
       - Usar `response.text` para verificar conteudo HTML (assertar com `in response.text`)
       - Para badge colors: verificar que string "#f97316" (laranja/ALTA) aparece no HTML

    2. Criar `app/templates/base.html` com:
       - DOCTYPE html, lang="pt-BR"
       - meta charset UTF-8, meta viewport width=device-width initial-scale=1.0
       - title block: {% block title %}Flight Monitor{% endblock %}
       - style tag com CSS inline (per D-11): body font-family system-ui, nav background #1e293b, nav a color white
       - Navegacao no topo (per D-03): links "Grupos" (href="/") e "Novo Grupo" (href="/groups/create")
       - Media query @media (max-width: 768px) para container padding e layout single-column
       - {% block head %} para paginas que precisam de scripts extras (Chart.js)
       - {% block content %} para conteudo da pagina

    3. Criar `app/templates/dashboard/index.html` que extends base.html:
       - Titulo "Grupos de Rota"
       - Para cada grupo em groups: card com nome do grupo (link para /groups/{group.group.id}), badge de sinal (per D-08/D-09), melhor preco formatado, rota mais barata (origin -> destination)
       - Badge colors: cinza #94a3b8 (sem sinal), amarelo #eab308 (MEDIA), laranja #f97316 (ALTA), vermelho #ef4444 (MAXIMA)
       - Se grupo nao tem snapshot: mostrar "Aguardando coleta"
       - Card layout responsivo: grid com 1 coluna em mobile, 2-3 colunas em desktop

    4. Criar `app/templates/dashboard/detail.html` que extends base.html:
       - Titulo com nome do grupo
       - Se chart_data.prices vazio: mostrar "Nenhum dado coletado ainda" (sem canvas)
       - Se tem dados: canvas id="priceChart" + script Chart.js CDN (https://cdn.jsdelivr.net/npm/chart.js@4.5.1) + script com new Chart() usando {{ chart_data | tojson }}
       - Informacoes do grupo: origens, destinos, duracao, periodo de viagem
       - Rota do grafico exibida como subtitulo (chart_data.route)
       - Link "Voltar" para /

    5. Criar `app/routes/dashboard.py`:
       - APIRouter(tags=["dashboard"]) sem prefix (rotas em / diretamente)
       - templates = Jinja2Templates(directory="app/templates")
       - GET "/" -> dashboard_index: chama get_groups_with_summary(db), passa format_price_brl como funcao do template context, retorna TemplateResponse("dashboard/index.html")
       - GET "/groups/{group_id}" -> dashboard_detail: busca grupo por ID (404 se nao existe), chama get_price_history(db, group_id), retorna TemplateResponse("dashboard/detail.html")

    6. Atualizar `main.py`:
       - Importar dashboard router: `from app.routes.dashboard import router as dashboard_router`
       - Registrar: `app.include_router(dashboard_router)` (sem prefix, pois rotas ja sao / e /groups/{id})
       - REMOVER o endpoint root atual (`@app.get("/") def root()`) pois sera substituido pelo dashboard index

    7. Executar testes: `python -m pytest tests/test_dashboard.py -x -q` — todos devem PASSAR (GREEN)
    8. Executar suite completa: `python -m pytest tests/ -x -q` — verificar que nada quebrou
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_dashboard.py -x -q mostra 0 falhas com 12+ testes
    - python -m pytest tests/ -x -q suite completa passa
    - GET / retorna HTML com lista de grupos (nao mais JSON {"status": "ok"})
    - GET /groups/{id} retorna HTML com dados Chart.js ou mensagem "Nenhum dado coletado ainda"
    - base.html contem meta name="viewport" e @media (max-width: 768px)
    - index.html contem badges com cores #94a3b8, #eab308, #f97316, #ef4444
    - detail.html contem "cdn.jsdelivr.net/npm/chart.js@4.5.1"
    - main.py contem "include_router(dashboard_router)" e NAO contem "def root()"
  </acceptance_criteria>
  <done>Dashboard index e detail funcionais com testes passando. Lista de grupos com preco e badges. Grafico Chart.js no detalhe. Layout responsivo com meta viewport.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_dashboard.py -x -q` passa com 12+ testes
- `python -m pytest tests/ -x -q` suite completa verde
- `curl http://localhost:8000/` retorna HTML (nao JSON)
- `curl http://localhost:8000/groups/1` retorna HTML com chart ou 404
- Templates herdam de base.html com nav e viewport
</verification>

<success_criteria>
- Dashboard index lista todos os grupos com melhor preco e badge de sinal ativo (ALRT-03, DASH-01)
- Dashboard detail mostra grafico de historico de precos com Chart.js (DASH-02)
- Layout responsivo com meta viewport e media queries (DASH-05)
- Navegacao por links no topo sem sidebar (D-03)
- Badges coloridos por urgencia (D-08, D-09)
- Todos os testes passando, suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-02-SUMMARY.md`
</output>
